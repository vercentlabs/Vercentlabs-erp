import fs from "node:fs";

const HEADER = [
  "requirement_id","module_or_platform","capability","expected_behavior",
  "existing_domain_code","existing_http_endpoint","existing_frontend",
  "existing_worker_or_job","existing_database_support","permission_and_scope",
  "missing_parts","implementation_status","test_evidence","real_runtime_evidence",
  "severity","dependency","target_phase","verified_commit",
];

const rows = [
  {
    requirement_id: "SEC-CRM-001",
    module_or_platform: "shared-platform",
    capability: "CRM generic-resource mutation authorization",
    expected_behavior: "Every CRM_RESOURCE_KEYS mutation (POST /api/crm/[resource], PATCH/DELETE /api/crm/[resource]/[id]) requires either a documented manage permission or an explicit, justified self-scoped exemption; anything else is denied by default.",
    existing_domain_code: "apps/web/src/features/crm/shared/crm-context.ts (requireCrmMutationAccess) + resource-permissions.ts (resolveCrmMutationPermission)",
    existing_http_endpoint: "apps/web/src/app/api/crm/[resource]/route.ts, apps/web/src/app/api/crm/[resource]/[id]/route.ts",
    existing_frontend: "n/a (server-side policy, not a screen)",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a (policy layer, not schema)",
    permission_and_scope: "Deny-by-default for ~40 of 62 CRM_RESOURCE_KEYS with no RESOURCE_MANAGE_PERMISSIONS entry; one documented exemption (saved-views, per-user SQL scope)",
    missing_parts: "none for the generic boundary itself — remaining work is extending RESOURCE_MANAGE_PERMISSIONS as new resource-backed screens ship, per the file's own documented policy",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "apps/web/src/features/crm/shared/resource-permissions.test.ts (5 tests, exhaustive over CRM_RESOURCE_KEYS)",
    real_runtime_evidence: "apps/web/e2e/crm-authorization.spec.ts — real HTTP requests against the running server/DB: POST /api/crm/ai-feedback now 403s (previously would have succeeded for any crm.view member including the org owner), saved-views create/delete and leads create still succeed",
    severity: "P0 (was: any CRM viewer could mutate ~40 resources with zero manage-tier check)",
    dependency: "none",
    target_phase: "Checkpoint A",
    verified_commit: "a0992cb0",
  },
  {
    requirement_id: "SEC-APPROVAL-001",
    module_or_platform: "shared-platform",
    capability: "Approval decision authorization (cancel)",
    expected_behavior: "Only the original requester of a pending approval, or someone holding approvals.manage, may cancel it. approved/rejected already require the underlying module's own permission (verified pre-existing, not this session's fix).",
    existing_domain_code: "services/api/src/core/approvals.js (decideApproval)",
    existing_http_endpoint: "apps/web/src/app/api/approvals/[id]/decide/route.ts",
    existing_frontend: "n/a (policy layer)",
    existing_worker_or_job: "n/a",
    existing_database_support: "public.approval_requests (existing table, no migration needed)",
    permission_and_scope: "Any authenticated org member could previously cancel any other user's pending approval — cancellation skipped both the SoD check (by design, correctly) and any permission check (not by design).",
    missing_parts: "none for this fix",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "services/api/tests/platform-approvals.test.mjs (2 new tests: bystander blocked, approvals.manage holder allowed) — full backend suite 1090/1090",
    real_runtime_evidence: "apps/web/e2e/platform-security.spec.ts covers the route's happy path end-to-end; the specific denial case is proven at the unit level, not yet E2E, because it needs a genuinely restricted (non-owner) session — see GAP-CRM-RESTRICTED-FIXTURE below",
    severity: "P1",
    dependency: "none",
    target_phase: "Checkpoint A",
    verified_commit: "d7e8fe88",
  },
  {
    requirement_id: "SEC-APPROVAL-002",
    module_or_platform: "shared-platform",
    capability: "Approval list visibility",
    expected_behavior: "GET /api/approvals scopes results to the caller's own requested/assigned rows unless they hold approvals.manage — the same permission the shell's badge count was already correctly gated on.",
    existing_domain_code: "services/api/src/core/approvals.js (listApprovals)",
    existing_http_endpoint: "apps/web/src/app/api/approvals/route.ts",
    existing_frontend: "apps/web/src/app/(workspace)/approvals/approvals-client.tsx (existing screen, unchanged by this fix)",
    existing_worker_or_job: "n/a",
    existing_database_support: "public.approval_requests",
    permission_and_scope: "Previously took a bare organizationId with no caller-identity filtering at all — any authenticated org member saw every pending approval org-wide (amounts, requester, entity_type), regardless of role.",
    missing_parts: "none for this fix",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "services/api/tests/platform-approvals.test.mjs (2 tests: with/without approvals.manage) — full backend suite 1090/1090",
    real_runtime_evidence: "apps/web/e2e/platform-security.spec.ts proves the route works end-to-end for the owner (who has approvals.manage, so this doesn't prove the SCOPED case E2E — see GAP-CRM-RESTRICTED-FIXTURE below)",
    severity: "P2 (information disclosure, not privilege escalation — approve/reject themselves were already gated at the module layer)",
    dependency: "none",
    target_phase: "Checkpoint A",
    verified_commit: "d7e8fe88",
  },
  {
    requirement_id: "SEC-ORIGIN-001",
    module_or_platform: "shared-platform",
    capability: "Same-origin/CSRF consistency on cookie-authenticated mutations",
    expected_behavior: "Every cookie-authenticated mutation route calls assertSameOriginOrMobile like the CRM mutation routes already did.",
    existing_domain_code: "services/api/src/core/security.js (assertSameOriginOrMobile — pre-existing)",
    existing_http_endpoint: "apps/web/src/app/api/auth/logout/route.ts, apps/web/src/app/api/notifications/route.ts, apps/web/src/app/api/notifications/[id]/read/route.ts, apps/web/src/app/api/approvals/[id]/decide/route.ts",
    existing_frontend: "n/a",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "4 routes were missing the check that every CRM mutation route already had. Session cookie is SameSite=Lax, which already blocks a forced cross-site POST/PATCH — this was a consistency/defense-in-depth gap, not a live, exploitable CSRF path (verified: SameSite=Lax in apps/web/src/core/session.ts).",
    missing_parts: "none for these 4 routes; a full route-by-route audit of every OTHER mutation route in the app (beyond CRM/notifications/approvals/logout) has not been performed yet",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "n/a (the check itself is a single existing, already-tested function; no new unit test needed)",
    real_runtime_evidence: "apps/web/e2e/platform-security.spec.ts — real requests prove all 4 routes still work for their real same-origin callers after the check was added, including that a revoked logout session is genuinely rejected (401) afterward",
    severity: "P3 (defense-in-depth, not a live exploit given SameSite=Lax)",
    dependency: "none",
    target_phase: "Checkpoint A",
    verified_commit: "d7e8fe88",
  },
  {
    requirement_id: "GAP-SAMEORIGIN-AUDIT-REMAINING",
    module_or_platform: "shared-platform",
    capability: "Same-origin/CSRF audit coverage beyond the 4 routes checked this checkpoint",
    expected_behavior: "Every cookie-authenticated mutation route across the whole app (not just CRM/notifications/approvals/logout) calls assertSameOriginOrMobile or has an equivalent verified protection.",
    existing_domain_code: "services/api/src/core/security.js (assertSameOrigin, assertSameOriginOrMobile — pre-existing)",
    existing_http_endpoint: "apps/web/src/app/api/workspace/context/route.ts, apps/web/src/app/api/crm/{accounts,contacts}/duplicates/route.ts, apps/web/src/app/api/crm/{accounts,contacts}/merge/preview/route.ts",
    existing_frontend: "n/a",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "Full enumeration performed: every POST/PATCH/PUT/DELETE handler under apps/web/src/app/api/**/route.ts (~110 files) cross-referenced against assertSameOrigin/assertSameOriginOrMobile usage (a plain substring check, not just the OrMobile variant — an earlier pass of this same check incorrectly flagged routes using the stricter assertSameOrigin as missing, since \\b-based regex doesn't split assertSameOrigin from assertSameOriginOrMobile). Found and fixed 4 real gaps (workspace/context switch; accounts/contacts duplicate-search and merge-preview POSTs — all cookie-authenticated). Confirmed apps/web/src/app/api/crm/public/meetings/{bookings/[token],links/[token]/book}/route.ts are correctly unprotected by design (token-authenticated public booking endpoints, no session cookie involved, verified by reading both files in full — no requireWorkspace call, access control is the opaque per-record token). Every auth/* pre-login and token-bearer route (forgot-password, reset-password, verify-email, resend-verification, login, invitations accept) already had assertSameOrigin. No other module (accounting/sales/procurement/stock/etc.) has any API routes yet to audit (see AUDIT-MODULE-STATE-* rows).",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "Full Playwright suite (29/29, one unrelated pre-existing flake reproduced as passing in isolation) and both packages' typecheck pass after the 4 fixes",
    severity: "P3 (defense-in-depth, consistent with SEC-ORIGIN-001 — SameSite=Lax already mitigates the live exploit class for all 4)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "f9280cf1",
  },
  {
    requirement_id: "GAP-MODULE-CATALOG-001",
    module_or_platform: "shared-platform",
    capability: "ERP_MODULE_CATALOG.availability accuracy",
    expected_behavior: "A module's availability field in packages/shared-types/src/modules.js (\"released\" vs \"roadmap\") reflects whether it actually has a web/API surface, since isModuleReleased()/resolveModuleAccess() (module-entitlements.js) uses this field as the first hard gate in the module-access pipeline.",
    existing_domain_code: "packages/shared-types/src/modules.js (ERP_MODULE_CATALOG, RELEASED_MODULE_KEYS)",
    existing_http_endpoint: "n/a (data file, consumed by module-entitlements.js's resolveModuleAccess/assertModuleAccessible)",
    existing_frontend: "Consumed by any screen that calls getAccessibleModules/resolveModuleAccess, e.g. a future module-enablement admin screen",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a — this is a data-accuracy finding, not an authorization bypass",
    missing_parts: "All 12 ERP_MODULE_CATALOG entries (accounting, procurement, sales, crm, stock, manufacturing, projects, assets, point-of-sale, quality, support, hr-payroll) are marked availability:\"released\". Verified directly against apps/web/src/app/(workspace)/<module>/page.tsx and apps/web/src/app/api/: 11 of 12 (all but crm) render only ModuleFoundationPage/PlatformFoundationPage stubs and have zero apps/web/src/app/api/<module> routes. The two module navigation registries (moduleNavigationRegistry.ts, module-navigation-registry.ts) correctly mark these 11 as \"foundation\"/PLANNED — they are NOT overstated. Only the shared-types catalog's availability field is inconsistent with actual build state. Current real-world impact is limited: since no web/API routes exist for the 11 mislabeled modules, nothing is actually reachable to exploit — this is a documentation/data-integrity gap, relevant to any future billing/entitlement/module-enablement admin screen that trusts this field, not a live access-control bypass today.",
    implementation_status: "PARTIAL",
    test_evidence: "",
    real_runtime_evidence: "Verified directly: packages/shared-types/src/modules.js lines 2-13 (read in full); apps/web/src/app/(workspace)/{sales,accounting,procurement,stock,manufacturing,projects,assets,pos,quality,support,hr}/page.tsx confirmed as ModuleFoundationPage stubs; apps/web/src/app/api/ directory listing confirmed no sales/accounting/procurement/stock/manufacturing/projects/assets/pos/quality/support/hr routes exist",
    severity: "P2 (data-integrity/documentation-honesty finding, not a current security bypass)",
    dependency: "Needs a product decision on the correct availability value/taxonomy for in-development modules (the codebase currently defines only \"released\"/\"roadmap\" — a third state may be needed, e.g. \"in_development\") before changing source",
    target_phase: "Checkpoint B or later (Phase 10 documentation reconciliation)",
    verified_commit: "",
  },
  {
    requirement_id: "AUDIT-MODULE-STATE-SALES",
    module_or_platform: "sales",
    capability: "Overall module readiness",
    expected_behavior: "n/a — audit finding, not a single requirement",
    existing_domain_code: "services/api/src/modules/sales/ — 9 files, ~5,425 lines (index.js, order-governance.js, quotation-governance.js, money.js, pass1-operations.js), 12 dedicated passing tests (sales-order-close-f059, sales-quotation-expiry-f038, sales-fulfillment-stock-movement-f047, sales-commission-auto-accrual-f057, sales-crm-opportunity-sync-f042, etc.)",
    existing_http_endpoint: "None (no apps/web/src/app/api/sales)",
    existing_frontend: "None — apps/web/src/app/(workspace)/sales/page.tsx is a ModuleFoundationPage stub; no apps/web/src/features/sales directory exists",
    existing_worker_or_job: "Not audited this pass",
    existing_database_support: "Not audited this pass",
    permission_and_scope: "n/a",
    missing_parts: "Entire web layer (routes, screens, API endpoints) — substantial, well-tested backend exists with no UI wired to it",
    implementation_status: "PARTIAL",
    test_evidence: "services/api/tests/sales-*.test.mjs (12 files)",
    real_runtime_evidence: "",
    severity: "Informational (correctly disclosed as PLANNED in nav registry, not overstated)",
    dependency: "Out of this execution's implementation scope per instruction (\"Do not start implementing Sales... in full\")",
    target_phase: "Future Sales module prompt",
    verified_commit: "",
  },
  {
    requirement_id: "AUDIT-MODULE-STATE-ACCOUNTING",
    module_or_platform: "accounting",
    capability: "Overall module readiness",
    expected_behavior: "n/a — audit finding, not a single requirement",
    existing_domain_code: "services/api/src/modules/accounting/ — 31 files, ~10,778 lines (core.js, journals.js, banking.js, banking-governance.js, close.js, compliance.js, forecast.js, accruals.js, assets.js, advanced.js, etc.) — the largest non-CRM backend module",
    existing_http_endpoint: "None (no apps/web/src/app/api/accounting)",
    existing_frontend: "None — ModuleFoundationPage stub only",
    existing_worker_or_job: "Not audited this pass",
    existing_database_support: "Not audited this pass",
    permission_and_scope: "n/a",
    missing_parts: "Entire web layer",
    implementation_status: "PARTIAL",
    test_evidence: "accounting-service.test.mjs, billing-service.test.mjs (passing)",
    real_runtime_evidence: "",
    severity: "Informational (correctly disclosed as PLANNED in nav registry, not overstated)",
    dependency: "Out of this execution's implementation scope",
    target_phase: "Future Accounting module prompt",
    verified_commit: "",
  },
  {
    requirement_id: "AUDIT-MODULE-STATE-PROCUREMENT",
    module_or_platform: "procurement",
    capability: "Overall module readiness",
    expected_behavior: "n/a — audit finding, not a single requirement",
    existing_domain_code: "services/api/src/modules/procurement/ — 7 files, ~3,455 lines (governance.js, money.js, pass1-operations.js); test coverage only indirect, via the shared pass1-f015-f114-runtime.test.mjs (21 cases), no dedicated procurement-*.test.mjs",
    existing_http_endpoint: "None",
    existing_frontend: "None — ModuleFoundationPage stub only",
    existing_worker_or_job: "Not audited this pass",
    existing_database_support: "Not audited this pass",
    permission_and_scope: "n/a",
    missing_parts: "Entire web layer; thinner backend test coverage than Sales/Accounting despite being listed alongside CRM in NATIVE_OPERATIONAL_MODULE_KEYS (modules.js)",
    implementation_status: "PARTIAL",
    test_evidence: "pass1-f015-f114-runtime.test.mjs (shared, not procurement-dedicated)",
    real_runtime_evidence: "",
    severity: "Informational (correctly disclosed as PLANNED in nav registry, not overstated) — worth noting the NATIVE_OPERATIONAL_MODULE_KEYS listing implies procurement should be further along than it is",
    dependency: "Out of this execution's implementation scope",
    target_phase: "Future Procurement module prompt",
    verified_commit: "",
  },
  {
    requirement_id: "AUDIT-MODULE-STATE-REMAINING-8",
    module_or_platform: "stock, manufacturing, projects, assets, point-of-sale, quality, support, hr-payroll",
    capability: "Overall module readiness",
    expected_behavior: "n/a — audit finding, not a single requirement",
    existing_domain_code: "Each has services/api/src/modules/<key>/ at 3 files / 430-1,004 lines — real, non-placeholder logic (quality/index.js verified directly: a real generic resource router across quality_plans/inspections/holds/nonconformances/capa/audits/events with permission checks and event logging, not a stub that throws), but small scale and mostly no dedicated test file (stock and manufacturing are the exceptions, covered by stock-balance-diagnostic.test.mjs / manufacturing-stock-integrity.test.mjs / point-of-sale-stock-integrity.test.mjs, which are cross-module integrity tests, not per-module unit suites)",
    existing_http_endpoint: "None for any of the 8",
    existing_frontend: "None — ModuleFoundationPage stub only, for all 8",
    existing_worker_or_job: "Not audited this pass",
    existing_database_support: "Not audited this pass",
    permission_and_scope: "n/a",
    missing_parts: "Entire web layer for all 8; dedicated backend test coverage for 6 of the 8 (all but stock, manufacturing)",
    implementation_status: "PARTIAL",
    test_evidence: "stock-balance-diagnostic.test.mjs, manufacturing-stock-integrity.test.mjs, point-of-sale-stock-integrity.test.mjs (indirect/cross-module only)",
    real_runtime_evidence: "",
    severity: "Informational (correctly disclosed as PLANNED in nav registry, not overstated)",
    dependency: "Out of this execution's implementation scope",
    target_phase: "Future per-module prompts",
    verified_commit: "",
  },
  {
    requirement_id: "AUDIT-PLATFORM-SURFACES",
    module_or_platform: "shared-platform",
    capability: "Global Home / Work / Search / Settings readiness",
    expected_behavior: "n/a — audit finding, not a single requirement",
    existing_domain_code: "n/a",
    existing_http_endpoint: "n/a",
    existing_frontend: "Global Home (apps/web/src/app/(workspace)/page.tsx) renders real session data honestly (welcome banner only, explicitly disclosed as not yet showing My Work/Approvals/Notifications/Favorites widgets — no fabricated KPIs). Work (.../work/page.tsx) and Search (.../search/page.tsx) are PlatformFoundationPage stubs, Search's own comment explicitly discloses no backend search adapter exists yet for any module. Settings (.../settings/page.tsx) is a PlatformFoundationPage stub except Settings > Profile (.../settings/profile/page.tsx), which is real and renders actual session data (name/email/org/company/branch/locale/timezone) — no organization/companies/branches/users/roles/security sub-pages exist yet.",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "Global Home's My Work/Approvals/Notifications/Recent Records/Favorites/Background Jobs/Quick Create sections (Phase 5 of the full instruction); Global Work's provider/read-model contract; Global Search's provider architecture and CRM search provider; Settings' organization/companies/branches/users/roles/security/MFA/billing/integrations/API-keys/OAuth/notification-preferences/privacy/feature-config/audit/AI-governance sub-pages",
    implementation_status: "PARTIAL",
    test_evidence: "",
    real_runtime_evidence: "",
    severity: "P1 (explicitly named as in-scope for this execution: \"Essential shared ERP platform\", \"Common application shell\") — substantial, not yet started this checkpoint",
    dependency: "none",
    target_phase: "Checkpoint C / D",
    verified_commit: "",
  },
  {
    requirement_id: "GAP-CRM-RESTRICTED-FIXTURE",
    module_or_platform: "testing-infrastructure",
    capability: "Restricted-role E2E coverage",
    expected_behavior: "A genuinely restricted (non-organization_owner) fixture user's password is set and usable so restricted-role authorization paths can be proven end-to-end via Playwright rather than only at the unit-test level.",
    existing_domain_code: "n/a",
    existing_http_endpoint: "n/a",
    existing_frontend: "apps/web/e2e/restricted-role-authorization.spec.ts (new)",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "e2e-restricted@crm-e2e-fixture.test holds crm_sales_representative_e2e: crm.view + leads/accounts/opportunities/activities/communications.manage, no users.manage/approvals.manage/crm.*.view_sensitive",
    missing_parts: "none — scripts/qa/set-qa-password.mjs (the same sanctioned, localhost-guarded tool previously left for the owner) was run this checkpoint and succeeded, setting the shared QA password. apps/web/.env.e2e.local's stale ERP_E2E_RESTRICTED_PASSWORD was updated to match (gitignored, not committed). Three real denial-path E2E tests now run as this restricted session instead of organization_owner (which bypasses hasSessionPermission entirely).",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "apps/web/e2e/restricted-role-authorization.spec.ts: cannot invite anyone into any role (403), an unmapped CRM resource mutation is still denied (403), cannot revoke another user's session by id (404, owner's session provably untouched). Full Playwright suite (34/34) unaffected.",
    severity: "P1 (was blocking real E2E proof of every authorization fix's denial path) — resolved",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "e7f9fc7c",
  },
  {
    requirement_id: "AUTH-LOCKOUT-001",
    module_or_platform: "authentication",
    capability: "verify-email / onboarding redirect targets exist",
    expected_behavior: "requireVerifiedUser()/requireWorkspace() (used by every authenticated route) redirect an unverified or org-less session to a real page, not Next's default 404.",
    existing_domain_code: "apps/web/src/core/session.ts (requireVerifiedUser, requireWorkspace, nextPath) — unchanged, already correct; the pages it redirected to simply didn't exist",
    existing_http_endpoint: "n/a (page-level bug, not an API route)",
    existing_frontend: "apps/web/src/app/(auth)/verify-email/page.tsx, apps/web/src/app/(auth)/onboarding/page.tsx — both new this checkpoint",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "Verified directly before the fix: grep across apps/web/src/app found zero matches for /verify-email or /onboarding as a page. Any unverified or org-less user hit a hard 404 with no recovery path, for every route in the entire app.",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "n/a (this specific finding is proven by the E2E test below, not a unit test)",
    real_runtime_evidence: "apps/web/e2e/auth-lifecycle.spec.ts's password-reset test logs in as a real org-less user and asserts the real /onboarding page renders (page.waitForURL + visible text), not a 404",
    severity: "P0 (complete, unrecoverable lockout for the exact session state the app's own auth code already produces)",
    dependency: "none",
    target_phase: "Checkpoint B",
    verified_commit: "4dae25d9",
  },
  {
    requirement_id: "SP004-VERIFY-EMAIL",
    module_or_platform: "SP004/SP005",
    capability: "Email verification",
    expected_behavior: "A user can verify their email via a mailed link, and resend it if needed, per SP004's Invite -> verify -> join flow.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (createEmailVerificationToken, consumeEmailVerificationToken)",
    existing_http_endpoint: "apps/web/src/app/api/auth/{verify-email,resend-verification}/route.ts",
    existing_frontend: "apps/web/src/app/(auth)/verify-email/",
    existing_worker_or_job: "n/a (synchronous mailer call, no queue)",
    existing_database_support: "email_verification_tokens (pre-existing, migration 001, previously unused)",
    permission_and_scope: "Token-authenticated (proof of mailbox control), not permission-gated; resend requires an authenticated (any-status) session",
    missing_parts: "none for the core flow; email delivery itself is a no-op in this environment until real SMTP/webhook config is supplied (deliverAuthMessage fails closed, doesn't throw, doesn't pretend to send — by design)",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs (real Postgres: create/consume/reuse-rejected/expired-rejected)",
    real_runtime_evidence: "apps/web/e2e/auth-lifecycle.spec.ts's onboarding assertion depends on this indirectly (the test user's email is verified via reset-password, an equivalent proof-of-mailbox path); consumeEmailVerificationToken's own real-DB round trip is proven in the integration test",
    severity: "P0 (was completely missing, backend and frontend)",
    dependency: "Real SMTP/webhook credentials for actual email delivery in a live environment — not needed for correctness of the flow itself",
    target_phase: "Checkpoint B",
    verified_commit: "4dae25d9",
  },
  {
    requirement_id: "SP005-PASSWORD-RESET",
    module_or_platform: "SP004/SP005",
    capability: "Forgot password / reset password",
    expected_behavior: "A user can request a reset link (without disclosing whether the email is registered) and set a new password via it; every pre-existing session for that user is revoked.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (requestPasswordReset, resetPasswordWithToken)",
    existing_http_endpoint: "apps/web/src/app/api/auth/{forgot-password,reset-password}/route.ts",
    existing_frontend: "apps/web/src/app/(auth)/{forgot-password,reset-password}/",
    existing_worker_or_job: "n/a",
    existing_database_support: "password_reset_tokens (pre-existing, migration 001, previously unused)",
    permission_and_scope: "Token-authenticated; account-enumeration safe (identical response for a registered or unregistered email)",
    missing_parts: "none",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs (real Postgres: request is enumeration-safe, reset changes the real password, session revocation, reuse-rejected)",
    real_runtime_evidence: "apps/web/e2e/auth-lifecycle.spec.ts, real browser: establishes a password via reset, logs in for real, performs a second real reset, confirms the pre-existing session is genuinely rejected (401) afterward, confirms the new password logs in from a fresh context and the old one returns 401",
    severity: "P0 (was completely missing, backend and frontend)",
    dependency: "none",
    target_phase: "Checkpoint B",
    verified_commit: "4dae25d9",
  },
  {
    requirement_id: "SP004-INVITATIONS",
    module_or_platform: "SP004",
    capability: "Organization invitations (issue + accept)",
    expected_behavior: "An org.owner/admin can invite someone by email + role; the invitee accepts via a mailed link, which creates their account (if new) or reuses it (if not), grants real organization_memberships + user_role_assignments, and signs them in.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (createOrganizationInvitation, getInvitationByToken, acceptOrganizationInvitation, listPendingInvitationsForEmail)",
    existing_http_endpoint: "apps/web/src/app/api/auth/invitations/{route.ts,[token]/route.ts,[token]/accept/route.ts}",
    existing_frontend: "apps/web/src/app/(auth)/invitations/[token]/, apps/web/src/app/(auth)/onboarding/ (shows pending invitations)",
    existing_worker_or_job: "n/a",
    existing_database_support: "organization_invitations + the company/branch/department-scoped grant tables (pre-existing, migrations 001/007, previously unused for the scoped grants — this pass only wires the base organization-level invitation, not the per-company/branch/department access grants those tables also support)",
    permission_and_scope: "Issuing requires users.manage AND now (Checkpoint D) the same SP008 grant-ceiling/SoD check role assignment uses (validateRoleSelection) — an inviter can no longer grant a role carrying permissions beyond their own, and can never grant organization_owner via invitation. Accepting a NEW account is token-authenticated (mailbox control); accepting for an EXISTING account now additionally requires the caller to already be authenticated as that exact account (server-resolved from the session cookie) — a bare invitation link is no longer sufficient by itself, and password_hash is never written for an existing account under any input.",
    missing_parts: "Company/branch/department-scoped access grants on an invitation (organization_invitation_company_access etc.) are not yet surfaced by createOrganizationInvitation's input or the issuing UI — a new member gets organization membership + their role's permissions, but no default company/branch access, which must be granted separately once a Settings > Users screen exists (see SP-SETTINGS-001). No Settings UI exists yet to issue invitations from — only the API.",
    implementation_status: "PARTIAL",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs (real Postgres: create, duplicate-member rejected, get-by-token, accept creates membership + role assignment, double-accept rejected, grant-ceiling denial/allow, existing-account sign-in-required denial/allow, password_hash never changed)",
    real_runtime_evidence: "apps/web/e2e/auth-lifecycle.spec.ts (2 specs: original manual-token flow, plus a new one that captures the REAL mailer url via a local test-support capture adapter and follows it verbatim — see AUTH-INVITATION-URL-001); apps/web/e2e/restricted-role-authorization.spec.ts proves a non-users.manage caller is denied at 403",
    severity: "P0 for the core flow (was completely missing); P2 for the deferred scoped-grants gap",
    dependency: "A Settings > Users/Invitations screen to issue invitations without direct API access — see SP-SETTINGS gap below",
    target_phase: "Checkpoint B / D",
    verified_commit: "4dae25d9, 50799ad3",
  },
  {
    requirement_id: "AUTH-INVITATION-URL-001",
    module_or_platform: "SP004",
    capability: "Correct invitation email link (path segment, not query string)",
    expected_behavior: "The URL createOrganizationInvitation hands to the mailer matches the real frontend route (/invitations/[token], a path segment), for every APP_URL including a production-style HTTPS origin.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (new pathTokenUrl(), used only for invitations; tokenUrl()'s query-string form is unchanged and still correct for verify-email/reset-password, both real Next.js searchParams pages)",
    existing_http_endpoint: "n/a (mailer input, not a route)",
    existing_frontend: "apps/web/src/app/(auth)/invitations/[token]/page.tsx (unchanged — this was always a path-segment route; the bug was the URL generator producing /invitations?token=... instead)",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "none — found via independent GitHub inspection, not this engagement's own earlier audits, which had verified acceptance worked by manually navigating to /invitations/${token} directly rather than following the mailer's actual generated URL",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs: captures the exact url via a local http server standing in for the webhook, under APP_URL=https://app.vercentlabs.example, asserts the path-segment shape and that the captured token hashes to the real stored token_hash (not a reconstructed guess)",
    real_runtime_evidence: "apps/web/e2e/auth-lifecycle.spec.ts's new spec: issues a real invitation, retrieves the REAL mailer url via a local test-support capture adapter (apps/web/src/app/api/test-support/email-capture, hard-blocked in production), navigates to that literal url (no reconstruction), completes acceptance",
    severity: "P0 (a real invitee's link was dead; only ever masked by every prior test manually reconstructing the correct URL instead of following the generated one)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "50799ad3",
  },
  {
    requirement_id: "AUTH-DELIVERY-STATE-001",
    module_or_platform: "SP004/SP005",
    capability: "Truthful email-delivery-state semantics",
    expected_behavior: "The UI never claims a verification/invitation email is \"on its way\" when deliverAuthMessage actually returned false; a public/anonymous endpoint (forgot-password) still cannot leak per-email delivery outcomes (account-enumeration safety).",
    existing_domain_code: "services/api/src/core/auth-mailer.js (new isAuthMailerConfigured() — a deployment-wide fact, identical for every caller regardless of target email)",
    existing_http_endpoint: "apps/web/src/app/api/auth/{resend-verification,invitations,forgot-password}/route.ts",
    existing_frontend: "apps/web/src/app/(auth)/verify-email/verify-email-client.tsx (uses payload.delivered)",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "resend-verification and invitation issuance are both authenticated (self, or users.manage) — safe to return the real per-call delivered boolean. forgot-password is anonymous — exposes only isAuthMailerConfigured()'s deployment-wide fact, never a per-request delivery outcome, so registered vs unregistered email still cannot be distinguished.",
    missing_parts: "none for the three routes audited. deliverAuthMessage throwing (not returning false) on a partially-configured SMTP env, discovered while auditing this, was found to be existing, intentional, already-tested behavior (services/api/tests/platform-*.test.mjs: \"deliverAuthMessage throws when SMTP configuration is present but incomplete, rather than silently downgrading to no-op\") — not a gap.",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "services/api/tests/platform-notifications-integrations.test.mjs (pre-existing, re-confirmed passing): deliverAuthMessage's false/throw contract",
    real_runtime_evidence: "Full backend suite (1090/1090) and Playwright suite unaffected by the route-level changes",
    severity: "P2 (silent-success UI trust issue, not an authorization bypass)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "50799ad3",
  },
  {
    requirement_id: "AUTH-TOKEN-ATOMICITY-001",
    module_or_platform: "SP004/SP005",
    capability: "Atomic, race-safe single-use token consumption",
    expected_behavior: "Two concurrent requests presenting the same still-valid email-verification or password-reset token can never both succeed, and password reset atomically coordinates token consumption + password update + session revocation.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (consumeEmailVerificationToken, resetPasswordWithToken — both now a single atomic UPDATE...WHERE used_at IS NULL...RETURNING claim, not a SELECT followed by a separate UPDATE)",
    existing_http_endpoint: "apps/web/src/app/api/auth/{verify-email,reset-password}/route.ts (now call transaction(), not withClient(), so the token claim and the follow-on user/session updates commit or roll back together)",
    existing_frontend: "n/a",
    existing_worker_or_job: "n/a",
    existing_database_support: "email_verification_tokens, password_reset_tokens (pre-existing tables, schema unchanged — this is a query-pattern fix, not a schema fix)",
    permission_and_scope: "n/a",
    missing_parts: "none — acceptOrganizationInvitation already used SELECT...FOR UPDATE correctly and needed no change; this fix brings the other two token flows to the same standard",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs: two new concurrent-replay tests, each racing the SAME still-valid token from two genuinely separate live Postgres connections via Promise.allSettled — exactly one request succeeds, the other gets AUTH_TOKEN_USED, and for password reset the final stored hash matches exactly one of the two racing attempts, never neither and never a corrupted mix",
    real_runtime_evidence: "Full backend suite (1090/1090) and Playwright suite unaffected",
    severity: "P1 (a real, previously unverified TOCTOU window on both token flows — the existing sequential replay test only proved single-request reuse was rejected, not that concurrent requests couldn't both win)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "50799ad3",
  },
  {
    requirement_id: "AUTH-INVITATION-EXISTING-ACCOUNT-001",
    module_or_platform: "SP004/SP005",
    capability: "Existing-account invitation acceptance requires real authentication, not just mailbox access",
    expected_behavior: "An organization invitation must not behave like an unrestricted password-reset mechanism, and must not grant a fully authenticated session for an existing account on token possession alone.",
    existing_domain_code: "services/api/src/core/auth-lifecycle.js (acceptOrganizationInvitation now takes authenticatedUserId, server-resolved from the caller's session cookie in the route — never client-supplied; an existing account's password_hash is never written under any input, and a session is only reused/authorized when authenticatedUserId already matches that account)",
    existing_http_endpoint: "apps/web/src/app/api/auth/invitations/[token]/accept/route.ts (resolves getSessionContext() — not requireUser(), since an anonymous new-account acceptance is still valid — and passes it through)",
    existing_frontend: "apps/web/src/app/(auth)/invitations/[token]/{page.tsx,accept-invitation-form.tsx} (three real states: new account form; existing account already signed in as itself -> one-click accept; existing account not signed in -> \"sign in first\" with no form that could imply a password change)",
    existing_worker_or_job: "n/a",
    existing_database_support: "no schema change",
    permission_and_scope: "Previously: `if (user && password)` unconditionally overwrote an existing account's password_hash whenever the request body included one, and the route minted a real session for any existing account regardless of whether the caller had ever proven they controlled it. Now: an existing account is only ever joined to the new organization (reusing its own live session, not minting a second one) when the caller's already-established session already belongs to that exact account.",
    missing_parts: "none",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs: anonymous visitor rejected (AUTH_INVITATION_REQUIRES_SIGN_IN), authenticated-as-someone-else rejected, a password supplied in the body with no matching session is NOT treated as proof of ownership, password_hash provably unchanged across all rejected attempts and the eventual successful one",
    real_runtime_evidence: "Full Playwright suite (34/34) unaffected — the existing brand-new-account invitation E2E flow (which this fix does not change) still passes",
    severity: "P0 (a real authentication bypass: possessing any valid invitation link addressed to an existing account's email — obtainable by any org admin regardless of whether they control that mailbox — previously granted a live session for that account with zero password verification)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "50799ad3",
  },
  {
    requirement_id: "SP008-GRANT-CEILING-INVITATION-001",
    module_or_platform: "SP008",
    capability: "Grant-ceiling/SoD enforcement on invitation issuance",
    expected_behavior: "An administrator cannot invite someone into a role carrying permissions beyond their own grant ceiling, or into organization_owner via a bare invitation — the same rule role assignment already enforces.",
    existing_domain_code: "services/api/src/core/access-administration.js (validateRoleSelection — pre-existing, already used for role assignment) — now also called from createOrganizationInvitation with roleIds:[roleId], primaryRoleId:roleId, allowOwnerRole:false",
    existing_http_endpoint: "apps/web/src/app/api/auth/invitations/route.ts (passes inviter:{roleSlugs,permissions} from the caller's own session)",
    existing_frontend: "n/a",
    existing_worker_or_job: "n/a",
    existing_database_support: "no schema change — reuses roles/role_permissions",
    permission_and_scope: "organization_owner is exempt from the ceiling check (permissionsOutsideGrantCeiling's existing rule); the OWNER ROLE ITSELF can never be granted via invitation regardless of the inviter (allowOwnerRole:false, matching role assignment's own \"ownership transfer needs its own controlled flow\" rule)",
    missing_parts: "none. Found while implementing AUTH-INVITATION-EXISTING-ACCOUNT-001 above — createOrganizationInvitation had never called this pre-existing, already-tested control at all.",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "tests/integration/auth-lifecycle.test.mjs: an inviter without a privileged role's permission is rejected (403, AccessAdministrationError); the same invitation succeeds once the inviter holds the permission (or is organization_owner)",
    real_runtime_evidence: "apps/web/e2e/restricted-role-authorization.spec.ts proves a non-users.manage caller is denied before the ceiling check even runs; apps/web/e2e/auth-lifecycle.spec.ts's existing invitation tests updated to select a non-owner role (the fixture org's earliest-created role happened to be organization_owner) and still pass",
    severity: "P1 (a real, if narrower, privilege-escalation path: any users.manage holder could grant permissions they didn't themselves have, via invitation instead of direct role assignment)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "50799ad3",
  },
  {
    requirement_id: "SP005-ACCOUNT-LOCKOUT-001",
    module_or_platform: "SP005",
    capability: "Per-account failed-login lockout/backoff",
    expected_behavior: "Repeated failed login attempts against a specific account trigger an escalating lockout or backoff, independent of IP-based rate limiting (which only throttles a source address, not a targeted account behind a botnet/proxy pool).",
    existing_domain_code: "users.failed_login_attempts integer column exists (database/platform/migrations/002_platform_foundation.sql) but is never read or written anywhere in services/api/src — confirmed via grep, zero references outside the migration itself",
    existing_http_endpoint: "apps/web/src/app/api/auth/login/route.ts only calls enforceRateLimit keyed by IP (10/300s) — no per-account check",
    existing_frontend: "none",
    existing_worker_or_job: "n/a",
    existing_database_support: "users.failed_login_attempts column exists and is unused — schema is ready, logic is not",
    permission_and_scope: "n/a — nothing built",
    missing_parts: "Incrementing failed_login_attempts on a failed login, resetting it on success, and an actual lockout/backoff policy once a threshold is reached (with a safe, enumeration-conscious error message and a recovery path, e.g. via the existing password-reset flow) — all still to be designed and built",
    implementation_status: "MISSING",
    test_evidence: "",
    real_runtime_evidence: "",
    severity: "P2 (IP-based rate limiting already mitigates the single-source brute-force case; this closes the distributed/targeted-account gap, not a currently-exploited hole)",
    dependency: "none blocking",
    target_phase: "Future checkpoint",
    verified_commit: "",
  },
  {
    requirement_id: "SP004-DEACTIVATION-001",
    module_or_platform: "SP004",
    capability: "User deactivation/suspension preserving audit attribution",
    expected_behavior: "An administrator can deactivate/suspend an organization member, immediately terminating their future access, without deleting or breaking historical audit-log attribution to that user.",
    existing_domain_code: "none found — users.status and organization_memberships.status exist as columns but no domain function sets a user to a deactivated/suspended state through any reviewed code path",
    existing_http_endpoint: "none",
    existing_frontend: "none",
    existing_worker_or_job: "n/a",
    existing_database_support: "users.status, organization_memberships.status columns exist; audit_events table has its own immutability trigger (prevent_audit_event_mutation) that already protects historical attribution independent of user status",
    permission_and_scope: "n/a — nothing built",
    missing_parts: "A deactivateUser/suspendMembership domain function, its HTTP route (users.manage-gated), session invalidation on deactivation (SP006 credential/access-change rule), and the Settings > Users UI action to trigger it (see SP-SETTINGS-001)",
    implementation_status: "MISSING",
    test_evidence: "",
    real_runtime_evidence: "",
    severity: "P1 (named in SP004's own business rules; no way to actually offboard a member today beyond direct DB access)",
    dependency: "Settings > Users screen (SP-SETTINGS-001) is the natural place to surface this, though the domain function/route could be built independently first",
    target_phase: "Future checkpoint",
    verified_commit: "",
  },
  {
    requirement_id: "SP007-MFA",
    module_or_platform: "SP007",
    capability: "MFA enrollment, challenge, recovery codes, step-up authentication",
    expected_behavior: "Per SP007: a user can enroll a TOTP factor, is challenged for it at login, can use recovery codes if they lose the factor, and step-up (re-auth) is required before a high-impact action.",
    existing_domain_code: "none",
    existing_http_endpoint: "none",
    existing_frontend: "none",
    existing_worker_or_job: "none",
    existing_database_support: "users.mfa_required/mfa_enrolled_at (migration 002) exist as flags/timestamps only — verified directly, no secret-storage table, no TOTP/recovery-code schema anywhere in database/platform/migrations/*.sql",
    permission_and_scope: "n/a — nothing built",
    missing_parts: "Everything: a new migration for factor secret storage (encrypted at rest) and recovery codes, TOTP enrollment (QR code generation) and verification, a login-time challenge step, recovery-code consumption, step-up re-auth middleware for high-impact actions, and all the frontend for each. This is a materially larger, more security-sensitive lift than verify-email/reset-password/invitations were (those had a designed, unused schema and a built, unused mailer already waiting — MFA has neither).",
    implementation_status: "MISSING",
    test_evidence: "",
    real_runtime_evidence: "",
    severity: "P1 (explicitly named as a required journey; genuinely not started, not attempted this checkpoint rather than rushed)",
    dependency: "A deliberate design/scope decision on TOTP vs. WebAuthn vs. both before implementation, and a new migration reviewed for encryption-at-rest of any stored secret",
    target_phase: "Future checkpoint (not attempted here — see report)",
    verified_commit: "",
  },
  {
    requirement_id: "SESSION-DEVICE-UI",
    module_or_platform: "SP006",
    capability: "Session/device listing and remote revocation",
    expected_behavior: "A user can see their active sessions (device, IP, last seen) in Security settings and revoke any of them remotely.",
    existing_domain_code: "services/api/src/core/session.js: listSessionsForUser/revokeSessionById plus new revokeOtherSessions, all scoped to the caller's own userId (ownership of the row is the authorization, no separate permission check) — added alongside the pre-existing createSession/revokeSessionByTokenHash. Idle/absolute timeout enforcement (resolveSessionContext's revoked_at/expires_at/idle_expires_at checks, sliding-window refresh) was audited and confirmed already correct — no gap found there.",
    existing_http_endpoint: "apps/web/src/app/api/settings/sessions/route.ts (GET list, DELETE = revoke all other sessions), apps/web/src/app/api/settings/sessions/[id]/route.ts (DELETE one, assertSameOriginOrMobile-protected)",
    existing_frontend: "apps/web/src/features/settings/security/screens/SecuritySettingsScreen.tsx (per-session revoke + \"Sign out all other sessions\" bulk action, both behind an AlertDialog confirmation), apps/web/src/app/(workspace)/settings/security/page.tsx — now linked from the real Settings index (see SP-SETTINGS-001)",
    existing_worker_or_job: "n/a",
    existing_database_support: "sessions table (migration 001 + 007's device_name/last_seen_at additions) — unchanged, no new migration needed",
    permission_and_scope: "Self-service — a user lists/revokes only their own sessions; the current session is always excluded from both the specific-revoke and revoke-others actions (revoking your own live session via this route would leave a stale cookie instead of a clean sign-out, so the screen points to the existing Sign Out action for that case instead)",
    missing_parts: "none for the core flow",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "Full Playwright suite (34/34): a dedicated spec covers both single-session revoke and revoke-all-others, confirming the caller's own session always survives and a genuinely different fresh session is actually invalidated (401 on its next request), not just a DB row removed. apps/web/e2e/restricted-role-authorization.spec.ts additionally proves cross-user revocation is refused (404, ownership-scoped, not permission-scoped).",
    severity: "P2 (data already captured; this closed a UI/API gap, not a data-model gap)",
    dependency: "none",
    target_phase: "Checkpoint D",
    verified_commit: "f9280cf1, 4851ddfa",
  },
  {
    requirement_id: "SEC-NAV-001",
    module_or_platform: "shared-platform",
    capability: "Permission-aware secondary sidebar navigation",
    expected_behavior: "A nav item only renders as a clickable Link when the feature is both built (AVAILABLE) and the caller holds its requiredPermission; otherwise it renders the existing disabled/locked state.",
    existing_domain_code: "n/a (client-side rendering logic)",
    existing_http_endpoint: "n/a",
    existing_frontend: "apps/web/src/shell/module-sidebar/SecondarySidebar.tsx",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "none for this fix",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "n/a (pure rendering-logic fix; the disabled-state branch it now correctly reaches was already covered by existing accessibility/regression Playwright specs rendering CRM Home/Lead List/etc.)",
    real_runtime_evidence: "Full Playwright suite (28/28) still passes after the fix; no visual regression on any already-covered nav-rendering screen",
    severity: "P2 (nav-hiding bug, not itself an authorization bypass — the underlying page/route must and does enforce its own permission independently, per \"never use UI hiding as the only access control\")",
    dependency: "none",
    target_phase: "Checkpoint C",
    verified_commit: "35164a26",
  },
  {
    requirement_id: "SEC-NAV-002",
    module_or_platform: "shared-platform",
    capability: "Mobile Sign Out and Profile access",
    expected_behavior: "Sign Out and Profile are reachable from the mobile drawer, not only the desktop-only (hidden lg:flex) ProfileMenu.",
    existing_domain_code: "n/a",
    existing_http_endpoint: "/api/auth/logout (pre-existing, reused as-is)",
    existing_frontend: "apps/web/src/shell/primary-sidebar/MobileNav.tsx, apps/web/src/shell/module-foundation/PlatformFoundationPage.tsx (new quickLinks prop), apps/web/src/app/(workspace)/settings/page.tsx",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "none for Sign Out/Profile specifically; the broader \"complete CRM destination hierarchy on mobile\" (per-module sub-navigation, not just module roots) named in the same instruction paragraph is still missing — MobileNav renders only top-level module entries, confirmed by reading it directly",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "Manually verified with a disposable, freshly-logged-in browser context (not the shared E2E fixture, to avoid revoking it): clicking Sign Out in the mobile drawer redirects to /login AND a subsequent API call genuinely returns 401, not just that the button renders",
    severity: "P1 for Sign Out specifically (was completely unreachable below 1024px); P2 for the still-missing full per-module mobile hierarchy",
    dependency: "none for what's fixed; the full mobile CRM hierarchy is a larger, separate UI build",
    target_phase: "Checkpoint C",
    verified_commit: "35164a26",
  },
  {
    requirement_id: "SEC-QUERY-SCOPE-001",
    module_or_platform: "shared-platform",
    capability: "Scope-aware React Query keys",
    expected_behavior: "Every query key starts with [organizationId, companyId] (scopedQueryKey convention) so a context switch's cache invalidation can never miss a scoped query and two organizations' responses can never collide under the same key.",
    existing_domain_code: "apps/web/src/shell/workspace-context/queryKeys.ts (scopedQueryKey — pre-existing, already used correctly elsewhere, e.g. CRM saved-views)",
    existing_http_endpoint: "n/a",
    existing_frontend: "apps/web/src/app/(workspace)/{notifications,approvals,jobs}/*-client.tsx; apps/web/src/features/crm/{accounts,contacts}/components/*DuplicatesPanel.tsx; apps/web/src/features/crm/settings/lead-assignment-policies/screens/AssignmentPoliciesSettingsScreen.tsx",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "none — the 3 keys left non-conforming at Checkpoint C (AccountDuplicatesPanel/ContactDuplicatesPanel's merge-preview dialogs, AssignmentPoliciesSettingsScreen's explain dialog) now all use scopedQueryKey. ContextSwitcher.tsx's workspace-companies key remains intentionally as-is (investigated at Checkpoint C and found already-correct via its own explicit removeQueries call).",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "Full Playwright suite (29/29) and both packages' typecheck pass after the remaining 3 fixes",
    severity: "P3 (defense-in-depth — none of the 5 fixed keys had a realistic scope-leak, all were already UUID-scoped)",
    dependency: "none",
    target_phase: "Checkpoint C / D",
    verified_commit: "447890bb, f9280cf1",
  },
  {
    requirement_id: "BUG-NOTIF-MUTATION-001",
    module_or_platform: "shared-platform",
    capability: "Notification mark-read/mark-all-read failure handling",
    expected_behavior: "A failed mark-read/mark-all-read HTTP response is treated as a failure (visible error, no optimistic-looking success), not silently as if it succeeded.",
    existing_domain_code: "n/a",
    existing_http_endpoint: "/api/notifications, /api/notifications/[id]/read (unchanged — the bug was entirely client-side)",
    existing_frontend: "apps/web/src/app/(workspace)/notifications/notifications-client.tsx",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "none",
    implementation_status: "VERIFIED_END_TO_END",
    test_evidence: "",
    real_runtime_evidence: "apps/web/e2e/platform-security.spec.ts: mocks the real page's list response and a failing PATCH .../read response, clicks the real \"Mark read\" button, and asserts the error text renders and the button does not disappear (i.e. the UI did not treat the failure as success) — this test was run against the pre-fix code during development and genuinely failed there, confirming it exercises the real bug, not just the fixed behavior",
    severity: "P2 (silent data-integrity/trust issue — a user could believe a failed action succeeded)",
    dependency: "none",
    target_phase: "Checkpoint C",
    verified_commit: "447890bb",
  },
  {
    requirement_id: "SP-SETTINGS-001",
    module_or_platform: "SP001-SP036 (Global Settings)",
    capability: "Global Settings screens (organization, companies, branches, users, roles, security, billing, integrations, API keys, privacy, feature config, audit, AI governance)",
    expected_behavior: "Real, governed screens for each area, per Phase 4 of the full instruction — not a single foundation stub.",
    existing_domain_code: "Per the foundation page's own description (unverified by direct code audit this checkpoint, disclosed rather than assumed): roles/RBAC, API keys, OAuth, privacy, feature flags, and AI governance APIs are claimed already ported and live in services/api",
    existing_http_endpoint: "apps/web/src/app/api/workspace/{companies,context}, apps/web/src/app/api/privacy/* exist; most named areas (users, roles, billing, integrations, API keys, feature config, audit) have no apps/web/src/app/api surface confirmed this checkpoint",
    existing_frontend: "apps/web/src/app/(workspace)/settings/page.tsx is now a real, permission-aware index (apps/web/src/features/settings/screens/SettingsIndexScreen.tsx + settings-navigation-registry.ts) — Profile and Security render as real clickable sections, every other named area (Organization, Companies, Branches, Users, Invitations, Roles, Billing, Integrations, API Keys, OAuth Connections, Notification Preferences, Privacy, Feature Configuration, Audit, AI Governance) renders honestly locked with a one-line description, never a dead link. Still only 2 of ~17 sections are real screens.",
    existing_worker_or_job: "not audited this checkpoint",
    existing_database_support: "roles/user_role_assignments/organization_invitations and their scoped-access tables exist and are used by this checkpoint's auth work; billing/API-key/feature-flag schema not audited this checkpoint",
    permission_and_scope: "not audited this checkpoint (SettingsIndexScreen checks requiredPermission per item for the future, e.g. Users/Invitations already declare users.manage, but neither screen exists yet to actually enforce it)",
    missing_parts: "Essentially the entire surface named in Phase 4 of the instruction: dedicated screens for Organization, Companies, Branches, Users, Invitations (the API exists as of Checkpoint B/D — SP004-INVITATIONS — but no screen calls it), Roles and Permissions, Billing and Entitlements, Integrations, API Keys, OAuth Connections, Notification Preferences, Privacy and Retention, Feature Configuration, Audit, AI Governance. MFA (SP007-MFA) and Sessions (SESSION-DEVICE-UI, now VERIFIED_END_TO_END) are the two sub-areas with dedicated register rows.",
    implementation_status: "PARTIAL",
    test_evidence: "",
    real_runtime_evidence: "Verified in a real browser: the index renders all ~17 sections correctly classified, Profile/Security are real clickable Links, everything else is visibly locked. Full Playwright suite (34/34) and typecheck unaffected.",
    severity: "P1 (explicitly in this execution's stated scope — \"Essential shared ERP platform\" — the index now exists but almost none of the actual sections do)",
    dependency: "none blocking — this is a large, independent build, not cross-module-blocked",
    target_phase: "Checkpoint D (index); future checkpoint (the ~15 remaining sections)",
    verified_commit: "2bf2f104",
  },
  {
    requirement_id: "SP-SHELL-001",
    module_or_platform: "shared-platform",
    capability: "Global Home (My Work/Approvals/Notifications/Recent/Favorites/Jobs/Quick Create), Global Work, Global Search, Command menu (Cmd/Ctrl+K), Quick Create action registry, Recent records/Favorites persistence",
    expected_behavior: "Per Phase 5 of the full instruction — real, scoped-data surfaces, not foundation stubs, and a working command palette.",
    existing_domain_code: "none for any of these six surfaces beyond what already exists per-module (CRM's own dashboard, notifications/approvals/jobs list pages)",
    existing_http_endpoint: "none",
    existing_frontend: "apps/web/src/app/(workspace)/page.tsx (Global Home) is an honest welcome-banner-only stub (explicitly disclosed in its own comment, verified by reading it: \"never fabricated KPIs\"); .../work/ and .../search/ are PlatformFoundationPage stubs (Search's own comment explicitly discloses no backend search adapter exists yet for any module); no command menu or quick-create registry exists anywhere in apps/web/src",
    existing_worker_or_job: "n/a",
    existing_database_support: "n/a",
    permission_and_scope: "n/a",
    missing_parts: "All six surfaces, essentially from zero: a cross-module work-item read-model contract (CRM would be the first real contributor), a governed CRM search provider (Leads/Accounts/Contacts/Opportunities, record- and field-level authorization), the actual Home page composition, the command palette (Ctrl/Cmd+K) itself, a quick-create action registry, and Recent-records/Favorites persistence scoped to user+organization with permission re-checks on read.",
    implementation_status: "MISSING",
    test_evidence: "",
    real_runtime_evidence: "",
    severity: "P1 (explicitly in this execution's stated scope — \"Common application shell and reusable functionality\" — and genuinely not started)",
    dependency: "none blocking for Home/Work/command-menu/quick-create; Search's authorization design should be finalized once (record + field level) rather than per-provider",
    target_phase: "Future checkpoint (not attempted here — see report)",
    verified_commit: "",
  },
];

function csvField(value) {
  const s = String(value ?? "");
  if (/[",\n\r]/.test(s)) return `"${s.replace(/"/g, '""')}"`;
  return s;
}

const lines = [HEADER.join(",")];
for (const row of rows) {
  lines.push(HEADER.map((key) => csvField(row[key])).join(","));
}

fs.writeFileSync("docs/frontend-rebuild/ERP_COMPLETION_GAP_REGISTER.csv", lines.join("\n") + "\n");
console.log(`Wrote ${rows.length} rows.`);
