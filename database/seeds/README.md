# Database seeds

Reserved for reference/lookup data seeded into the platform or tenant schemas
(for example: currency codes, timezone lists, default role definitions) once
the corresponding shared-platform capability (SP008, SP027, ...) is
implemented with acceptance evidence.

No seed scripts exist yet. Do not add business-module sample or demo data
here; root governance rule 14 prohibits mock data in production paths.
