# Journey — Email to resolved case

Inbound message → mailbox/tenant resolution → stable message-id/thread-reference dedupe → create/append canonical ticket → category/priority/routing → assignment → SLA clocks → public agent response stamps first response once → customer replies continue same thread → resolution code/summary → configured CSAT → closure. Repeated delivery or worker retry never duplicates ticket/comment/notification.
