# Journey — Web/manual intake to governed assignment

Authenticated portal or authorized agent creates ticket → validate customer/contact/context → category/priority/default SLA → queue routing → eligible agent selection by policy/skill/capacity/presence → assignment history → manual supervisor override with reason. Concurrent routing/reassignment uses version/lock semantics and one final authoritative owner.
