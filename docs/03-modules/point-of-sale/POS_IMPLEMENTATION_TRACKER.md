# POS Implementation Tracker (F268-F307)

## Session 1 starting state

- Branch: `rebuild/clean-frontend`
- Starting SHA: `524dc6a8c9ecf0ea69a585ab3d4978fe9bf9f73e`
- Working tree: clean, 10 commits ahead of `origin/rebuild/clean-frontend` (unrelated prior CRM/shell work), no divergence.
- Canonical register counts confirmed by direct CSV parsing (not inferred):
  - `FEATURE_REGISTER.csv`: exactly 40 unique `feature_id` values F268-F307.
  - `CAPABILITY_REGISTER.csv`: exactly 9 rows, `POS-CAP-001`..`POS-CAP-009`, `feature_ids` columns match the F-ranges in this program's brief exactly. All 9 rows carry `working_status: SPECIFICATION_READY` (i.e. spec exists, not "implemented" — consistent with, not proof of, the code audit below).
  - `SUBREQUIREMENT_REGISTER.csv`: 1,480 rows across the 40 features (37/feature).
  - `FEATURE_SEMANTIC_SUBCAPABILITY_REGISTER.csv`: 320 rows (8/feature).
  - `FEATURE_FLOW_REGISTER.csv`: 400 rows (10/feature).
  - `FEATURE_STATE_TRANSITION_REGISTER.csv`: 200 rows (5/feature).
  - `docs/PRODUCTION_TRACKER.md:252` and `docs/ERP_COMPLETION_EXECUTION_TRACKER.md:664` both independently record POS as "0/40 ... Scaffolding only / thin, not re-verified" prior to this session. Consistent with the code audit below, not identical wording — treated as corroboration, not proof.
  - No `POS_IMPLEMENTATION_TRACKER.md` existed before this file. No POS wireframes exist anywhere in the repo despite every dossier citing `docs/11-visual-assets/wireframes/POS_PASS8_WORKSPACES.md` (that path does not exist) — treat POS as having zero visual-design evidence; this is a real, standing gap not resolved by this session's work.

## Audit method

Four parallel read-only audits were run against the actual repository (not against dossier prose) before any code was written:
1. Docs/dossiers (blueprint, capability pack, architecture, research, all 40 `features/F26[89]|F27..|F30[0-7]-*.md` dossiers).
2. Existing POS backend + DB (`services/api/src/modules/point-of-sale/index.js`, `database/tenant/migrations/048_point_of_sale_module.sql` + every later migration touching `pos_*`, permissions, existing tests).
3. Existing POS frontend/nav/mobile (`module-navigation-registry.ts`, `apps/web/src/app/**pos**`, `packages/shared-sdk`, `FRONTEND_CONTRACT_REGISTER.csv`, design system primitives, `apps/mobile` offline architecture, `apps/web` PWA/offline infra).
4. Cross-module contracts POS must integrate through (Stock, Sales pricing/tax, Accounting posting, CRM customer master, shared approvals, background jobs, payment-provider precedent).

## Ground truth established by the audit (supersedes any older report/tracker line)

**Backend (`services/api/src/modules/point-of-sale/index.js`, 1005 lines, single file):**
- Real: `getPointOfSaleDashboard`, `listPointOfSaleResource` (generic, unfiltered field-wise), `createStore`, `createTerminal`, `openShift` (+ opening cash movement), `completePointOfSale` (cash-only), `createPointOfSaleReturn` (server-recomputed refund, approval-policy branch), `approvePointOfSaleReturn` (self-approval blocked by user-ID comparison), `completePointOfSaleReturn` (cash-only refund + restock), `closeShift` (variance vs. `pos_cash_movements` sum, no `pos_reconciliations` row written).
- Real and correctly used: Stock integration goes through `postStockMovement` (issue/receipt), never a private fork — regression-tested in `services/api/tests/point-of-sale-stock-integrity.test.mjs`. Idempotency goes through the shared `operation_idempotency` primitive (`beginIdempotentOperation`/`completeIdempotentOperation`) for `pos.sale.complete` / `pos.return.create` / `pos.return.approve` / `pos.return.complete`.
- **Deliberately fails closed, not a bug**: any `payment.method !== "cash"` throws `POS_PAYMENT_PROVIDER_NOT_CONFIGURED` at sale time and `POS_REFUND_PROVIDER_NOT_CONFIGURED` at refund time (`index.js:332-338`, `808-822`). No provider adapter interface exists anywhere in the repo to plug in — this is 100% greenfield.
- **No HTTP route layer exists at all.** No file under `apps/web/src/app/api` references POS. The only web-facing artifact is `apps/web/src/app/(workspace)/pos/page.tsx`, which renders the generic `ModuleFoundationPage` placeholder and makes zero POS API calls. The domain functions in `index.js` are unreachable from any UI today.
- **No Accounting integration**: `pos_sales.accounting_invoice_id` column exists (migration 048) but is never populated; `createJournalEntry`/`postJournalEntry` are never called.
- **No Sales pricing/tax reuse**: POS re-implements a much simpler price-list lookup (`resolvePointOfSaleUnitPrice`) and trusts client-supplied `taxAmount`/`discountAmount` (range-validated only, not derived). `previewSalesDocument` (the real Sales tax/discount engine, handles India CGST/SGST/IGST split) is never called.
- **No customer FK**: `pos_sales.customer_id` has no `REFERENCES tenant.business_parties`; `completePointOfSale` accepts free-text `customerName` and doesn't validate `customer_id` at all.
- **`pos_reconciliations` table is completely unused** by any function despite existing since migration 048.
- **No cash paid-in/paid-out**: `pos_cash_movements.movement_type` CHECK allows `paid_in`/`paid_out`/`closing_adjustment`, but no function ever inserts them, despite the `pos.cash.adjust` permission existing for exactly this.
- **No real cashier/supervisor role split**: only one seeded role, `pos_manager` (`database/platform/migrations/027_role_catalogue_module_completion.sql`), holds all 16 `pos.*` permissions. There is no narrower cashier/operator role. The self-approval block in return-approval compares user IDs, not roles — it only works today because two *different humans* hold the same all-powerful role.
- **POS return-approval is invisible to the global approvals inbox**: it implements its own status machine on `tenant.pos_returns` directly rather than creating a `public.approval_requests` row + `COMMAND_DISPATCH` entry (the pattern every other module's approval flow uses, e.g. `accounting.journal.approve`).
- Test coverage: exactly 2 files touch POS — stock-issue correctness (thorough) and one payment-fail-closed test. Shift lifecycle, return lifecycle, cash variance, and all permission enforcement are untested.

**Frontend/nav:** nav registry (`module-navigation-registry.ts`) lists 8 sections / ~17 destinations across F268-F307 with provisional (non-canonical) F-range comments; only the Home stub is `AVAILABLE`, everything else is `PLANNED` with no backing route file — clicking any of them would 404. No `apps/web/src/features/pos` directory exists. No `packages/shared-sdk` POS contracts exist. No kiosk/focused-mode/touch-first layout primitive exists anywhere in `packages/design-system`.

**Cross-module contracts confirmed usable (cite once, reuse everywhere below):**
- Stock: `postStockMovement(client, c, { movementType: 'issue'|'receipt', itemId, warehouseId, warehouseLocationId?, batchId?, serialId?, quantity, unitCost, referenceType, referenceId, reason, idempotencyKey })` — `services/api/src/modules/stock/index.js:188-256`. Row-locked + advisory-locked, negative-stock policy respected, idempotent.
- Stock reservation (not currently used by POS, available for held-cart stock guarantees): `reserveStock`/`releaseStockReservation`/`getStockAvailability` — `stock/index.js:452-564`, `391-450`.
- Sales pricing/tax: `previewSalesDocument(client, context, input, options)` — `services/api/src/modules/sales/index.js:533-667`. Pure preview, computes India GST split (CGST+SGST intra-state, IGST inter-state), price-list resolution, header discount, rounding. Heavyweight (built for quotations/orders) but is the one legitimate authoritative tax engine.
- Accounting posting: `createJournalEntry` / `submitJournalEntry` / `approveJournalEntry` / `postJournalEntry` / `reverseJournalEntry` — `services/api/src/modules/accounting/journals.js`. `options.internal = true` bypasses the interactive permission gate for trusted cross-module callers. Period-lock checked via `getOpenPeriod` at both create and post. No idempotency key parameter on the contract itself — caller stores the resulting journal id on its own source record (exactly the pattern `pos_sales.accounting_invoice_id` exists for but has never used). Real example to follow: `services/api/src/orchestration/procurement-accounting-vendor-bill.js`.
- Customer master: `tenant.business_parties WHERE party_type IN ('customer','both') AND status='active'`, owned/mutated by CRM's `account-operations.js`. POS must reference, never fork.
- Approvals: `public.approval_requests` + `COMMAND_DISPATCH` map in `services/api/src/core/approvals.js` — register a `pos.return.approve` command key so return approvals surface in the global inbox.
- Background jobs: `enqueueJob`/`claimJobs`/`completeJob`/`failJob` — `services/worker/src/queue.js`, `FOR UPDATE SKIP LOCKED`, DB-enforced `UNIQUE(organization_id, idempotency_key)`. Template for offline-sync replay, payment reconciliation polling, deferred accounting-posting retry.
- Payment providers: **zero reusable adapter/interface exists anywhere in the repo.** The only precedent is SaaS-subscription-billing-specific (`services/api/src/core/billing.js`): pure payload builders, a `shouldApplyProviderEvent` out-of-order-webhook guard (the one reusable idiom), and a "server issues session + public key, client completes, client posts back signature for server verification, never trust client's own success claim" shape. No HMAC verification code, no webhook route, was locatable. **POS is the first payment-provider adapter built in this codebase.**

## Gap matrix — F268-F307

Legend: REAL/VERIFIED, PARTIAL, FOUNDATION ONLY, FRONTEND MISSING, BACKEND MISSING, INTEGRATION MISSING, TEST GAP, EXTERNAL ACTIVATION BLOCKED.

| F | Feature | Status (session 2 updates bolded) | Evidence / gap |
|---|---|---|---|
| F268 | Stores and outlets | **REAL/VERIFIED (admin UI added)** | `tenant.pos_store_access` (migration 115) + `assertPosStoreAccess()` gate every cart/shift/return/product-search entry point — permissive until an organization creates its first assignment row for a company, fail-closed for everyone non-bypass once configured. `updatePosStore`/`setPosStoreActive` added (name/branch/warehouse/price-list/timezone edits; deactivation and warehouse/currency changes blocked while a shift is open or an active/held cart exists — `POS_STORE_HAS_OPEN_SHIFT`/`POS_STORE_HAS_ACTIVE_CART`/`POS_STORE_UNSAFE_TRANSITION`). Real admin screen at `/pos/stores` (list/create/edit/activate-deactivate). Verified against real PostgreSQL: `pos-store-access-f268-f273.test.mjs` (11/11), `pos-store-terminal-cashier-admin.test.mjs` (6/6) |
| F269 | POS terminals | **REAL/VERIFIED (admin UI added)** | `updatePosTerminal`/`setPosTerminalStatus` added (name/receipt-prefix/store reassignment; active/inactive/maintenance; blocked while a shift is open on the terminal). `createTerminal` is now store-access-gated. Real admin screen at `/pos/terminals`. Verified against real PostgreSQL (same two suites as F268) |
| F270 | Cashiers | **PARTIAL (real eligibility model, was FOUNDATION ONLY)** | `tenant.pos_store_access` is the real cashier-eligibility model (which store(s) a user may operate); `features/cashier-access.js`'s `listPosEligibleCashiers` derives candidates from the platform's own `organization_memberships`+`user_role_assignments` (the same "currently active role assignment" shape `resolveSessionContext()` uses for real authorization) filtered to holders of any point-of-sale-module role — a role-less member can never appear. `grantPosStoreAccess`/`revokePosStoreAccess` manage assignments, rejecting a non-member/fabricated user id. Real admin screen at `/pos/cashiers`. `openShift`'s `cashierUserId` is now validated: opening a shift on someone else's behalf requires `pos.terminal.manage`/`pos.store.manage`/`organization_owner` AND the named cashier must themselves pass store-access for that store. Still no terminal-level (only store-level) eligibility. Verified against real PostgreSQL (both suites above) |
| F271 | Cashier permissions | **REAL/VERIFIED** | `pos_cashier`/`pos_supervisor` roles; `pos_return_create_approve` blocking SoD (prior sessions). Added `pos.discount.approve`, split from `pos.discount.apply` the same way (`pos_manager` now holds approve, not apply; `pos_supervisor` keeps apply only) — a new `pos_discount_apply_approve` blocking SoD conflict enforces the split. Verified: `packages/permissions/tests/roles.test.mjs` 8/8. Assignment UI is the platform's own generic role-assignment screen (not audited/rebuilt here) plus the new `/pos/cashiers` store-access screen |
| F272 | Product search | **REAL/VERIFIED** | `searchPointOfSalePosProducts` — bounded, company/store-scoped, excludes cost fields, wired into `/pos/checkout`. Now store-access-gated (was a disclosed gap; closed) |
| F273 | Barcode scanning | **REAL/VERIFIED** | `lookupPointOfSaleBarcode`, wired into `/pos/checkout`. Now store-access-gated (was a disclosed gap; closed) |
| F274 | Product variants | **REAL/VERIFIED (Session 8 — variant-specific pricing closed; F295 lot/serial confirmed already landed)** | `variant_id` flows end-to-end through cart/sale lines. Lot/serial requirement validation landed in a prior session (`5ecbdda5`, migration 117 — `tenant.items.tracking_type`, a real serial-status lifecycle) — this row's own text was stale, not the code; see F295 below. **Variant-specific pricing gap closed this session**: `tenant.price_list_items` gained a `variant_id` column (migration 131); `resolveUnitPrice` (cart-pricing.js) now prefers a variant-specific price-list row over the item-generic one when both exist, and a plain (no-variant) line only ever matches the generic row. Also found and fixed a real, more serious pre-existing bug while wiring this: `services/api/src/modules/sales/pass1-operations.js`'s 12 exported functions (including `upsertSalesPriceListItem`, the ONLY way to create a `price_list_items` row through the product, and `upsertSalesCustomerPrice`, the ONLY way to create the `sales_pricing_rules` rows F275's own fix depends on) were declared in `index.d.ts` and fully implemented but never actually re-exported from `sales/index.js` — completely unreachable from any HTTP route or UI in the entire application. Fixed with one `export * from "./pass1-operations.js"` line; verified no circular-import breakage. **Same-session follow-up**: the separate legacy flat-lines `completePointOfSale` path (`sale-completion.js`) already persisted `line.variantId` onto `pos_sale_lines` but never priced it (same "recorded, never priced" gap this row's own cart-path fix closed) and never validated a supplied `variantId` actually belongs to the claimed item — a real, existing variant belonging to a *different* item could be silently attached to a sale line (a DB FK only stops a nonexistent one). `resolvePointOfSaleUnitPrice` now runs the same variant-preferring price-list query `resolveUnitPrice` does, and the line loop validates the variant the same way `resolveItemAndVariant` (cart-pricing.js) does (belongs to the item, active, company-scoped) before ever pricing or persisting it. `POST /api/pos/sales`'s Zod schema gained the `variantId` field it was missing (the cart-lines route already had it). Verified against real PostgreSQL: `pos-variant-specific-pricing-f274.test.mjs` (7/7, up from 5/5) — the legacy path resolves the Large variant's own rate (130) via `completePointOfSale` directly, and rejects a real variant belonging to a different item with `POS_SALE_VARIANT_NOT_FOUND` |
| F275 | Price lists | **REAL/VERIFIED (Session 6 customer-pricing fix + Session 8 legacy-path closure)** | `resolveUnitPrice` (cart-pricing.js) previously consulted only the store's price list, ignoring any customer entirely — confirmed via direct code read. Now reuses Sales' own authoritative `tenant.sales_pricing_rules` table (the exact one `previewSalesDocument` reads, `sales/index.js:334-364`) via `applyCustomerPricingRules`: discount_percent/discount_amount/fixed_rate applied in priority order, scoped by party/item/item_group/price_list/minimum_quantity, after the store price-list lookup and before tax — no second POS-owned pricing master. Manual price override still bypasses entirely. **Session 8**: the legacy flat-lines `completePointOfSale` path (`sale-completion.js`'s `resolvePointOfSaleUnitPrice`) was disclosed since Session 6 as still store-list-only, and re-examination this session showed it is NOT a dead/deprecated path — it is what `POST /api/pos/sales` (a real, `customerId`-accepting HTTP endpoint) and offline-sync's own sale-completion step (`inventory-and-offline-continuity/offline-sync.js`) both complete through — so a customer's negotiated rate was still being silently ignored on two live surfaces. `applyCustomerPricingRules` is now exported from `cart-pricing.js` and reused (not duplicated) by `resolvePointOfSaleUnitPrice`, requiring `tenant.items.group_id` to be selected alongside the line items it already batch-loads. Verified against real PostgreSQL: `pos-customer-pricing-f275.test.mjs` (9/9, up from 7/7) — the two new assertions prove the legacy path now applies a discount_percent rule and leaves a walk-in (no-customer) sale at the plain list price. The raw-`Number`-arithmetic concern was re-examined and found to be a single value conversion immediately re-entering fixed-point `decimal()` before any arithmetic occurs (not a repeated-float-ops accumulation risk like the F302 bug); left as-is rather than rewritten for its own sake |
| F276 | Customer selection | **REAL/VERIFIED (raw UUID field removed)** | New `searchPointOfSaleCustomers` (bounded to 25, safe fields only — no gstin/pan/credit_limit/msme_number) + `GET /api/pos/customers`, replacing the checkout screen's plain "Customer ID" text field with a real debounced (250ms) search-select built on the design system's `ComboBox`. Select/clear both trigger `setPosCartCustomer`, which already repriced (and therefore re-evaluated coupon/promotion eligibility) before this change — that behavior is unchanged, just reachable without knowing a UUID. Reuses `tenant.business_parties` through the same eligibility rule `setPosCartCustomer`/`completePointOfSale` already enforce; no second customer master. Verified against real PostgreSQL: finds an active customer by partial name/phone, excludes inactive customers and non-customer party types, never selects sensitive fields |
| F277 | Cart | **REAL/VERIFIED** | Real server-side cart aggregate (`tenant.pos_carts`/`pos_cart_lines`) with a draft/priced/held/completed/cancelled/expired state machine, optimistic version locking, one-active-cart-per-terminal DB constraint, and a full command set. `completePosCart` is the fully authoritative sale-completion path, now also gated by store-access and the F279 discount-approval guard. `/pos/checkout` real cashier UI. Held-cart UX (queue + resume) — see F287/F288 |
| F278 | Taxes | **REAL/VERIFIED** | Shared `services/api/src/core/tax-engine.js` (CGST+SGST intra-state vs IGST inter-state, inclusive/exclusive) used by both POS and Sales. Client-supplied `taxAmount` is compared for conflict, never trusted. Missing tax configuration resolves to a real 0-rate row, never a silent exemption bypass |
| F279 | Discounts | **REAL/VERIFIED — genuine maker-checker, forgery hole closed (cart path AND legacy path)** | The prior `approvedBy` field (a client-supplied user id trusted as proof of approval with no authentication check at all) is removed entirely from the API/domain input — no code path reads it anymore. An above-threshold discount now creates a real row in `public.approval_requests` (`command_key: "pos.discount.approve"`), decidable only through the platform's maker-checker engine (`decideApproval()`, the same one Sales uses), which derives the approver from the authenticated session, enforces `pos.discount.approve`, and blocks self-approval via `assertSeparationOfDuties`. `completePosCart()` fails closed (`POS_DISCOUNT_APPROVAL_REQUIRED`) unless an `approved` decision exists bound to the cart's *current* version — a later cart edit invalidates a stale approval and requires re-decision (verified explicitly). Fixed a flat-amount threshold bypass: a cart-level `amount`-type discount used to skip the threshold check entirely (only `percent`-type was ever compared); both now compute effective-percent-of-base with fixed-point decimals, never floats. **Security-matrix item #13** (this pass): the separate legacy flat-lines `completePointOfSale` path had no approval-workflow integration at all — its `pos_settings` SELECT never even fetched `max_line_discount_percent`/`discount_approval_threshold_percent`, so the org's configured cap was silently ignored and any above-threshold flat-line discount was accepted with zero supervisor approval. Fixed: the SELECT now fetches both columns and the path fails closed with `POS_DISCOUNT_APPROVAL_REQUIRED` above the configured threshold, directing the caller to the cart-based checkout (a second, weaker approval mechanism was deliberately not built for a path already flagged for eventual removal). Discount-approval titles carry the amount/percent/reason so a supervisor deciding from the platform's existing generic `/approvals` inbox (reachable from anywhere via the global sidebar) can decide without opening the cart. Verified against real PostgreSQL: `pos-cart-tax-promotions-coupons-f277-f281.test.mjs` (17/17) including forged-approvedBy/self-approval/insufficient-permission/stale-version-after-mutation/flat-amount-bypass/legacy-path-threshold-enforcement/legacy-path-configured-cap-enforcement |
| F280 | Promotions | **REAL/VERIFIED (admin UI added)** | `tenant.pos_promotions` + deterministic priority-ordered evaluation; immutable per-sale evidence; usage_count incremented atomically under the promotion's own row lock. Fixed a real audit-field bug: `updatePosPromotion`/`setPosPromotionActive` stamped the record's OWN id into `updated_by` instead of the acting user (a literal `updated_by=$3` where `$3` was `id`) — both now derive the actor correctly. Added the previously entirely-missing per-customer usage-limit enforcement (checked at preview AND re-checked under the promotion's row lock at commit, closing a real two-terminals-racing-for-the-last-use race). Real admin screen at `/pos/promotions` (list/search/filter/create/edit/activate-deactivate — item/item-group/customer ID-array eligibility has no picker yet, disclosed gap). Verified against real PostgreSQL including a genuine two-connection concurrency test |
| F281 | Coupons | **REAL/VERIFIED (admin UI added)** | Reservation/commit/release lifecycle inside the sale-completion transaction (closes the "two terminals racing for the last use" race for the TOTAL limit). Fixed the same audit-field bug as F280 in `updatePosCoupon`/`setPosCouponActive`. Fixed a real gap: the PER-CUSTOMER limit was only checked at preview time, never re-verified inside the actual completion transaction — now re-checked under the coupon's row lock at commit, proven race-free with a genuine two-connection concurrency test. A FULL return releases the redemption; a partial return still leaves it committed as-is (no fractional-usage-credit model is specified anywhere — documented as the concrete dependency, not silently skipped). Real admin screen at `/pos/coupons` (redemption counts shown read-only; historical redemption facts are never editable) |
| F282 | Cash payments | REAL/VERIFIED (unchanged) | `completePosCart` cash path; change computed correctly; net cash movement posted |
| F283 | Card payments | **REAL/VERIFIED (this row was stale — landed in a parallel worktree session, merged to `main` before Session 6 ever started; not previously reflected in this table)** | Real payment-provider adapter contract (`tender-and-payment-execution/adapter.js`) with a genuine, fully-implemented sandbox adapter (real HMAC-SHA256 webhook signature verification with `timingSafeEqual`, not a stub) — `resolvePaymentAdapter` fails closed with `EXTERNAL ACTIVATION BLOCKED` for any real (non-sandbox) provider key, since no live merchant credentials exist in this environment and none are fabricated. `initiatePosPayment`/`refundPosPayment` real, idempotent, capped at captured amount. Checkout UI has real Card/UPI/Wallet tender options. Verified against real PostgreSQL: `pos-payments-f283-f286.test.mjs` — immediate success/decline, delayed-webhook success, redelivered-webhook no-op, idempotent retry, refund capped at captured amount |
| F284 | UPI and digital payments | **REAL/VERIFIED (same stale-row correction as F283)** | Same adapter contract and sandbox implementation as F283, different tender method. Verified in the same suite: delayed UPI webhook blocks sale completion until the webhook arrives, then completes |
| F285 | Split payments | **REAL/VERIFIED (same stale-row correction as F283)** | `completePosCart` accepts multiple payment legs (cash + captured non-cash payment ids) summing exactly to the sale total via the shared `allocate()` decimal primitive; each leg independently refundable. Verified: cash+card split sums exactly, completes only once the card leg captures |
| F286 | Multiple payment methods | **REAL/VERIFIED (same stale-row correction as F283)** | Same underlying mechanism as F285 — multiple concurrent tender legs on one sale, exactly-once capture per leg, concurrent webhook-delivery race proven race-free with a genuine two-connection test |
| F287 | Hold/suspend sale | **REAL/VERIFIED** | `holdPosCart` releases the terminal's active-cart slot. New `listHeldPosCarts` (the held-cart queue, store-access-scoped, text search over store/terminal/customer name) and a real 24h lazy expiry (holding clears `expires_at`, so a held cart previously had NO abandonment policy at all — checked on next access, same on-read convention as ordinary cart expiry). "Held sales" dialog in `/pos/checkout` lists and resumes. Cross-terminal transfer/claim deliberately NOT implemented (would need its own authorization-checked operation the dossier doesn't clearly require) |
| F288 | Resume sale | **REAL/VERIFIED** | `resumePosCart` now explicitly checks for a different active (draft/priced) cart already on the target terminal before flipping status, failing with a clear `POS_TERMINAL_CART_CONFLICT` instead of a raw unique-constraint violation. Fully reprices on resume (fresh promotion/coupon eligibility, no stale snapshot). Fixed a real, pre-existing UX bug found while wiring this in: the checkout screen's auto-create-cart effect only fired on shift-id change, not on `cart` becoming null again, so "New sale" (and the new "Hold sale") would clear the cart and never create a fresh one — extracted `startFreshCart()` and call it from both. Verified against real PostgreSQL: `pos-hold-resume-f287-f288.test.mjs` (5/5) |
| F289 | Receipt printing | **REAL/VERIFIED (Session 6 — durable print-attempt evidence added)** | `getPosSaleReceipt` builds a deterministic receipt entirely from persisted facts — unchanged. **Fixed a real, self-disclosed gap**: the file's own prior comment admitted "'Original' vs 'reprint' is derived from whether this is the first read after completion or not tracked at all here" and the frontend badge was driven by a client-supplied `?original=1` URL query parameter — trivially forgeable by anyone typing that into the address bar, proving nothing. New `tenant.pos_receipt_print_events` (migration 129, immutable via trigger) + `recordPosReceiptPrintAttempt`/`listPosReceiptPrintEvents` record that a print was REQUESTED (never that a physical printer confirmed output — no browser API can observe that; this distinguishes "print requested" from "printer result confirmed" exactly as the brief asked, and is honest that only the former is achievable). `print_type` (original/first vs. reprint) is derived server-side from whether a prior event row already exists, never from client input. `PosReceiptScreen.tsx`'s badge now reads real print history (`Not yet printed` / `Printed — original` / `Reprinted (N×)`), and clicking Print records the event via a new `POST /api/pos/sales/[id]/receipt/print` route (billing-write-gated, same as every other POS mutation) before invoking `window.print()`. Verified: real Postgres coverage via the pre-existing F277-F281 receipt assertion (unaffected) plus manual route/typecheck/lint/build verification; a live-browser E2E assertion was updated (`pos-checkout.spec.ts`) to assert the new server-derived badge and a real `pos_receipt_print_events` row instead of the old `?original=1` param, though a full green E2E run was blocked this session by pre-existing environment flakiness unrelated to this change (see Session 6 narrative). **Prompt 3 (Section 6C browser-side recovery)**: found and fixed a real, silent failure mode — `PosReceiptScreen.tsx`'s print-attempt mutation had an `onSuccess` handler (fires `window.print()`) but no `onError` handling at all, so a failed `recordPosReceiptPrint` call (network blip, permission change, server error) left the cashier with a stuck loading spinner and no explanation, and `window.print()` correctly never fired but nothing said why. Added a `role="alert"` error banner (matching the checkout screen's own established pattern) and relabels the button "Retry print" so the existing click handler doubles as the retry action. Device/hardware failure recovery (a physical printer jam, out of paper, offline printer) remains honestly out of reach of any browser API — not reclassified as NOT_APPLICABLE, left as an accurately-disclosed permanent limitation (see F289-CAP-001's own override note) |
| F290 | Invoice generation | **REAL/VERIFIED (Session 4)** | `generatePosInvoice` creates a real `tenant.accounting_customer_invoices` row through Accounting's own `createCustomerInvoice`/`submitSubledgerDocument`/`postCustomerInvoice` contract (never a POS-owned invoice table), idempotent per sale via `pos_sales.accounting_invoice_id`, requires a real customer (`POS_INVOICE_CUSTOMER_REQUIRED` for a walk-in sale). On-demand from the receipt screen (`/pos/receipts/[saleId]`) and browsable at `/pos/invoices`. Verified against real PostgreSQL |
| F291 | Returns | **REAL/VERIFIED (operator UI added)** | New `findPosSaleForReturn` (receipt-number search, store-access-gated, reports real remaining-returnable quantity per line using the exact same math `createPointOfSaleReturn` itself enforces). Real screen at `/pos/returns`: search, select eligible quantities (bounded to remaining), per-line restock checkbox, reason, request; grid of existing returns with status-appropriate actions |
| F292 | Refunds | **REAL/VERIFIED (operator UI added; Prompt 3 — historical allocation backfilled)** | Cash refund real; "Complete refund" action in `/pos/returns` (gated on `pos.return.approve`, the same authority that approves). Non-cash never offered as an option in the UI, matching the backend's fail-closed behavior. **Section 6D**: investigated the live database for returns predating migration 130 (Gap B's exact-refund-allocation fix) lacking a persisted `pos_return_payment_refunds` row. Found 4 real historical returns, all 100%-cash with zero non-cash tender legs — an unambiguous, not estimated, allocation — and backfilled them via migration 132. Found 53 more, sampled and confirmed they reference `organization_id`s with no corresponding `public.organizations` row (orphaned test-run debris, unreachable by any tenant-scoped read); deliberately left untouched rather than guessed at. No return with genuine multi-tender ambiguity was backfilled — `accounting-posting.js`'s existing proportional-reconstruction fallback continues to serve those correctly and transparently, exactly as designed |
| F293 | Exchanges | **REAL/VERIFIED** | `completePosExchange` implements exchange as linked lineage: calls the existing, already-tested `completePointOfSaleReturn()` and `completePosCart()` exactly once each inside ONE transaction (a failure partway rolls back both), then stamps the new `pos_sales.exchange_return_id` column (migration 116). No new stock/cash/payment logic invented. Settlement is not netted into one payment — the return posts its own refund movement, the replacement sale posts its own sale movement (economically identical to netting for both equal- and uneven-value exchanges). Requires the return to already be approved AND requires the acting session to hold both `pos.sale.create` and `pos.return.approve` (completing an exchange completes its return half too). "Exchange" action on an approved return in `/pos/returns` hands off to `/pos/checkout?exchangeReturnId=` for ringing up the replacement item(s); "Complete cash sale" becomes "Complete exchange". Verified against real PostgreSQL: `pos-exchange-f293.test.mjs` (6/6) covering the not-yet-approved block, the permission gate, exactly-once stock/cash effects, linkage, and idempotent-retry safety |
| F294 | Stock reduction | REAL/VERIFIED (unchanged) | Goes through canonical `postStockMovement`; regression-tested against real double-decrement/idempotency/insufficient-stock races |
| F295 | Lot/serial support | **REAL/VERIFIED (this row was stale — the blocker it describes was closed in a parallel session, `5ecbdda5`, before Session 6 ever started)** | The Stock-module gap this row described (no tracking-type column, no serial-status lifecycle) was real when written but is no longer true: migration 117 added `tenant.items.tracking_type` (`none`/`batch`/`serial`) and a genuine serial-status lifecycle (`available`→`sold`, restored on a full return) inside Stock's own `postStockMovement`, never a POS-owned fork. Verified against real PostgreSQL: `pos-serial-batch-tracking-f295.test.mjs` (6/6) — selling without a serial on a serial-tracked item is rejected, selling the same serial twice is rejected, a full return restores the serial to available and it can genuinely be sold again, a batch-tracked item requires a batch |
| F296 | Real-time inventory | **REAL/VERIFIED (row text already argued this was correct architecture, not a gap — status label corrected to match)** | `stockAvailable()` unlocked pre-check for search/UI; final safety is Stock's own row lock inside `postStockMovement` — this is the correct, deliberate design (POS never re-implements Stock's own concurrency guarantee), proven race-free by the existing two-connection last-unit-stock test (security matrix item #19) |
| F297 | Offline POS | **REAL/VERIFIED (this row was stale — landed in a parallel worktree session, merged to `main` before Session 6 ever started)** | Real IndexedDB-backed offline queue (`apps/web/src/features/pos/offline/`) wired directly into the checkout screen (`PosCheckoutScreen.tsx` checks IndexedDB before the "no open shift" guard) — a bounded catalog snapshot, offline sale capture, and a real conflict-resolution admin screen (`/pos/offline-sync-conflicts`). Backend: `getPosOfflineSnapshot`/`syncOfflinePosSale`/`resolvePosOfflineSyncConflict`. Verified against real PostgreSQL: `pos-offline-sync-f297-f298.test.mjs` — bounded snapshot with an unsupported-operations list, offline sale creation, local transaction-id replay is a safe no-op, a stale-price sync is routed to conflict (not completed at the stale price), a sync against an already-closed shift is routed to conflict, an unsupported non-cash tender is routed to conflict, two offline terminals racing for the last unit converge to exactly one sale and one conflict |
| F298 | Offline-to-online sync | **REAL/VERIFIED (same stale-row correction as F297)** | Idempotent upload via the local transaction id; interrupted/duplicate synchronization never duplicates the sale, stock movement or cash effect. See F297's own evidence — the same suite covers both. |
| F299 | Cash drawer opening balance | REAL/VERIFIED (unchanged) | `openShift` posts an immutable opening cash movement correctly |
| F300 | Cash movements | **REAL/VERIFIED (idempotency added)** | `recordPosCashMovement`/`listPosCashMovements` added — `pos_cash_movements.movement_type` accepted `paid_in`/`paid_out` in the schema since before this pass, but nothing ever wrote one. Amount/reason validation, requires an open shift on an accessible store, signed the same way `completePointOfSaleReturn`'s refund movement already is (positive = cash added, negative = removed). Every movement is immutable once inserted (no update/delete path exists). **Security-matrix item #23** (this pass): `recordPosCashMovement` had no idempotency protection at all — unlike every other financial mutation in the module, a client retry could double-post a movement with no way to detect it after the fact. Fixed: wired into the same `operation_idempotency` reserve-then-complete mechanism the rest of the module uses; `idempotencyKey` is now a required input (every call site updated: route Zod schema, `pos-api.ts`, shared-sdk, `index.d.ts`, and the `/pos` overview UI, which rotates the key after each successful post). UI: a "Cash paid in / out" section in the `/pos` overview screen's open-shift panel with a running log. Verified against real PostgreSQL: signed amounts for both types, validation, listing, and a retried call with the same key replaying the original movement (exactly one row persisted) vs. the same key with a different payload failing closed with `IDEMPOTENCY_KEY_REUSED` |
| F301 | Shift opening | **REAL/VERIFIED (Session 6 — idempotency-key protection added)** | Previously relied ONLY on the DB's one-open-shift-per-terminal unique index — correct for rejecting a genuinely different concurrent open, but a RETRY of the same request (client timeout, double-tap) had no replay contract and would surface as a raw unique-constraint error instead of safely returning the original shift. `openShift` now uses the same `beginIdempotentOperation`/`completeIdempotentOperation` reserve-then-complete contract every other POS financial mutation uses (`recordPosCashMovement`, `completePosCart`, etc.): `idempotencyKey` is now a required input — every call site updated (`POST /api/pos/shifts`'s Zod schema, the `/pos` overview screen's open-shift mutation with the same key-rotation-on-success pattern `recordPosCashMovement`'s UI already used, `pos-fixtures.ts`'s e2e persona setup, and all 28 direct domain-function call sites across 5 real-Postgres integration test files). A retry with the SAME key safely replays the original shift; the same key with a DIFFERENT payload is rejected (`IDEMPOTENCY_KEY_REUSED`); a genuinely different request still hits the DB constraint as before. Verified: the full existing real-Postgres suite (254/254, including every test that opens a shift) passes unchanged after the signature change, proving no regression. **Prompt 3 (Section 6B)**: the register's own override had disclosed that no dedicated test proved openShift's own replay-safety/reuse-rejection/concurrency/authorization behavior specifically (only inferred from the shared primitive's generic coverage plus a passing regression sweep). New `tests/integration/pos-shift-open-idempotency-f301.test.mjs` (5/5) proves all four directly: same-key/same-payload replay returns the identical shift with exactly one row persisted; same-key/different-payload is rejected with `IDEMPOTENCY_KEY_REUSED`; a replay attempt by an unauthorized user is stopped by permission checks before ever reaching the idempotency table; and a genuine two-Postgres-connection concurrent race with the identical key/payload converges to exactly one shift, both callers observing the same id |
| F302 | Shift closing | **REAL/VERIFIED (Session 6 — decimal arithmetic fixed, in addition to Session 3's unresolved-transaction guards)** | Unresolved-transaction guards (active cart / pending return, held cart exempt) unchanged from Session 3. **Fixed a real gap this session**: `closeShift`'s cash-variance calculation used native JS `Number(...) - Number(...)` — the exact floating-point-for-money violation the brief explicitly asked to investigate, and, being untyped `Number()` on a client-supplied `countedCash`, non-numeric input would previously have silently produced `NaN` written to the database rather than failing closed. Now uses the same fixed-point `decimal`/`sub`/`asDatabaseDecimal` utilities every other monetary calculation in this module already uses; invalid input now throws instead of persisting `NaN`. Still does not write `pos_reconciliations` (unchanged from Session 3 — F304 populates that table from the day-end report, not shift-close, per Session 4). Verified against real PostgreSQL: the full existing `pos-cash-movements-and-shift-close-f299-f302.test.mjs` suite passes unchanged with exact decimal variance values, proving no regression from the arithmetic rewrite |
| F303 | Day-end / Z report | **REAL/VERIFIED** | See the F303 integration session narrative below — draft/reviewed/closed maker-checker, DB-trigger-enforced immutability, append-only corrections |
| F304 | Payment reconciliation | **REAL/VERIFIED (Session 4)** | `tenant.pos_reconciliations` re-keyed to `(day_end_report_id, payment_method)` (migration 127), a resolved row immutable via `pos_reconciliation_protect_resolved()` trigger. Cash reconciles against F303's own counted-vs-expected cash count (no duplicate calculation); every other tender reconciles against real settlement evidence imported via `importPosSettlementBatch` (provider statement/file, or the sandbox adapter's own test-settlement generator — this environment has no live merchant-certified provider, disclosed not fabricated) and matched against `pos_payments.provider_reference`, distinguishing `captured` from `settled` for the first time. `generatePosReconciliation`/`resolvePosReconciliation` write back onto the closed day-end report's `reconciliation_status`/`reconciliation_references`/`outstanding_exceptions` columns (migration 121's own reserved slot). Genuine maker-checker: `pos.reconciliation.manage` (match/generate) vs `pos.reconciliation.approve` (resolve a variance), blocking SoD conflict, same-person resolve blocked at runtime. Verified against real PostgreSQL including a real captured card payment matched by its own real provider reference and a genuine cash variance investigated/resolved by a different approver |
| F305 | POS accounting posting | **REAL/VERIFIED (Session 4)** | `postPosSaleToAccounting`/`postPosReturnToAccounting` post a real, balanced journal through Accounting's own `createJournalEntry`/`postJournalEntry` contract (never a POS-owned ledger) — revenue/output_tax/rounding/cogs reuse the SAME pre-seeded mapping keys Sales' own invoices post through (zero config); tender-clearing (card/upi/wallet/store-credit), inventory relief and the two loyalty-accrual accounts are new POS-specific mapping keys needing one-time admin configuration via Accounting's existing generic mapping API (no POS-specific mapping UI was built, per the task's own "no functionality outside canonical scope" instruction). COGS/inventory relief is real, sourced from `stock_movements.unit_cost` (not invented). A posting failure is caught inside a SAVEPOINT and recorded as `pos_sales.accounting_posting_status='failed'` with the real error message — visible and retryable (`/pos/accounting`), never silently lost or rolled back with the sale itself. `postPosDayEndReportToAccounting` posts a closed report's remaining sales/returns best-effort (mirrors `procurement-accounting-vendor-bill.js`'s own documented precedent). Idempotent per sale/return via `journal_entry_id`. Verified against real PostgreSQL: a cash sale, a card sale (tender debits the configured clearing account), and a full best-effort batch post, all with a balanced-journal assertion |
| F306 | Loyalty | **REAL/VERIFIED** | See `f1967b83`'s own commit message — real ledger, program config, checkout redemption UI |
| F307 | POS sales analytics | **REAL/VERIFIED (Session 4)** | `getPosSalesAnalytics` replaces the coarse today-only dashboard aggregate with a real date-range/store/terminal/cashier-filtered report: gross/net sales, transaction count, AOV, store/terminal/cashier/product-category breakdowns, tender split, promotion/coupon/other discount split (sourced from `pos_promotion_applications`/`pos_coupon_redemptions`, not invented), returns/refunds, cash variance (reused from F303), reconciliation exceptions (F304), offline-sync exceptions (F297/298), accounting-posting status (F305), loyalty activity (F306), and margin (only over lines with a real linked `stock_movement` — never assumed for untracked items). Every figure is a real aggregate over the same rows F303/F304 already treat as authoritative, no duplicated calculation. Scoped by `accessiblePosStoreIds` the same as every other POS read. Verified against real PostgreSQL: known fixture sales/tender/COGS totals reconcile exactly |

## Implementation decisions this session

1. **Do not rebuild the giant `index.js` from scratch.** Extend it, and begin splitting new capability work into dedicated files under `services/api/src/modules/point-of-sale/` per the POS-CAP-00x boundaries, re-exported from `index.js` — mirroring the pattern already used by `services/api/src/modules/crm/index.js`.
2. **Fix the two gaps that block everything else first**: (a) there is no HTTP route layer, so nothing built is reachable; (b) there is no real cashier-vs-supervisor role, so "cashier permissions" (F271) is not a real feature yet. Both are Tranche-0-level foundation repairs, done before new capability code.
3. **Server-authoritative pricing/tax**: reuse `previewSalesDocument` rather than extending POS's own simplified lookup, per the explicit instruction not to trust browser-supplied tax/discount amounts and not to fork a second pricing engine.
4. **Payment provider architecture**: build the adapter *interface* and a test-only sandbox adapter now; keep production fail-closed exactly as today until a real merchant-certified provider is wired — this is an EXTERNAL ACTIVATION BLOCKED item for the actual card/UPI capture, not for the code-controllable adapter boundary.
5. Continue numbering DB migrations from `112` under `database/tenant/migrations/` (last: `111_f029_opportunity_bulk_scale.sql`) and from `039` under `database/platform/migrations/` (last: `038_attachment_versioning.sql`) — these are two separate numbering sequences, do not conflate them.

## Continuation point (read this first in any future session)

This is a genuinely large, multi-tranche program (40 features spanning store/terminal/cashier control, a full cart/pricing/tax/promotion engine, a payment-provider framework, receipts/invoices, returns/refunds/exchanges, offline sync, shift/cash/reconciliation/accounting, loyalty, and analytics — each with real DB, backend, API, frontend, and test surface). It is not realistic to complete in a single pass, and the trackers already on record (`PRODUCTION_TRACKER.md`, `ERP_COMPLETION_EXECUTION_TRACKER.md`) reflect that reality rather than a false "quick fix" framing.

Update this section at the end of every future session with: what tranche was completed, what verification ran, and the exact next tranche to pick up.

### Session 1 (this session) — what actually landed

Starting SHA `524dc6a8`, five commits on `rebuild/clean-frontend` (each independently verified against real PostgreSQL and, where applicable, real HTTP against the running dev server — not just typecheck/lint/fake-client tests):

1. `af380e17` — this tracker + the four-audit gap matrix (the required first deliverable).
2. `157d8167` — real `pos_cashier`/`pos_supervisor` roles (F270/F271 foundation), a new `pos_return_create_approve` blocking SoD conflict, `pos_manager` no longer holds both `pos.return.create` and `pos.return.approve`. Platform migration 039.
3. `7a23ad37` — the entire missing HTTP route layer (`apps/web/src/app/api/pos/**`, `apps/web/src/features/pos/shared/pos-context.ts`) for every domain function that existed before this session (stores, terminals, shifts+close, sales, returns+approve+complete, dashboard, generic resource list). Also fixed a real bug this exposed: `completePointOfSale` inserted `line.description` into a NOT NULL column with no fallback, and `closeShift`'s "not found" path threw a bare `Error` instead of a proper 404.
4. `c857d622` — F276 customer validation: `completePointOfSale` now validates `customerId` against `tenant.business_parties`; tenant migration 112 adds the missing FK.
5. `26691647` — F272/F273: `searchPointOfSalePosProducts` and `lookupPointOfSaleBarcode`, the first capability-owned file (`features/assortment.js`) split out of the original single `index.js`, with HTTP routes.
6. `2cc9b8a9` — taught `scripts/qa/generate-route-security-matrix.mjs` to recognize `requirePosAccess` (the POS analogue of `requireCrmAccess`) as a real authorization primitive; the 8 new POS mutation routes were already passing `pnpm verify:route-security` (it only requires authentication + an origin check, both present from commit 3 onward) but showed `has_authorization_check=false` in the generated matrix purely because the pattern list didn't know the function name yet.

Verification run repeatedly through the session, every gate green: `pnpm test:api` (1104/1104), `pnpm test:web` (21/21), `pnpm test:sdk` (14/14), `pnpm test:worker` (102/102), `pnpm test:security` (4/4), `pnpm test:integration` (5/5, real Postgres — see toolchain note below), `packages/permissions/tests/roles.test.mjs` (8/8), `pnpm typecheck:web`, `pnpm lint:web`, `pnpm build:web`, `pnpm verify:db`, `pnpm verify:architecture`, `pnpm verify:routes`, `pnpm verify:route-security`. Real end-to-end HTTP smoke tests were run against the actual dev server (port 3001) and real Postgres for: store→terminal→shift→cash-sale→idempotent-replay→non-cash-fails-closed→shift-close, customer validation (real customer accepted, fake one rejected), product search, and barcode lookup — each confirmed by inspecting actual database state afterward (e.g. `stock_balances` decremented 100→98), not just a 2xx status code.

**Toolchain note for future sessions**: the composite `pnpm verify:erp` (= `pnpm verify`) fails immediately at its first step, `verify:toolchain`, because this environment runs Node v26.5.0 and the repo pins exactly Node 24 — a pre-existing environment characteristic, not something this session's changes caused (nothing here touches `.nvmrc`/`.node-version`/engines config). Every individual gate the composite would have chained (`verify:t01`, `verify:experience`, `verify:architecture`, `verify:fast`, `verify:routes`, `verify:route-security`, `verify:mobile`, `verify:db`, `verify:worker`, `test:sdk`, `test:packages`, `test:integration`, `test:security`, `test:enterprise-rbac`) was run directly instead (bypassing the toolchain gate) and passed. A real Node 24 environment should be able to run the composite `pnpm verify:erp` directly without this workaround.

One transient false alarm worth recording so it isn't rediscovered: running `test:integration` ad hoc with `DATABASE_URL` accidentally set to the *superuser* connection string (same as `MIGRATION_DATABASE_URL`) makes the RLS-isolation test fail, because a superuser has `BYPASSRLS` — the failure means the test setup is wrong, not that RLS is broken. `DATABASE_URL` must be the actual restricted runtime role (`vercent_app`, provisioned by `pnpm db:provision:runtime-role`) for that test to mean anything; with the correct role, all 5 integration tests pass.

**Not started this session** (i.e. still exactly as the original audit found them, see the gap matrix above): F274 (variant identity on sale lines), F275 (Sales' `previewSalesDocument` reuse for real tax), F277 (a real server-side cart/hold aggregate — F287/F288), F278-F281 (server-authoritative tax/discount-policy/promotions/coupons), the entire payment-provider adapter (F283-F286), F289/F290 (receipts/invoices), F293 (exchanges), F295 (lot/serial requirement validation in POS itself, though Stock's own contract already supports it), F296 (a dedicated availability query beyond the unlocked pre-check), F297/F298 (offline), F303-F305 (Z report/reconciliation/Accounting posting), F306 (loyalty), F307 (real analytics beyond the coarse dashboard). **All frontend/UI work is still entirely unstarted** — every route added this session is an API contract only; `/pos` still renders the generic `ModuleFoundationPage` placeholder.

### Session 2 — what actually landed

**Phase 0 reconciliation**: session 1's 8 commits (`af380e17`..`e5304a22`) were already safely present locally AND already pushed to `origin/rebuild/clean-frontend` by the tail end of an earlier conversation in this same overall engagement (not by session 2 itself) — verified `git rev-list --left-right --count HEAD...origin/rebuild/clean-frontend` = `0 0` and PR #8's `headRefOid` matched local HEAD exactly before any session-2 work began. No reconciliation was needed; starting SHA for session 2's own work is `4d0f82c5`.

Starting SHA `4d0f82c5`, six commits on `rebuild/clean-frontend`, each verified against real PostgreSQL and (for the frontend) a real running browser — not just typecheck/lint/mocked tests:

1. `f1a06fdf` — the entire F277-F281 backend: `tenant.pos_carts`/`pos_cart_lines`/`pos_cart_discount_approvals`/`pos_promotions`/`pos_promotion_applications`/`pos_coupons`/`pos_coupon_redemptions` (migration 113), `services/api/src/core/tax-engine.js` (shared with Sales, which was refactored to call it — Sales' own 79-test suite re-verified unchanged), `features/cart-pricing.js` (the single authoritative pricing/tax/discount/promotion/coupon evaluator), `features/cart.js` (the cart aggregate CRUD + state machine), `features/promotions.js`/`features/coupons.js`, `completePosCart` (the new primary sale-completion path), and a decimal-safe rewrite of the legacy `completePointOfSale`'s tax/discount handling. Also fixed a real, previously-undetected bug in `createPointOfSaleReturn` (a missing `::uuid` cast that a real-Postgres/non-mocked test finally exposed).
2. Type declarations (`services/api/src/modules/point-of-sale/index.d.ts`) for every new export.
3. `apps/web/src/app/api/pos/{carts,promotions,coupons}/**` — 18 new HTTP routes (cart CRUD/lines/discount/customer/coupon/hold/resume/cancel/complete; promotion and coupon admin CRUD), all passing `verify:route-security` with a real authorization check, not just an origin/auth floor.
4. Rewrote `packages/shared-sdk/src/point-of-sale.js` (previously dead code — exported from the package index but imported nowhere, and targeting a `/api/point-of-sale` base path no route in the repo has ever served) to match the real `/api/pos/**` contracts and add the new cart/promotion/coupon methods.
5. `apps/web/src/features/pos/{shared,screens}/**` — the first real POS frontend: `/pos` (shift open/close + dashboard, replacing the generic `ModuleFoundationPage` placeholder) and `/pos/checkout` (search/scan, cart lines with quantity controls, customer/coupon/cart-discount fields, a live subtotal/discount/tax/total breakdown, cash tender with change calculation, and a real confirmation screen with the authoritative receipt number).
6. Two genuine bugs found and fixed via real-browser testing that no amount of unit/typecheck coverage would have caught: (a) `reprice()` was reading `customer_id`/`coupon_code`/`cart_discount_*` off the caller's **stale pre-mutation** cart object rather than the row it had just updated, so e.g. setting the customer immediately after applying a coupon silently reverted the coupon on the very next reprice; (b) receipt numbers were sequenced **per terminal** but the DB uniqueness constraint is **per organization** — two terminals sharing the schema's own default `receipt_prefix` ('POS') collide on their very first sale ("POS-000001" vs "POS-000001") and the second one hard-fails with an unhandled 500. Both are permanently regression-tested (see below).

**Real-browser QA performed** (not certified from unit tests alone, per the session brief): `/pos` and `/pos/checkout` loaded cleanly at 1440/1024/390px with zero console errors; a full cash-checkout journey — search a real product, add it to a real cart, tender cash, complete the sale — was driven through the actual rendered UI against the actual running dev server and real Postgres, ending in a real confirmation screen showing the authoritative server-generated receipt number (`POS-000003` in the recorded run) and change due. This same testing is what surfaced both bugs in item 6 above and a third, purely-frontend bug (the cart-pricing.js pipeline returns raw snake_case DB rows — `grand_total`, `unit_price`, `coupon_code` — not the camelCase shape the first draft of the `.d.ts`/React components assumed; TypeScript's `[key: string]: any` escape hatch on the cart types let this compile cleanly while rendering "INR 0.00" for every total in the browser). All three are fixed; the type declarations now document the real snake_case shape with an explanatory comment so the mistake isn't repeated.

**Verification**: `pnpm test:api` 1112/1112, `pnpm test:web` 21/21, `pnpm test:sdk` 14/14, `pnpm test:worker` 102/102, `pnpm test:packages` all-green (10 packages), `pnpm test:security` 4/4, `pnpm test:enterprise-rbac` 5/5, `node --test tests/integration/*.test.mjs` 21/21 (includes the new 11-assertion F277-F281 real-Postgres suite), `typecheck:web`/`lint:web`/`build:web` clean, `verify:db` (113 tenant migrations), `verify:architecture`, `verify:routes` (172 route.ts, 116 CRM + the 18 new POS ones), `verify:route-security` (140 mutation routes, 0 unexplained gaps), `verify:t01`, `verify:mobile` typecheck. `verify:experience` reports 3 pre-existing raw-`<table>` violations in CRM dashboard/forecast/import-export screens this session never touched — unrelated, not introduced here. `pnpm verify:erp` still fails at its first step (`verify:toolchain`, Node v26.5.0 vs the repo's pinned v24) for the exact same pre-existing environment reason session 1 already documented; every individual gate the composite chains was run directly above and passed.

**Not built this session** (disclosed, not silently skipped): promotion/coupon *configuration* UI (API-only; a cashier can apply a code, but only an API caller can create one); a "held carts" resume list; a cart-expiry sweep job (lazy/on-read expiry only); a customer *search*-select in checkout (plain ID field — F276's pre-existing gap); variant-specific pricing; partial-return coupon reversal; receipt printing (F289, explicitly out of this session's scope per the brief); card/UPI provider adapters (F283-F286, explicitly out of scope); Playwright E2E automation for the new cashier journey (the journey was proven manually through a real, scripted browser session this pass, but no `apps/web/e2e/*.spec.ts` file was added for it — the closest existing spec, `crm-regression.spec.ts`, is CRM-only); the full PHASE 13 negative-security-test matrix (cross-store/cross-company cart access, forged-unit-price, inactive-price-list, etc.) beyond what the mocked/unit suites and the route-security matrix already cover structurally.

### Session 3 (this session) — what actually landed so far

Starting SHA `c39b7ac8` (no reconciliation needed — `main` was already up to date with the prior session's merged work; note the task's cited historical PR #8 SHA `027695ef` is stale/superseded, `main` is the correct base). Work so far is uncommitted in the working tree pending a logical commit split; not yet pushed.

**Phase 1 — SECURITY FIX (top priority, complete and verified)**: see the rewritten F279 row above for the full description. Files touched: `services/api/src/modules/point-of-sale/features/cart.js` (rewrote `applyPosCartLineDiscount`/`setPosCartDiscount`, removed `requireApprover`/`recordDiscountApproval`, added `ensureDiscountApprovalRequested`/`approvePosCartDiscountApproval`/`rejectPosCartDiscountApproval`/`assertPosCartDiscountsApproved`), `services/api/src/modules/point-of-sale/index.js` (checkout guard in `completePosCart`), `services/api/src/core/approvals.js` (registered `pos.discount.approve` in `COMMAND_DISPATCH`), `packages/permissions/src/{point-of-sale,roles}.js` (new permission + SoD conflict), platform migration `040_pos_discount_approval_permission.sql`, tenant migration `114_pos_discount_approval_integrity.sql`, both POS discount route Zod schemas (dropped `approvedBy`), `index.d.ts`. Rewrote the F279 block of `tests/integration/pos-cart-tax-promotions-coupons-f277-f281.test.mjs` with the mandatory negative tests. **No dedicated approval-decision route was needed** — the platform's existing generic `/api/approvals`/`/api/approvals/[id]/decide` routes already work for POS once the command was registered.

**Phase 2 — store/terminal/cashier record-level access (first real slice, not the full scope)**: researched existing platform access-control infrastructure first (per the task's explicit instruction not to invent a parallel identity system) — confirmed the platform already has a generic `membership_company_access`/`membership_branch_access`/`membership_department_access` pattern (`database/platform/migrations/002_platform_foundation.sql`) but nothing at store granularity, and no other module implements a hard "user restricted to assigned sub-resource" gate (CRM territory assignment is routing/eligibility, not security). Added `tenant.pos_store_access` (migration 115, tenant-schema because `pos_stores` is tenant-schema data, with the same RLS treatment every other POS table has) and `assertPosStoreAccess()` in `features/cart.js`, wired into `lockCart`/`getPosCart`/`createPosCart` (covers every cart mutation/read), `completePosCart` and `openShift` in `index.js`, and row-filtering in `listPointOfSaleResource` for `stores`/`terminals`/`shifts`/`sales`/`returns`. Deliberately **permissive until configured**: an organization that has never created a `pos_store_access` row is completely unaffected (every existing single-store tenant and the entire F277-F281 test suite, which configures none, keeps passing unchanged); the moment any row is created for a company, every non-bypass user needs an explicit grant to touch that store's carts/shifts. `pos.store.manage`/`pos.settings.manage`/`organization_owner`/`system_administrator` always bypass. Verified against real PostgreSQL: new suite `tests/integration/pos-store-access-f268-f273.test.mjs` (7/7) proves the permissive default, cross-store read/write/complete denial in both directions, legitimate same-store completion, and the manager/owner bypass.

**Phase 2 gap closure (same session, follow-up pass)**: closed most of the disclosed gaps above. `createTerminal` (input storeId), `openShift` (input storeId), `closeShift` (the fetched shift's store_id), `completePosCart` and the legacy `completePointOfSale` (the fetched shift's store_id), `createPointOfSaleReturn`/`approvePointOfSaleReturn`/`completePointOfSaleReturn` (the underlying sale's/return's store_id), and `searchPointOfSalePosProducts`/`lookupPointOfSaleBarcode` (`features/assortment.js`, the resolved store's id) are now all store-access-gated. Also closed a related, previously undisclosed gap found while doing this: `openShift`'s `input.cashierUserId` let ANY caller with `pos.shift.open` name an arbitrary user as a shift's cashier with zero checks — opening a shift on behalf of someone else now requires `pos.terminal.manage`/`pos.store.manage`/`organization_owner`, AND the named cashier must themselves pass `assertPosStoreAccess` for that store (checked against the assigned cashier's own eligibility, not the caller's bypass permissions). Verified against real PostgreSQL: the store-access suite grew from 7 to 11 tests, covering the legacy sale path, terminal creation, the full return create/approve/complete chain, and the on-behalf-of shift-open rule — all passing, plus the full existing regression sweep (`test:api` 1112/1112, `test:sdk`/`test:packages`, all `tests/integration/*` and `services/api/tests/point-of-sale-*` suites) with zero failures (three hand-rolled mock-client unit tests needed a one-line `pos_store_access` case added to their query router, since a mock has no real table to return an empty result from automatically).

**Phase 2's originally-disclosed gaps closed later in this same session**: `createTerminal`/`openShift`/`closeShift`/the legacy `completePointOfSale`/the full return create-approve-complete chain/product search/barcode lookup are all now store-access-gated (see the F268-F273 gap-matrix rows above). `pos_payments`/`pos_cash_movements`/`pos_reconciliations` row-filtering in `listPointOfSaleResource` and true terminal-level (not just store-level) cashier eligibility remain genuinely not built — small, disclosed, low-priority residual gaps, not silently dropped.

### Session 3 (this session) — consolidated pass, full account

This single session ended up covering far more than the original security-fix brief once the owner asked for a large consolidated pass across the rest of the core retail POS surface. Starting SHA `c39b7ac8` (`main`); branch reconciliation: `origin/rebuild/clean-frontend` was frozen at `027695ef`, a strict ancestor of `main` (verified via `git merge-base main rebuild/clean-frontend` = `027695ef`, `git rev-list --left-right --count main...rebuild/clean-frontend` = `11 0`) — safely fast-forwarded local `rebuild/clean-frontend` onto `main` with `git merge --ff-only main` (no rebase, no reset, no force-push; both branches' histories fully preserved) and continued all new work there, per the task's explicit instructions. `apps/landing/scripts/prepare-standalone.mjs` has an unrelated, pre-existing uncommitted change from earlier in this engagement (a Hostinger SWC-helper fix) that this session found already sitting in the working tree and deliberately left alone/uncommitted — not this session's work, not touched.

Also fixed, as a genuine local-dev blocker unrelated to POS but discovered and needed before any of the above could be tested: an earlier Hostinger-deployment fix (commit `d62e0c65`) changed root `package.json`'s `packageManager` to `npm@11.5.1` so Hostinger's own install step works, but Corepack then refuses to run bare `pnpm` from any OTHER script since it no longer matches the declared package manager — silently breaking `npm run dev` and effectively every local pnpm-invoking script. Every script (and `apps/web/playwright.config.ts`'s `webServer.command`) now uses `npx --yes pnpm@11.21.0` instead of bare `pnpm`, matching the pattern the `build` script already used. `packageManager` itself is untouched — the Hostinger deploy path is unaffected.

Ten POS commits landed, each independently verified against real PostgreSQL (and, where a UI was involved, `typecheck:web`/`lint:web`/`verify:routes`/`verify:route-security`/`verify:architecture`) — not just typecheck/lint:

1. `d0d9530d` — the F279 discount-approval forgery fix + the first store-access slice (see Phase 1/Phase 2 narrative above for full detail).
2. `e7a7b564` — closed Phase 2's initially-disclosed store-access gaps (terminal creation, shift open/close, legacy sale path, full return chain, product search/barcode).
3. `50169c04` — Phase 3 (confirmed and fixed the exact reported `updated_by=$3` bug in `features/promotions.js`/`features/coupons.js` — `$3` was the record's own id, not the actor; audited the rest of the module for the same class of mistake and found no other instance), Phase 4 (promotions never enforced `usage_limit_per_customer` at all, coupons only checked it at preview — both now re-checked under the record's own row lock at commit time, proven race-free with genuine two-Postgres-connection concurrency tests for both), a richer discount-approval title (amount/percent/reason) so the platform's existing generic `/approvals` inbox is a real usable supervisor UX without a POS-specific screen, and F276 (real customer search-select via new `searchPointOfSaleCustomers`, replacing the raw "Customer ID" field).
4. `dbad4254` — F268-F271 store/terminal/cashier administration: `updatePosStore`/`setPosStoreActive`/`updatePosTerminal`/`setPosTerminalStatus` (with unsafe-transition guards), `features/cashier-access.js` (`listPosEligibleCashiers` deriving real eligibility from the platform's own membership/role-assignment tables, `grantPosStoreAccess`/`revokePosStoreAccess`), and three new admin screens (`/pos/stores`, `/pos/terminals`, `/pos/cashiers`) — also fixed a latent response-shape bug in `createPosStore`'s client wrapper (`{record}` vs the route's actual `{store}`) caught while building its first real caller.
5. `866df105` — F280/F281 promotion/coupon admin screens (`/pos/promotions`, `/pos/coupons`) against the already-complete session-2 backend; added the missing `updatePosPromotion`/`updatePosCoupon` client methods.
6. `0df51949` — F287/F288 held-cart operator workflow: `listHeldPosCarts`, a real 24h lazy expiry for held carts (previously none at all), `resumePosCart`'s terminal-conflict guard, a "Held sales" dialog in checkout, and the `startFreshCart()` fix for a real pre-existing "New sale doesn't actually start a new sale" bug.
7. `d14b7f5d` — F289 receipts: `getPosSaleReceipt` + `/pos/receipts/[saleId]` screen, linked from the checkout confirmation screen.
8. `4c22ef6f` — F291/F292 returns/refunds UI: `findPosSaleForReturn` + `/pos/returns` screen (search/request/approve/complete).
9. `69de89e6` — F300 cash movements (`recordPosCashMovement`/`listPosCashMovements`, wired into the `/pos` overview screen) and F302 shift-close unresolved-transaction guards (active cart, pending/approved return — a held cart is deliberately exempt).
10. `dda2d36a` — F293 exchanges: `completePosExchange` as linked lineage over the existing return/sale primitives (no duplicated stock/cash/payment logic), a real permission bug (missing `pos.return.approve` requirement) caught by the test suite itself before it shipped, and an "Exchange" action wired from Returns into checkout.

**Verification run repeatedly through the session** (every gate run directly, not through the composite `pnpm verify`/`verify:erp`, which still fails at its first step, `verify:toolchain`, purely because this environment runs a newer Node than the repo's `>=24 <25` pin — the exact same pre-existing, undisclosed-by-this-session environment characteristic every prior session already documented, not something these changes caused or should paper over): `typecheck:web`, `lint:web` (clean after each batch, including fixing every real warning/error introduced along the way — unescaped JSX entities, `useMemo` dependency arrays, unused imports, and replacing `Record<string, any>` receipt/return types with concrete field types per the task's own explicit instruction not to reintroduce the permissive-typing class of bug that hid the session-2 snake_case/camelCase defect), `verify:architecture`, `verify:db`, `verify:routes` (grew from 172 to 185 `route.ts`/84 `page.tsx` files), `verify:route-security` (grew from 140 to 147 mutation-capable routes, 0 unexplained gaps throughout), `test:api` (1112/1112 unchanged), full `tests/integration/*.test.mjs` (grew from 21 to 64 assertions across the suite, all POS-specific additions passing, the 2 pre-existing restricted-role-only skips unchanged and unrelated). Six new dedicated POS real-Postgres suites were added this session on top of the session-2 F277-F281 suite (which itself grew from 11 to 15 assertions): `pos-store-access-f268-f273.test.mjs` (11), `pos-store-terminal-cashier-admin.test.mjs` (6), `pos-audit-integrity-and-usage-concurrency.test.mjs` (7, including two genuine two-Postgres-connection race tests), `pos-hold-resume-f287-f288.test.mjs` (5), `pos-cash-movements-and-shift-close-f299-f302.test.mjs` (4), `pos-exchange-f293.test.mjs` (6).

**Genuinely blocked, disclosed rather than silently skipped**: F295 (lot/serial requirement validation) cannot be built as a POS-owned check without either inventing POS-owned lot/serial truth (explicitly forbidden) or Stock's own data model first gaining a per-item tracking-type column and a real serial-status lifecycle (confirmed absent from `services/api/src/modules/stock/index.js` and the `items`/`stock_serials` schema during this session's investigation) — this is a real cross-module dependency for a future Stock-module pass, not a testing oversight. F283-F286 (payment providers), F297/F298 (offline), F303-F305 (Z-report/reconciliation/accounting posting), F306 (loyalty), F307 (real analytics) are deliberately out of scope for this pass per the task's own explicit deferral instruction.

**Not built this session** (disclosed): item/item-group/customer ID-array eligibility pickers for promotions/coupons (API-only for those specific fields — the admin screens cover every other field). True terminal-level cashier eligibility (only store-level exists). `pos_payments`/`pos_cash_movements`/`pos_reconciliations` row-filtering by store access in `listPointOfSaleResource`. Per-store promotion/coupon usage limits (`usage_limit_per_store` — migration 113 only defines `usage_limit_total`/`usage_limit_per_customer`; this is a genuine data-model gap, not a test gap, disclosed rather than built under time pressure).

### Session 3 continued — verification gate + 26-item security/concurrency matrix

A full verification-gate battery and an independent 26-item negative security/concurrency matrix were run against the state above (both via background agents, each independently re-verified by re-running their claims directly rather than taking the reports on faith).

**Verification gate battery — zero failures.** `verify:architecture`, `verify:db` (116 tenant + 40 platform migrations), `verify:routes` (185 route.ts/84 page.tsx), `verify:route-security` (147 mutation routes, 0 unexplained gaps), `typecheck:web`, `lint:web` all clean. `test:web` 21/21, `test:api` 1112/1112, `test:sdk` 14/14, `test:packages` all-green across 10 packages, `test:security` 4/4, `test:enterprise-rbac` 5/5, `test:worker` 102/102, `permissions` tests 8/8, `build:web` clean (147 dynamic + 2 static routes, all `/pos/*` pages and `/api/pos/*` routes present). `tests/integration/*.test.mjs` 62/64 pass, 2 skipped (pre-existing, unrelated: the CRM-only restricted-runtime-role RLS tests, which need a separately-provisioned `DATABASE_URL` role this environment doesn't have configured). `verify:toolchain` still fails for the same pre-existing Node v26.5.0-vs-v24-pin reason every prior session documented — not something this session's changes touch. `verify:experience` (optional) reports the same 3 pre-existing CRM-only raw-`<table>` violations, zero POS-related.

**26-item security/concurrency matrix — full results** (COVERED / PARTIALLY COVERED / NOT COVERED against the code as it stood before this remediation):
- Originally COVERED with existing tests: #3 (cross-store), #4 (cross-terminal), #5 (shift-open-as-someone-else), #8 (forged approvedBy), #9 (self-approval), #10 (stale discount approval), #11 (stale cart version), #17 (per-customer coupon race), #22 (duplicate exchange), #24 (shift-close unresolved-transaction), #26 (audit-field forgery).
- Originally NOT COVERED or PARTIALLY COVERED, now closed this pass:
  - **#13 (forged/tampered discount amount, legacy path)** — genuine bug, fixed (see F279 row above) and regression-tested.
  - **#23 (duplicate/concurrent cash movement)** — genuine bug (no idempotency at all), fixed (see F300 row above) and regression-tested.
  - **#6/#7 (revoked/inactive cashier, inactive/unauthorized supervisor)** — the audit read `services/api/src/core/approvals.js`'s lack of a local membership/status check as a possible architecture gap. Traced the real call path (`requireWorkspace()` → `resolveSessionContext()` in `services/api/src/core/session.js`) and confirmed every session is resolved fresh from the database on every single request, already requiring an active user (`app_user.status='active'`) and filtering role assignments to active-and-in-window — a deactivated user or a revoked/expired role assignment cannot produce a session at all, so `decideApproval` never sees them. No code gap; added `tests/integration/session-revocation-inactive-approver.test.mjs` to prove this against real Postgres rather than leave it asserted-but-untested.
  - **#1/#2 (cross-organization, cross-company access)**, **#14 (cross-company item/variant)**, **#16 (expired coupon)**, **#25 (cancelled-cart / closed-shift mutation)** — all guards already existed and were already correct; no code gap. Added `tests/integration/pos-cross-tenant-and-lifecycle-security.test.mjs` (8/8) proving each one against real Postgres.
  - **#12 (unit-price forgery, cart path)**, **#19 (concurrent last-unit stock race)**, **#20 (duplicate checkout idempotency retry)**, **#21 (duplicate return idempotency retry)** — all mechanisms already existed and were already correct; no code gap. Added `tests/integration/pos-idempotency-and-price-integrity.test.mjs` (6/6) proving each one against real Postgres, including a genuine two-Postgres-connection stock race for #19.
- **Confirmed genuine, out-of-scope data-model gaps** (not fixable by a test or a small patch): **#15** (no `requires_serial`/`requires_batch` column on `tenant.items`, no serial-status lifecycle writes anywhere in Stock — same root cause as F295 above) and the per-store half of **#18** (no `usage_limit_per_store` column exists; the per-customer half of #18 was already covered and race-tested in session 3's earlier F280/F281 work).

Net result: of the 26 items, 21 are now COVERED with a real-Postgres regression test (11 pre-existing + 10 added/fixed this pass), 2 are confirmed genuine data-model gaps disclosed for a future Stock/promotions pass (#15, #18-per-store), and the remaining items resolved to "no code gap, coverage added" rather than staying open. Five new test files, two production-code fixes (items #13 and #23), zero production-code regressions (every existing suite still green after each change).

### Session 3 continued — permanent Playwright E2E suite

Built and independently re-verified: `apps/web/e2e/pos-{authorization,checkout-safety,checkout,discount-approval,hold-resume,returns}.spec.ts` plus `pos-fixtures.ts` (three genuine, separately-authenticated `pos_cashier`/`pos_supervisor`/`pos_manager` personas with real scrypt-hashed passwords and a real login flow, dedicated stores/terminals/shifts, one deliberately-ungranted store) and `pos-global-teardown.ts` (deletes every seeded row after the run; deactivates rather than hard-deletes the persona users, since `public.audit_events` is DB-enforced immutable). 11/11 passing against the real dev server and real Postgres, both desktop and tablet viewports, no `sleep()`/`waitForTimeout()` anywhere, every denied-action test verified directly against Postgres rather than the UI's error text alone. Full detail, including the exact journey list, in `POS_SESSION3_VERIFICATION_REPORT.md` §5.

This work found two more genuine, non-security correctness bugs (fixed in `cd9fe19f`) that no unit or mocked test had ever been able to see: (1) cart-line descriptions were computed correctly by `reprice()` but never persisted onto `tenant.pos_cart_lines`, so every cart showed a blank line description for its entire lifetime up to sale completion; (2) React StrictMode's dev-mode double-invoke could fire two concurrent `createPosCart()` calls from `PosCheckoutScreen`'s mount effect, orphaning one of the two resulting carts — fixed with a `useRef` guard keyed by shift id. Fixing bug #1 also surfaced a real gap in this session's OWN verification discipline: two mocked-client unit tests (`point-of-sale-stock-integrity.test.mjs`, `point-of-sale-sale-validation.test.mjs`) had a hand-written query router matching `pos_settings`' SELECT by exact column list, which the earlier item #13 fix had already changed without anyone re-running the full mocked `test:api` suite to notice — caught and fixed only once that full suite was run again before declaring this pass done (`test:api` is 1112/1112 again).

### Session 4 — F290/F304/F305/F307 final functional closure

Starting SHA `f1967b83` (F306 loyalty merge, already on `rebuild/clean-frontend`). This session closed the four remaining major backend-missing features per the owner's own "final major pass" plan (item 7 below, from the prior session's own next-tranche note), plus one smaller disclosed gap.

**Research first**: a dedicated read-only research pass across Sales/Accounting confirmed the exact reusable contracts before writing any code — `tenant.accounting_customer_invoices` (Accounting's own generic AR invoice/credit-note document, already the target of Sales' own dead-and-unwired `sales_invoice_requests` → `createInvoiceFromSalesRequest` bridge) for F290; `createJournalEntry`/`postJournalEntry` + `getAccountMapping` (the exact pattern `postVendorBill`/`postCustomerInvoice` already use) for F305; `tenant.pos_day_end_reports`' own F304-reserved `reconciliation_status`/`reconciliation_references`/`outstanding_exceptions` columns (migration 121, never populated until now) for F304; and confirmed F307's entire prior surface was a 33-line, today-only, 4-metric stub.

**F290 Invoice generation** (`transaction-continuity-and-documents/invoices.js`): on-demand (not automatic for every sale — a receipt remains the default document), requires a real customer, idempotent per sale via `pos_sales.accounting_invoice_id` (a plain, unenforced column since migration 048 — now a real FK, migration 126). Reconciles its own line construction against the sale header before ever calling Accounting, failing loud (`POS_INVOICE_RECONCILIATION_MISMATCH`) rather than silently posting a mismatched invoice. A genuine, disclosed architecture extension was needed and made: `submitSubledgerDocument`/`postCustomerInvoice` (Accounting) had no `options.internal` bypass the way `createJournalEntry`/`postJournalEntry`/`createCustomerInvoice` already did — added it, exactly mirroring the existing precedent, so a trusted system caller (a POS invoice always trails an already-completed, already-fully-paid till transaction, with no real commercial risk left for Accounting's interactive human maker-checker workflow to catch) can move an invoice from draft to posted without requiring the acting POS user to hold Accounting-module permissions. A `posAccountingContext()` helper (`shared/accounting-bridge.js`) carries the narrow `accounting.view`/`accounting.receivables.manage` permissions the still-permission-gated nested reads (`getCustomerInvoice`) need, scoped to one server-side call chain, never derived from end-user input.

**F304 Payment reconciliation** (`cash-shift-day-end-and-reconciliation/reconciliation.js`, migration 127): `tenant.pos_reconciliations` (unused since migration 048) re-keyed from `(shift_id, payment_method)` to `(day_end_report_id, payment_method)` since a business-day report can span multiple shifts. New `pos_settlement_batches`/`pos_settlement_entries` hold real settlement evidence — imported (never fabricated) from a provider statement/file or the sandbox adapter's own test-settlement generator, since this environment has no live merchant-certified provider credential (the same disclosed limitation `tender-and-payment-execution/adapter.js` already carries). `pos_payments` gained `settlement_status`/`settled_amount`/`settlement_fee_amount`/`settled_at` — the first place in the codebase distinguishing `captured` (money changed hands at the terminal) from `settled` (the provider's own settlement batch actually paid it out). Cash needs no settlement evidence at all — it reconciles directly against F303's own already-computed counted-vs-expected cash total, avoiding a duplicate calculation. A resolved reconciliation is immutable (`pos_reconciliation_protect_resolved()` trigger, mirroring migration 121's day-end pattern exactly) with an append-only `pos_reconciliation_corrections` table for anything discovered after the fact. Genuine maker-checker: `pos.reconciliation.manage` (import/match/generate) is a blocking SoD conflict against `pos.reconciliation.approve` (resolve a variance), and `resolvePosReconciliation` additionally blocks the SAME PERSON who generated the reconciliation from resolving it, even if one custom role somehow held both permissions.

**F305 POS accounting posting** (`cash-shift-day-end-and-reconciliation/accounting-posting.js`, migration 126): posts a real, balanced journal per completed sale/return through Accounting's own `createJournalEntry`/`postJournalEntry` — never a POS-owned ledger. Revenue/output-tax/rounding/COGS reuse the exact `revenue`/`output_tax`/`rounding`/`cogs` mapping keys Sales' own invoices already post through (`foundation.js`'s `MAPPING_SEED`, pre-provisioned for every company — zero config). Cash/bank tender lines reuse the pre-seeded `cash`/`bank` keys; card/UPI/wallet/store-credit tender-clearing, inventory relief and the two loyalty-accrual accounts are genuinely new POS-specific mapping keys needing one-time admin configuration through Accounting's existing generic mapping API (no new mapping-configuration UI was built — reusing Accounting's own surface, not duplicating it, per the task's explicit "no functionality outside canonical scope" instruction). COGS/inventory relief is computed from `stock_movements.unit_cost` — real, sourced data, never invented — and is honestly disclosed as the one area genuinely new to this entire codebase (no other module posts COGS/inventory-relief journal entries anywhere yet). A F306 loyalty deferred-revenue accrual (earn: debit a program-expense account / credit a loyalty-liability account at the program's current redemption value; redeem: debit the liability / credit revenue, restoring gross revenue recognition while relieving cash collected via the discount) rides in the same sale-posting journal when applicable. A posting failure is caught inside a `SAVEPOINT` (mirroring `createInvoiceFromSalesRequest`'s own established pattern) and recorded as `accounting_posting_status='failed'` with the real error message on the sale/return row itself — durably visible and retryable at `/pos/accounting`, never silently lost with a rolled-back transaction. `postPosDayEndReportToAccounting` posts a closed report's remaining sales/returns best-effort, mirroring `procurement-accounting-vendor-bill.js`'s own documented "a clean match is durable truth whether or not the downstream posting succeeds immediately" precedent. Idempotent per sale/return via a new `journal_entry_id` column.

**F307 POS sales analytics** (`pos-analytics/reports.js`): replaces the 33-line, 4-metric, today-only dashboard stub with a real date-range (required, bounded to 366 days — no unbounded scan)/store/terminal/cashier-filtered report covering every metric the brief named: gross/net sales, transaction count, average order value, store/terminal/cashier/product-category breakdowns, tender split, promotion/coupon/other-discount split (sourced from `pos_promotion_applications`/`pos_coupon_redemptions`, never invented or duplicated from elsewhere), returns/refunds, cash variance (reused directly from F303's own totals), reconciliation exceptions (F304), offline-sync exceptions (F297/298), accounting-posting status (F305), loyalty activity (F306), and margin (only over lines with a real linked `stock_movement` — a non-tracked item is excluded, never assumed to have zero cost). Row-scoped by `accessiblePosStoreIds`, same boundary every other POS read already enforces — "enforce permissions on aggregate data as well as individual transactions" was a design constraint from the start, not bolted on.

**Smaller gap closed**: `pos_payments`/`pos_reconciliations` now have real `store_id` columns (from F283-286/F304's own migrations) — `listPointOfSaleResource`'s row-filtering, disclosed as a gap since session 3 ("no store_id column of their own"), is wired up for both; `pos_cash_movements` (still genuinely columnless for store_id) is filtered via a `shift_id` subquery instead of a table-level column, closing the entire disclosed gap without adding a new column.

**Verification**: `test:api` 1112/1112, `test:web` 21/21, `test:sdk` 14/14, `test:packages` all-green (11 packages incl. `permissions` 8/8), `test:security` 4/4, `test:enterprise-rbac` 5/5, `test:worker` 102/102, the full real-Postgres `tests/integration/*.test.mjs` 146/146 (up from 136 — the 10 new F290/F304/F305/F307 assertions in `pos-invoice-reconciliation-accounting-analytics-f290-f304-f305-f307.test.mjs`, exercising invoice idempotency, a balanced-journal assertion, a real card payment matched against real settlement evidence, a genuine cash variance investigated and resolved by a different approver than who generated it, and analytics totals reconciling exactly against the fixture's own known sales/tender/COGS figures), `typecheck:web`, `lint:web` (0 warnings), `build:web` (4 new pages: `/pos/invoices`, `/pos/reconciliation`, `/pos/accounting`, `/pos/analytics`), `verify:architecture`, `verify:db` (127 tenant + 45 platform migrations), `verify:routes` (219 route.ts/92 page.tsx), `verify:route-security` (170 mutation routes, 0 unexplained gaps).

**Not built this session** (disclosed): a dedicated Accounting account-mapping configuration UI (reused Accounting's existing generic API instead — a pre-existing, cross-module gap, not POS-specific); real merchant-certified payment-provider settlement API polling (no live credential exists in this environment — settlement evidence is imported, matching the sandbox-only adapter precedent already established for F283-286); loyalty-accrual reversal on a return's own journal (the return's revenue/tax/tender/COGS reversal is real; a disclosed simplification skips separately reversing the original sale's loyalty accrual). See `POS_FINAL_FUNCTIONAL_VERIFICATION.md` and `POS_VISUAL_QA_HANDOFF.md` for full feature-by-feature evidence and the next session's actual scope.

### Session 5 — final gap closure + comprehensive visual/accessibility QA

Starting SHA `72ca8be5` (Session 4's F290/F304/F305/F307 merge). Two parts: (1) closed the four gaps Session 4 disclosed as open (items 3/4/6 below, plus non-cash refunds); (2) a comprehensive real-browser visual, responsive, interaction and accessibility QA pass across every implemented POS screen. Full detail for both in `POS_FINAL_FUNCTIONAL_VERIFICATION.md` ("Four disclosed gaps closed" + verification-run evidence) and `POS_FINAL_VISUAL_QA_REPORT.md` (screen inventory, viewport coverage, bugs found and fixed, accessibility results, final F268–F307 status table).

Gap closure summary (real backend + UI + DB + test evidence for each, not UI hiding): terminal-level cashier eligibility (migration 128, `assertPosStoreAccess`/`accessiblePosTerminalIds`, 10/10 new tests); item/item-group/customer eligibility search-pickers on promotion/coupon admin screens (`EligibilitySearchPicker`, reusing existing search functions plus one new minimal `searchPointOfSaleItemGroups`); an Accounting account-mapping configuration section on `/pos/accounting` (reuses Accounting's own real API, no POS-owned mapping table); non-cash refunds (`completePointOfSaleReturn` now allocates across every real tender leg via the existing `refundPosPayment`, 5/5 new tests).

Visual QA pass: `apps/web/e2e/pos-visual-qa.spec.ts` (screenshot capture across 6 viewports/19 screen-state combinations) and `apps/web/e2e/pos-accessibility.spec.ts` (axe-core, 16/16, 0 critical/serious violations) — both new, both run against the real dev server and real PostgreSQL. Found and fixed three genuine bugs by actually inspecting the resulting screenshots (not assuming correctness from a passing build): a checkout cold-load race showing a false "no open shift" state, a raw-ISO-datetime display bug on both day-end report screens, and a promotion discount stored/receipted 1,000,000× too large (`asDatabaseDecimal()` omitted at one call site — the same bug class as a prior session's BigInt-into-JSON crash, in a codepath that fix didn't cover). All three fixed with regression coverage; full battery re-run green afterward with no baseline regression.

### Next tranche to pick up

1. Close F295's real blocker (also #15 above): add a tracking-type column to `tenant.items` and a real serial-status lifecycle to Stock's own `postStockMovement`/`stock_serials` handling (Stock-module work, not POS-module work, even though POS is the consumer that needs it). *Superseded — F295 lot/serial tracking landed in `5ecbdda5`, prior to Session 4; verify still current before assuming complete.*
2. ~~Add `usage_limit_per_store` to `tenant.pos_promotions`/`tenant.pos_coupons`~~ — landed in `86d110b1`, prior to Session 4.
3. ~~Terminal-level cashier eligibility~~ — closed Session 5 (migration 128).
4. ~~Item/item-group/customer eligibility pickers for the promotion/coupon admin screens~~ — closed Session 5 (`EligibilitySearchPicker`).
5. ~~`pos_payments`/`pos_cash_movements`/`pos_reconciliations` row-filtering in `listPointOfSaleResource`~~ — closed Session 4.
6. ~~A dedicated Accounting account-mapping configuration screen~~ — closed Session 5.
7. ~~Comprehensive visual/responsive/accessibility QA pass~~ — closed Session 5 (`POS_FINAL_VISUAL_QA_REPORT.md`).
8. Two small residual gaps remain, both disclosed and non-blocking (see `POS_FINAL_FUNCTIONAL_VERIFICATION.md`'s "Remaining functional gaps"): loyalty-accrual reversal on a return's own accounting journal; a return split across multiple different non-cash tenders has its accounting attribution reconstructed via proportional allocation rather than an exact stored record.
9. **If genuinely needed beyond this**: performance/load hardening, a full atomic F268-F307 requirement-by-requirement audit against `docs/02-register/SUBREQUIREMENT_REGISTER.csv`, and real merchant payment-provider certification (external activation blocker, not code).

### Session 6 — "POS Completion Program, Prompt 1" — ground-truth re-audit + the four explicitly-flagged gaps closed

Starting SHA `64e0628c9ecad4f984948fdf057f83e739224402` (`main`, confirmed exactly matching the reference SHA given at the start of this session and in sync with `origin/main` — no reconciliation needed, no divergence).

**Critical correction to this program's own premise, established by re-reading actual code and `git log` before writing anything**: the "POS Completion Program" brief that opened this session assumed Groups A-E (F268-F271, F272-F276, F277-F281, F287-F290, F299-F302) were largely unbuilt, with payment providers/offline explicitly deferred to Prompts 2-3. Actual `git log main -- services/api/src/modules/point-of-sale/` (31 commits) and this very tracker (Sessions 1-5 above) show the OPPOSITE: F268-F293, F295, F297-F307 are already REAL/VERIFIED or PARTIAL-with-disclosed-gaps, including the payment-provider adapter (F283-F286) and offline sync (F297/F298) the brief assumed were still Prompt-2/3-scope. This is not a discrepancy this session should paper over — the brief's own SHA reference matched exactly, so this is simply a stale premise, and the tracker/gap-matrix above (not the brief) is the ground truth per the brief's own explicit instruction ("actual code, runtime behavior and reproducible tests take precedence over completion claims").

Given that, this session's actual, valuable scope was: (1) verify the four SPECIFIC gaps the brief named by number and investigate as still-possibly-open (F275 customer pricing, F289 print evidence, F301 shift idempotency, F302 decimal arithmetic) against current code — all four were confirmed genuinely still open by direct code inspection; (2) fix all four with real DB/backend/test evidence; (3) fix one directly-related cross-cutting concurrency defect surfaced while testing F289 (see below); (4) build the durable evidence artifacts this program requires (atomic register + validator, architecture-alignment note, handoff).

**F275 (price lists) fixed**: see the gap-matrix row above. `applyCustomerPricingRules` (`cart-pricing.js`) added, reusing `tenant.sales_pricing_rules` (Sales' own table) with the identical adjustment semantics `previewSalesDocument` already applies. New dedicated suite `tests/integration/pos-customer-pricing-f275.test.mjs` (7/7): a genuine, non-trivial test-authoring bug was found and fixed along the way — the first draft asserted against `cart.lines[0]` across six sub-tests sharing one terminal, not realizing `createPosCart` returns/reuses a terminal's existing active cart rather than always creating an isolated one, so lines accumulated across sub-tests and produced a false positive AND a false negative before each cart was explicitly cancelled between sub-tests. The legacy flat-lines path's own equivalent gap (still store-list-only, still raw `Number` arithmetic) was investigated and deliberately NOT fixed this pass — disclosed, not silently skipped; see the Remaining-work register.

**F289 (receipt printing) fixed**: see the gap-matrix row above. New tenant migration `129_pos_receipt_print_events.sql` (immutable evidence table, RLS-enforced, matching every other POS evidence table's convention exactly). A genuine, if minor, pre-existing security/integrity issue was fixed as a side effect: the Original/Reprint badge was previously 100% driven by a client-controlled `?original=1` URL query parameter — anyone could paste that into the address bar and see "Original" regardless of real print history. It is now derived entirely from server-recorded evidence.

**F301 (shift opening) fixed**: see the gap-matrix row above. Required updating every call site to pass a real `idempotencyKey` (28 direct domain-function calls across 5 test files, 2 e2e-fixture calls, 1 HTTP route Zod schema, 1 frontend mutation) — the full existing 254-test real-Postgres suite re-passing unchanged after this sweep is the regression evidence that nothing was missed.

**F302 (shift closing) fixed**: see the gap-matrix row above. Straightforward decimal-utility substitution; existing suite re-passing with exact values is the regression evidence.

**Cross-cutting fix (directly surfaced by F289 testing, in scope per the brief's own "fix cross-module problems directly required by these features" instruction)**: `services/api/src/modules/accounting/receivables.js`'s `getCustomerInvoice` — a function POS's F290 invoice-generation path calls — had a six-way `Promise.all([client.query(...), ...])`, the same "concurrent queries on one pg client" deprecation-warning class already fixed elsewhere in this codebase this session (outside POS scope, in the shared platform layer). Confirmed via a live real-Postgres test run that this line was ACTUALLY firing the deprecation warning in a POS code path; fixed to sequential awaits; re-verified warning-free and 10/10 still passing.

**Verification this session**: `test:api` 1112/1112 (mocked-client suite, unchanged), the full real-Postgres `tests/integration/*.test.mjs` 254/254 (up from 247 baseline this session started with — +7 new F275 assertions, 0 regressions across every other suite including F283-F307), `typecheck:web`/`lint:web`/`build:web` all clean, `verify:db` (129 tenant migrations), `verify:routes` (243 route.ts/100 page.tsx), `verify:route-security` (191 mutation routes, 0 unexplained gaps — the new print-attempt route required and received the standard `mutation: true` billing-write gate), `verify:architecture` clean.

**A live-browser Playwright run of `pos-checkout.spec.ts` could not be brought to a clean pass this session** (see the Regression report in the handoff for the full diagnostic trail) — traced, via a rigorous A/B test (same spec, same clean server, cart-pricing.js reverted to its unmodified baseline vs. restored), to a PRE-EXISTING flake unrelated to any change made this session: the identical "add-to-cart line stuck loading" failure reproduces on completely unmodified code. A separate, also-encountered `.next` build-manifest corruption (from this session's own earlier partial cache deletion, unrelated to POS source changes) was found and fixed (full `.next` removal + clean restart resolved a transient 404 on `/api/auth/*` routes). The underlying pre-existing flake was not root-caused or fixed this session — disclosed as a known environment/test-infrastructure gap for the next session, not silently ignored. `pos-checkout.spec.ts` was updated to assert the new F289 server-derived print-evidence behavior regardless (replacing the now-removed `?original=1` assertion), so the spec is correct and ready to pass once the flake itself is diagnosed.

**Deliberately not attempted this session** (given the corrected premise above, most of Groups A-E were already done by prior sessions): a full hand-verified atomic register covering all 1,480 F268-F307 subrequirement rows. See `POS_ATOMIC_ACCEPTANCE_REGISTER.csv`/`POS_ACCEPTANCE_OVERRIDES.json`/`validate-pos-acceptance-register.mjs` and their own accompanying notes for exactly what was and wasn't verified at atomic granularity, and why bulk-marking rows PASS from feature-level narrative alone was deliberately refused as inconsistent with this program's own explicit anti-fraud rules ("Never infer PASS from source-file existence").

Ending SHA: see `POS_COMPLETION_PROMPT1_HANDOFF.md` for the exact commit(s) this session produced.

### Session 7 — "POS Completion Program, Prompt 2" — financial-integrity fixes + the E2E "flake" properly root-caused

Starting SHA `6e75b9197c70dfb32222688030a02d14b292a9cf` (Session 6's own ending SHA). Full detail in `POS_COMPLETION_PROMPT2_HANDOFF.md`; summarized here because it directly corrects a claim Session 6 recorded above.

**Financial-integrity Gaps A/B/C closed** (`services/api/src/modules/point-of-sale/cash-shift-day-end-and-reconciliation/accounting-posting.js`, commit `e89dd459`): a return's journal now reverses the original sale's loyalty accrual (previously not built at all); refund tender allocation now reads the exact per-payment split `return-lifecycle.js` persists at refund time (`tenant.pos_return_payment_refunds`, migration 130) instead of reconstructing a proportional guess; loyalty accrual is now valued at the sale's own historical rate snapshot (`pos_sales.loyalty_redemption_value_per_point_snapshot`), never the program's current live rate. Proven by `tests/integration/pos-financial-integrity-gaps-abc.test.mjs` (8/8), including the two scenarios the old code specifically got wrong.

**Correction to Session 6's own claim above**: "the underlying pre-existing flake was not root-caused or fixed this session" is no longer accurate as a standing description of `pos-checkout.spec.ts` — Session 7 investigated it properly (network capture, direct DB inspection, Playwright page snapshots at the exact moment of failure) rather than accepting "flake" as a final answer, and found **six separate, concrete, fixable defects, none of them actually a flake**: (1) stale UI-copy locators ("Cash tendered"/"Complete cash sale" vs. the real "Amount"/"Complete sale") across five spec files; (2) real, previously-undetected test-data pollution — `pos-visual-qa.spec.ts` seeds a promotion/coupon into the shared, persistent `CRM E2E Fixture Org` for its own screenshots and never deactivated them, silently corrupting every other spec's hardcoded expected totals the moment visual QA ever ran once (four already-leaked promotions/coupons from past sessions were found still active and deactivated; the seeding spec now tracks and deactivates its own in a new `afterAll`); (3) an over-broad console-error assertion flagging Chrome's own routine 404 logging for an intentionally-tolerated invoice-lookup 404; (4) an over-broad `role="alert"` assertion colliding with the app's own always-mounted, empty accessibility live region; (5) ambiguous bare-status-text locators in the shared fixture org's now-populated returns list; (6) a real regression from Session 6's own F301 idempotency fix breaking an unrelated authorization test, plus Session 6's F289 table and this session's Gap B table breaking the E2E teardown script's own cleanup (FK order + an immutability-trigger bypass needed for administrative test cleanup). Commit `60221917`.

**Verified**: the full core POS E2E batch (`pos-checkout`, `pos-checkout-safety`, `pos-hold-resume`, `pos-returns`, `pos-authorization`, `pos-discount-approval`) passes together in one run — 11/11. `pos-visual-qa.spec.ts` — 7/7, with its own newly-seeded promotion/coupon confirmed `inactive` afterward. Full regression: `test:api` 1112/1112, real-Postgres integration suite 262/262 (up from 254), `typecheck:web`/`lint:web` clean.

**Verification-only findings** (no code change needed, confirmed correct by direct inspection): payments (F282-F286) — sandbox-only adapter architecture is genuine and fail-closed for any real provider, real HMAC webhook verification, no PAN/CVV storage anywhere in the schema; stock (F294-F296) and offline (F297-F298) already correctly built with their own passing real-Postgres suites from prior sessions; a systematic grep across every Prompt-2-owned capability directory found no other self-disclosed "NOT BUILT"/TODO/FIXME gap beyond the three already fixed.

**Atomic register**: 8 PASS / 1 PARTIAL / 1,471 UNVERIFIED (was 4/1/1475 after Session 6) — see `POS_COMPLETION_PROMPT2_HANDOFF.md` §9 for the exact remaining-work list assigned to Prompt 3.

Ending SHA: `43b37c6c` (pushed to `origin/main`).


## Session 7 — design-standard convergence with CRM + gap closure (2026-09-19)

**Trigger**: POS screens were "not maintaining the design standards" of CRM, and several documented workflows had no usable UI. The earlier `POS_FINAL_VISUAL_QA_REPORT.md` called the module visually clean; a fresh, real-browser pass found otherwise (details below), so treat that report as superseded for layout/consistency.

### Design convergence (apps/web/src/features/pos)

Root cause was systemic, not per-screen: ~13 screens hand-rolled their headers and stat boxes, and *every* POS panel used `radius-panel` / `border-strong` / `p-5` / `text-base` headings where CRM uses `radius-card` / `border` / `p-4` / `text-sm` headings.

- New shared, presentation-only building blocks in `shared/PosUi.tsx` (`PosPanel`, `PosAlert`, `PosFacts`, `PosDataTable`, `PosBackLink`, `PosLoading`) composed from design-system primitives; `shared/PosCustomerPicker.tsx` (customer search-select); `shared/format.ts` gained `dateTime`, `statusTone`, `statusLabel` (one status vocabulary — Title-case everywhere).
- Rebuilt on `PageHeader` / `MetricStrip` / `EnterpriseListPage` / `RecordHeader`: Overview, Analytics (tabs instead of a 14-section scroll), Invoices, Reports hub, Reconciliation, Accounting posting, Shift detail, Z-report detail, Customers, Loyalty, Cash movement, Receipt, Transaction detail, Checkout. Toolbars (search + filters + active-filter chips) added to Stores, Terminals, Cashiers, Returns, Offline conflicts, Promotions/Coupons.
- `PosDataTable` is built on `EnterpriseDataGrid`; POS now contains **zero raw `<table>`** (`verify-experience` lists only 3 pre-existing CRM files).
- **Design-system bug fixed at source** (`EnterpriseDataGrid`): sortable column headers lost their `uppercase` (Tailwind preflight resets `text-transform` on `<button>`), so headers rendered in mixed case. Affects every grid, CRM included.

### Functional gaps found and closed

| Gap | What was wrong | Fix | Evidence |
|---|---|---|---|
| F279 discount approval had no usable UI | Above-threshold discounts blocked completion, but managers could not *find* the request (generic `/approvals` inbox never lists it for a `pos.discount.approve` holder), and the cashier saw no pending state | `listPosDiscountApprovals` + `GET /api/pos/discount-approvals`; `/pos/discount-approvals` queue; cart payload carries current-version `discountApprovals`; checkout shows waiting/approved/rejected and polls | `pos-cart-tax-promotions-coupons-f277-f281.test.mjs` (extended) |
| F279 line/cart discounts | `applyPosLineDiscount` existed in the API layer but no checkout control used it; cart discount was percent-only | Line + cart discount dialog (percent/amount, reason), remove controls | typecheck/lint + browser (see below) |
| POS policy unreachable | `pos_settings` (discount limits/threshold, return rules, negative stock, price override, cart expiry, shift reconciliation) had no API or UI — fixed at migration defaults for every tenant | `getPosSettings`/`updatePosSettings` (validated, cross-field rule, audit event), `GET/PUT /api/pos/settings`, `/pos/settings` | `pos-settings-and-payment-config.test.mjs` |
| Stores could only take cash | A store needs `allowed_payment_methods` **and** an active provider row for card/UPI; neither could be created | `getPosStorePaymentConfig`/`setPosStorePaymentConfig`, `PUT /api/pos/stores/[id]/payment-config`, store "Payment methods" dialog (registered providers only; credential field is an env-var *name*, values rejected) | same suite — proves a card payment succeeds end-to-end after configuring a previously cash-only store |
| F306 points expiry was configuration without effect | `points_expiry_days` was stored and shown; nothing expired points | `expirePosLoyaltyPoints` (FIFO-conservative, idempotent, ledger-only) + `POST /api/pos/loyalty/expire` + "Run points expiry" | `pos-loyalty-earn-redeem-reverse-f306.test.mjs` (new expiry test) |
| F304 import/correction | Import accepted one settlement entry; reconciliation detail + linked correction endpoints had no UI | Multi-entry import (+ paste-from-statement), detail dialog, correction action | typecheck/lint |
| F307 drilldown | Analytics exposed only a store filter though the API supports terminal/cashier | Store/terminal/cashier filters | typecheck/lint |
| Loyalty form saved wrong values | Form seeded 0.1/0.5 defaults instead of the saved program, so saving could silently overwrite real rates; customer lookup asked for a raw UUID | Form initialised from stored program; expiry field; customer search picker | typecheck/lint + screenshot |
| Terminals showed no store | Store names were resolved client-side from a store list capped at 100 rows | Server-side `store_name` join; admin lists request the 200-row ceiling and say so when hit | screenshot before/after |

### Verification run this session

- `test:api` 1112/1112; `test:web` 21/21; POS real-Postgres integration **186/186 across 22 suites** (includes the new/extended ones above).
- Browser E2E: **31/31** POS specs pass in a real browser (authorization, checkout desktop+tablet, checkout-safety, discount maker-checker, hold/resume, offline sync, returns, shifts/cash, transactions, and the axe accessibility sweep). Along the way the specs needed updating for pre-existing drift (refund confirmation dialog, receipt badge wording) and for my UI (dialog-based cash form, receipt heading); no product regression was found by them. A throwaway smoke test additionally exercised the new Settings save/persist, store payment-methods dialog and reconciliation import dialog, and caught two real bugs of mine (lost "saved" confirmation; "UPI" shown as "Upi"), both fixed.
- `typecheck` (src + e2e) and `eslint` clean for POS + shell. Gates: route-security (199 routes, 0 gaps), billing-mutation gate (0 unaccounted), architecture, route smoke — pass.
- Visual QA: all 22 POS routes re-shot at 1440px in a real browser as the owner and reviewed by eye; defects found were fixed. **Not re-shot this session**: tablet/mobile widths and print preview — the earlier 6-viewport sweep pre-dates these layouts.

### Honest limits — NOT done, do not read as complete

- **Atomic register**: 1,465+ of 1,480 rows remain `UNVERIFIED`. This session verified specific behaviours (see the table) and marked only the rows it could genuinely evidence; it did not re-audit the register.
- **No real payment gateway** exists; only the sandbox adapter. Card/UPI are code-complete against it only (F283/F284 external blocker unchanged).
- **F269 "peripheral configuration" and "session health"** (printer/scanner/cash-drawer settings, last-seen) have no data model or UI.
- **Points expiry runs on demand** (button/API); no scheduled worker calls it.
- Admin lists (stores/terminals) are capped at 200 rows with a visible notice, not paginated.
- CRM's own raw-table violations (Dashboard, Forecast, Import/Export) predate this work and were not touched.
