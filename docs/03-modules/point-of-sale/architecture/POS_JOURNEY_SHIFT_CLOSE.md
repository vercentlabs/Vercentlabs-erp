# Journey — Shift close / Z / reconciliation

Stop/complete in-flight transactions → compute expected cash/tender totals from immutable shift facts → cashier counts cash → supervisor variance policy/approval → close shift → generate reproducible Z report → reconcile provider settlements/cash → submit idempotent Accounting batch → unresolved variance/reconciliation remains explicit rather than silently closing finance truth.
