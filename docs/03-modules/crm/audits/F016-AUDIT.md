# F016 Follow-ups and reminders — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows) by reading the `leads/[id]/follow-up` route in full and `apps/web/src/orchestration/work/follow-ups.ts` in full.

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001 | PASS | scheduling a follow-up correctly creates a **real governed activity** (a call, meeting, or generic task — reusing F013/F014/F015's own commands, not a parallel write path) and sets the lead's `next_follow_up_at` in the same transaction; "My Follow-ups" aggregates these into a dueAt-sorted list. |
| CAP-002 (**multiple reminders**, working hours, **escalation**, no-activity rules, **delivery receipts**, **deduplication**, timezone behavior) | **PARTIAL — a real, well-integrated feature, but a genuinely narrower scope than the dossier's enterprise list.** Timezone: PASS — `classifyDueAt(row.nextFollowUpAt, session.timezone)` computes urgency relative to the viewer's actual timezone. **Note: a separate, more sophisticated system (`crm_lead_nurture_queue`, `lead-intelligence.js`) does implement claim/snooze/complete/exit with a real deduplication mechanism (a partial unique index, `crm_lead_nurture_queue_open_idx`) — but it is a distinct, AI-priority-scored next-best-action queue reached via `/api/crm/lead-intelligence/nurture`, not F016's `/api/crm/leads/[id]/follow-up`. I'm not treating it as F016's implementation since it's a different route, different data model, and system-generated rather than user-scheduled — but it's worth the owner confirming whether F016 and this nurture queue were meant to be the same capability, because if so, most of CAP-002 is actually satisfied elsewhere in the codebase under a different name.** Assuming they're distinct (as the routing suggests): no multiple-concurrent-reminders (one `next_follow_up_at` per lead at a time), no escalation chain, no delivery-receipt tracking for the reminder notification itself, no explicit "no-activity-in-N-days" rule engine (the aging/stale-bucket calculations found elsewhere in CRM are not wired to this feature specifically). |
| CAP-003 (notification channels consume idempotently; scope never bypassed) | PASS (for what's built) | the route enforces `crmLeadsManage`+`crmLeadsViewSensitive`+`crmActivitiesManage` together and blocks scheduling on converted/archived leads (`CRM_LEAD_FOLLOW_UP_CLOSED`) — scope is not bypassed. |
| FR-001/002/003 | PASS | validation/permission/conflict states present; not a bulk-volume feature by nature. |
| US-001/US-002 | PASS | same evidence. |
| FLOW-001/002 | PASS | two audit events are written per follow-up (`crm.{call,meeting,activities}.scheduled` and `crm.lead.followup.scheduled`), and the underlying activity's own lifecycle (start/complete/cancel) is F013/F014/F015's already-verified state machine — this feature doesn't duplicate that logic, it composes it. |
| BR-001/BR-002 | PASS | single scheduling path; the actual completion/history recording is delegated to the owning activity type's governed commands, not reimplemented here. |
| DATA-001/002 | PASS for what's built | `crm_leads.next_follow_up_at` plus whichever `crm_activities`/`crm_calls`-equivalent row was created; no separate "reminder" entity exists beyond that. |
| VAL-001/002 | PASS | `scheduleLeadFollowUpSchema` validates input; stable `CRM_LEAD_FOLLOW_UP_CLOSED` code for the one identified precondition. |
| CALC-001 | N/A | no monetary/derived calculation. |
| UX-001/002/003 | PASS (by test evidence) | "Add follow-up uses one dedicated workflow and requires a due time" and "follow-up endpoint is scoped, permissioned, atomic, audited, and updates both records" both confirmed passing in this session's earlier full CRM test run. |
| SEC-001/002 | PASS | three permissions required together (manage leads + view sensitive + manage activities) — a stricter, compound gate than most other features in this audit. |
| AUTO-001 / APP-001 / NOTIF-001 (**delivery receipts** land here too) / REP-001 / AI-001 / INT-001/002 | NOT INDEPENDENTLY VERIFIED / GAP (NOTIF-001) | Reminder delivery itself (does a push notification or email actually fire when `due_at` arrives, and is delivery confirmed?) was not found in this feature's own code path — worth a specific follow-up alongside F014's reminder gap, since both point at the same underlying question: does anything actually *notify* a user when a reminder is due? |
| API-001/002 | PASS | route pattern consistent with the rest of the module. |
| PERF-001 / E2E-001-002 / UAT-001-002 | GAP | standing gap class. |

## Net assessment

22 of 37 rows PASS. The feature that exists is well-integrated (composes F013/F014/F015 rather than duplicating them, correctly transactional, compound-permissioned) but is scoped much more narrowly than the dossier's enterprise wishlist. The most important open question isn't really a "gap" so much as a **naming/scope ambiguity worth resolving with the owner**: a separate, more sophisticated nurture-queue system already implements several of the CAP-002 items (snooze, dedup) under a different feature. Also flagging, in combination with F014: **no evidence was found anywhere in this session's CRM audit that a reminder notification is actually delivered to a user** when a due date arrives — this is worth a dedicated, cross-feature investigation before the gap-closing pass.
