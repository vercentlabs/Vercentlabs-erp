# CRM Journey — Pipeline to Forecast and Analytics

Trigger: opportunities progress through configured pipeline. Owner: CRM opportunity/analytics capabilities.

Flow: stage/probability history -> pipeline dashboard -> forecast category/period rollup -> submission -> manager adjustment -> snapshot/lock -> reports and accuracy analysis.

Controls: permission-safe aggregation, fixed-precision money, hierarchy semantics, reproducible snapshots and immutable underlying opportunity truth.
