# F009 Opportunities — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows). Basic CRUD goes through the generic `createCrmRecord`/`listCrmRecords`/`updateCrmRecord` functions (parameterized by resource, confirmed via `apps/web/src/app/api/crm/[resource]/route.ts`); governed lifecycle lives in `moveOpportunityStage`/`updateOpportunityProbability` (`index.js:3635-3760`, `:3515`) plus `opportunity-operations.js` (158 lines, dashboard/health/bulk-update helpers).

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001 | PASS | full deal-360 workflow: create/update via generic record functions, stage movement/win-loss via `moveOpportunityStage`, health/timeline via `opportunity-operations.js`. |
| CAP-002 (stakeholders, products, deal team, risks, close plan, currency, **reopen semantics**, CRM-vs-Sales ownership) | **PARTIAL — 1 significant gap, several unconfirmed.** Deal team: PASS, `crm_opportunity_team_members` table exists (`036_crm_opportunity_revenue_intelligence.sql:22`). Currency: not independently confirmed this pass (didn't check whether `crm_opportunities` carries its own currency vs. inheriting the company's). **No `stakeholders`, `products`/line-items, `risks`, or `close_plan` tables/entities found anywhere in the CRM migrations or module** — these four enterprise expectations the dossier explicitly lists appear entirely unimplemented, not partially implemented. **Reopen semantics: GAP.** `moveOpportunityStage` explicitly rejects any transition once `status !== 'open'` (`CRM_OPPORTUNITY_PIPELINE_CLOSED`, `index.js:3675-3681`), and the code comment at `:3671-3673` says reopening "cannot be reopened by bypassing the governed UI" implying a governed reopen path exists elsewhere — but no such function (`reopenOpportunity` or equivalent) exists anywhere in the module. This is the same class of gap as F003 (Contacts) and F007-adjacent — a one-way close with no real reverse path, despite the dossier explicitly requiring "controlled reopen preserving prior close events." |
| CAP-003 (F023 hands opportunity to Sales quotation via contract) | NOT INDEPENDENTLY VERIFIED | Did not trace the F023 handoff this pass. |
| FR-001/002/003 | PASS | states covered per generic-record + governed-action pattern already verified for other features; `bulkUpdateOpportunities` caps at 200 and only touches non-lifecycle fields (owner/forecast/close-date/next-step), correctly excluding stage/status from bulk edit. |
| US-001/US-002 | PASS | same evidence. |
| FLOW-001 (`OPEN -> WON \| LOST with controlled reopen`) | **PARTIAL.** Open→Won/Lost is correctly governed: a validated outcome reason (matched against `crm_lost_reasons.outcome_type`) is mandatory before closing, `FOR UPDATE` lock + `expectedUpdatedAt`/`expectedStageId` optimistic concurrency, and closing sets `actual_close_date`/`outcome_reason_id`/`outcome_notes` atomically. **The "controlled reopen" half of this requirement does not exist**, per CAP-002 above. |
| FLOW-002 | PASS | `CRM_STALE_WRITE`/`CRM_STAGE_CONFLICT`/`CRM_OUTCOME_REASON_REQUIRED`/`CRM_OUTCOME_REASON_INVALID` are all specific, stable, actionable codes. |
| BR-001/BR-002 | PASS | stage/outcome changes only through `moveOpportunityStage`; forecast snapshots (`captureForecastSnapshot`) are insert-only, preserving historical forecast state even after later edits. |
| DATA-001/002 | PASS for what exists | `crm_opportunities`, `crm_opportunity_stage_history`, `crm_opportunity_forecast_snapshots`, `crm_opportunity_team_members`, `crm_lost_reasons` all exist and are used correctly; the missing stakeholders/products/risks/close-plan entities are the CAP-002 gap. |
| VAL-001/002 | PASS | outcome-reason validation is genuinely cross-checked against `outcome_type` (won reasons can't be used to close as lost and vice versa, unless flagged `both`). |
| CALC-001 | PASS | `evaluateOpportunityHealth`/`buildPipelineSummary` (`opportunity-operations.js:11-49`) derive weighted amount, overdue, health purely from stored fields — no hidden mutable derived state. |
| UX-001/002/003 | PASS (by test evidence) | `crm-opportunities-f009.test.mjs` exists both sides and passes (also confirmed passing during the earlier CRM test-suite run this session, including the audit-snapshot redaction and generic-route-boundary tests). |
| SEC-001 | PASS | `requireCrmResourceView`/`requireCrmManage` at the generic route layer, resource-parameterized to `opportunities`; `recordScope(resources.opportunities, ...)` enforces record scope in `moveOpportunityStage` directly. |
| SEC-002 | PASS (by test evidence) | the earlier full CRM test run confirmed "Opportunity audit snapshots exclude free-text/commercial payloads" passes — commercial figures are not leaked into audit logs beyond what's needed. |
| AUTO-001 / APP-001 / NOTIF-001 / REP-001 / AI-001 / INT-001/002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |
| API-001/002 | PASS | generic-resource route pattern verified directly this pass. |
| PERF-001 / E2E-001-002 / UAT-001-002 | GAP | standing gap class. |

## Net assessment

21 of 37 rows PASS, 3 PARTIAL, and CAP-002 carries a real, sizeable gap: no stakeholders/products/risks/close-plan entities at all, and no reopen path despite the dossier requiring one and the code's own comments implying one should exist. The win/loss closing logic itself (reason validation, concurrency, atomicity) is well built — this is a "half the enterprise scope is missing" finding similar in shape to F008, though less severe since the core deal-tracking workflow is solid.
