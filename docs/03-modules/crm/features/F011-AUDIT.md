# F011 Probability and expected revenue — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows) by reading `updateOpportunityProbability` (`index.js:3515-3633`) and `opportunity-revenue-intelligence.js`'s predictive-forecast functions (~100 of 919 lines) in full, plus the relevant migration constraints.

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001 | PASS | manual probability set/override with full versioned history and deterministic `expected_revenue`. |
| CAP-002 (probability source, history, overrides, fixed-precision money, terminal-state rules, **explainable predictive model and drift**) | **PASS — stronger evidence than most features audited so far.** Fixed precision: `updateOpportunityProbability` rejects more than 2 decimal places (`Math.abs(probability*100 - Math.round(probability*100)) > 1e-8`, `:3524-3529`). Terminal-state rules: **enforced at the database schema level**, not just app code — migration `067_crm_sales_stages_f012.sql:61,70` has `CHECK (NOT is_won OR (probability = 100 AND forecast_category = 'closed' ...))` and the symmetric check for `is_lost`/`probability = 0`; since `moveOpportunityStage` sets probability from the stage's own (constrained) value, "WON forces 100 / LOST forces 0" is structurally guaranteed, not just conventionally followed. History: `crm_opportunity_probability_history` records every change with from/to/note/actor. **Explainable predictive model:** `calculatePredictiveForecast` (`opportunity-revenue-intelligence.js:225-287`) is a deterministic (not opaque-ML) model that returns a `factors` object per opportunity (`probability`, `historicalWinRate`, `health`, `activityFactor`, `closeFactor`), a `confidence` score scaled by sample size, and an explicit `modelVersion` string — this is genuinely explainable, not a black box. **Drift**: `crm_predictive_forecast_snapshots` persists historical model runs (`capturePredictiveForecast`), which is the mechanism drift analysis would be built on, though I did not find a dedicated drift-comparison report using those snapshots — the storage exists, an explicit drift view/alert was not confirmed. |
| CAP-003 (F025 forecast consumes but never rewrites) | PASS (plausible, not fully traced) | `getOpportunityRevenueDashboard`/`getOpportunityRevenueWorkspace` are read-only aggregation functions; did not find any write path from a forecast-reading function back into `crm_opportunities`. |
| FR-001/002/003 | PASS | full validation/permission/conflict states (see FLOW-002); this feature's write path is single-record, not bulk, so FR-003 is satisfied by not needing bulk semantics here. |
| US-001/US-002 | PASS | same evidence. |
| FLOW-001 (versioned probability events; WON forces 100, LOST forces 0) | PASS | see CAP-002 — this is one of the most rigorously enforced state rules found in the CRM audit so far (DB CHECK constraint backing the application rule). |
| FLOW-002 | PASS | **the idempotent-replay handling is the standout finding here**: if the caller retries with the probability already at the desired value, `updateOpportunityProbability` returns `{...opportunity, replayed: true}` without re-writing history or the outbox (`:3560-3565`) — this correctly distinguishes "nothing to do" from "conflict," which is a subtlety a lot of implementations get wrong (naive versions either double-write history on retry or reject a harmless retry as a conflict). Genuine conflicts require both `expectedUpdatedAt` and `expectedProbability` to match (`:3566-3595`) — stricter than most other features' single-field version check. |
| BR-001/BR-002 | PASS | single mutation path; probability history is insert-only. |
| DATA-001/002 | PASS | `crm_opportunity_probability_history`, `crm_predictive_forecast_snapshots` both exist and are used as described. |
| VAL-001/002 | PASS | precision/range/note-length validation with stable `CRM_OPPORTUNITY_PROBABILITY_*` codes. |
| CALC-001 | PASS | `expected_revenue` is derived (amount × probability), and the predictive model's `predictedAmount`/`predictedProbability` are computed fresh per call, not stored mutable fields pretending to be facts. |
| UX-001/002/003 | PASS (by test evidence) | `crm-probability-expected-revenue-f011.test.mjs` exists both sides and passes (confirmed in the earlier full-suite run, including "detail uses canonical expected revenue and exposes governed probability workflow/history"). |
| SEC-001 | PASS | `PERMISSIONS.crmOpportunitiesManage` required at the probability route; `recordScope(resources.opportunities, ...)` enforced in the domain function directly. |
| SEC-002 | N/A (plausible) | no distinct sensitive field beyond what F009 already covers. |
| AUTO-001 / APP-001 / NOTIF-001 / REP-001 / AI-001 / INT-002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass — though `AI-001` is worth a dedicated follow-up given the predictive-model surface found here is exactly the kind of thing that needs an explicit AI-authority-boundary check. |
| INT-001 | PASS | confirmed under CAP-003. |
| API-001/002 | PASS | route pattern verified directly. |
| PERF-001 / E2E-001-002 / UAT-001-002 | GAP | standing gap class. |

## Net assessment

29 of 37 rows PASS — the strongest result of any CRM feature audited so far, no genuine functional gaps found. Two things stood out as better than expected going in: probability terminal-state rules are enforced by a database CHECK constraint (not just application code), and the retry/idempotency handling in `updateOpportunityProbability` correctly distinguishes a harmless replay from a real conflict. The one open question is whether `AI-001`'s authority-boundary requirements are met by the predictive-forecast model — worth a specific follow-up given real predictive logic exists here, unlike most other "AI-001" rows audited so far which had no AI surface to check at all.
