# F033 Products and services — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows, same generic template) by reading `calculateLine` in full (`services/api/src/modules/sales/index.js:285-475`, the pricing/UOM/tax engine every quotation and order line goes through), `tenant.sales_quotation_lines`/`tenant.sales_order_lines` (`database/tenant/migrations/008_sales_module.sql:160-198,356+`), `tenant.items` (`001_business_data_foundation.sql:292-321`), and `tenant.item_variants` (`071_pass1_f015_f114_operational_gaps.sql:37-61`, owned by Stock/Inventory as F100).

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001/FR-001/US-001 | PASS | `calculateLine` resolves the catalog item, applies UOM conversion, price-list/pricing-rule resolution, discount, tax and cost/margin in one deterministic pass; every quotation/order line goes through it — no parallel calculation path exists. |
| CAP-002 (services vs stock items) | PASS | `item_type CHECK (IN ('product','service','consumable','asset'))` (`001_business_data_foundation.sql:292-293`) is a real, enforced distinction, exposed through `getSalesOptions`'s `items` query and usable by the UI to differentiate fulfillment-relevant items from services. |
| CAP-002 (UOM conversions) | PASS | `calculateLine:297-314` looks up `tenant.item_uom_conversions` in both directions (direct factor, or reciprocal `1/conversion_factor` when only the inverse pair is defined) and rejects the line with a stable `409` if no conversion path exists — not a silent 1:1 assumption. |
| CAP-002 (cost visibility) | PASS | Same `redactMargin` boundary verified for F031/F032: `standard_cost`/`cost_amount`/`margin_amount`/`margin_percent` are stripped from every read path unless the caller holds `sales.margin.view`, now including `getSalesOptions` after the F031 fix. |
| **CAP-002 (variants) — GAP, recorded, not fixed this pass.** | FAIL | `tenant.item_variants` exists (owned by Stock/Inventory, F100) but neither `tenant.sales_quotation_lines` nor `tenant.sales_order_lines` has a `variant_id` column — `item_id` is the only product reference (`NOT NULL REFERENCES tenant.items`). Sales cannot sell a specific variant (e.g., a size/color) distinctly from its parent item; a variant-tracked catalog degrades to item-level selection in every quotation and order. |
| **CAP-002 (write-in policy) — GAP, recorded, not fixed this pass.** | FAIL | `calculateLine`'s `itemId = uuid(line.itemId, ...)` throws a `400` if `itemId` is missing — there is no supported path to add a free-text/non-catalog line to a quotation or order. The omission-gate explicitly names "write-in policy" as a topic that must be *addressed* (allowed with a policy, or explicitly decided against); no such decision is recorded anywhere retrievable (`SALES-P2-DEC-001..006` are referenced by every Sales dossier but their actual content isn't captured in any register file this repo has), so this reads as an unaddressed gap rather than an intentional, documented exclusion. |
| VAL-001/APP-001 (manual price override) | PASS | A requested unit price that differs from the calculated price requires `sales.price.override` AND a non-empty `manualPriceReason` (`:375-383`) before the line is accepted — real segregation-of-duties enforcement, not just a UI convention. |
| CALC-001 (tax) | PASS | Correct intrastate/interstate GST split (CGST+SGST vs IGST) driven by seller/buyer state comparison, plus tax-inclusive-price back-calculation (`:415-449`) — a genuinely complete deterministic tax engine, not a flat-rate stub. |
| DATA-002/BR-002 (snapshot) | PASS | Every line snapshots `item_code_snapshot`/`item_name_snapshot`/`uom_snapshot`/`hsn_sac_snapshot` plus its own `standard_cost`/`unit_price`/`pricing_trace`/`tax_trace` at creation time (`sales_quotation_lines` columns) — a later catalog edit cannot retroactively alter what was quoted or ordered. |
| SEC-001 (company scope) | PASS | `item.company_id && item.company_id !== master.companyId` throws `409` (`:292-296`) before any pricing happens — same write-side company boundary already verified for F031/F032. |
| FR-003/PERF-001 | Same class already recorded for F031 (`getSalesOptions`'s `items` query has no `LIMIT`). |
| AUTO-001 / APP-001 (beyond price override) / NOTIF-001 / REP-001 / AI-001 / INT-001/002 / UX-001-003 / UAT-001-002 / OBS-001 / E2E-001-002 / SEC-002 (beyond cost) | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |

## Net assessment (2026-09-06)

The pricing/tax/UOM engine underneath F033 is real and unusually thorough. Two concrete, named-by-the-dossier gaps found: no item-variant support on sales lines (Stock owns variants, Sales never consumes them), and no write-in/non-catalog line capability with no documented decision either way. Both are scoped feature gaps for the Sales gap-closing pass, not security issues.
