# F038 Quotation expiry — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows) by reading `resolvePublicQuoteToken`/`recordPublicQuoteDecision` in full (`services/api/src/modules/sales/index.js:1197-1290`, the only path a customer-facing accept/reject/view goes through), grepping the entire Sales module and worker for any write of `lifecycle_status='expired'`, and re-reading `getSalesReport`'s `expiring-quotations` variant (already read for F031).

| ID | Verdict | Evidence |
|---|---|---|
| **FLOW-001 (ACTIVE/SENT -> EXPIRED) — GAP, recorded, not fixed this pass (same "wiring gap" pattern found repeatedly in CRM).** | FAIL | `valid_until` is a real column and the `lifecycle_status` CHECK constraint accepts `'expired'` as a value, but **nothing anywhere writes it** — grepped the full Sales module and the worker's scheduler/handlers for `lifecycle_status='expired'`: zero matches. `getSalesReport`'s `expiring-quotations` variant only *reads* quotations approaching `valid_until` for a manual dashboard view; there is no scheduled job (unlike CRM's `crm-automation-overdue.js`/the newly-added `crm-lead-sla-scan.js` pattern from this session) that ever transitions a quotation past its validity date into `expired`. |
| **CAP-002 (expired acceptance) — real functional gap, recorded, not fixed this pass.** | FAIL | Because nothing ever marks a quotation `expired`, and because `resolvePublicQuoteToken` (the single gateway every public view/accept/reject action calls through) checks only the **share-link's** `expires_at` (a separate, independently-configured 1-90-day token-access window) and version staleness — never the quotation's own commercial `valid_until` date — a customer can accept a quotation past its stated validity date as long as the unrelated access token hasn't separately expired. This is a genuine commercial-integrity gap (an org could end up contractually bound to honor stale pricing/terms), not just a missing status label. Not treated as the "fix immediately" security exception since it isn't an authorization/cross-tenant bug — it's a missing business rule, the same class already recorded (and fixed in the dedicated pass, not mid-trace) for CRM's F016/F005 wiring gaps. |
| CAP-002 (reminder windows) | Same standing cross-feature gap CRM's own trace already found and documented ("no confirmed reminder-notification delivery mechanism anywhere") — `expiring-quotations` is pull-based (a report someone has to open), not a push reminder. |
| CAP-002 (extensions) | **GAP, recorded.** No function anywhere extends a quotation's `valid_until` without going through a full `reviseQuotation` (which reverts to `draft` and forces reapproval/resend). There's no lighter "extend validity by N days, keep current status" affordance — extending an already-sent quotation's deadline currently requires re-sending it as a brand-new version. |
| CAP-002 (version binding) | PASS | Already verified for F037: `resolvePublicQuoteToken` rejects with `410` if `quote.current_version_id !== link.quotation_version_id`, so a customer can never act on a superseded version even before this expiry gap is fixed. |
| FLOW-002 (safe retry, single decision) | PASS | `recordPublicQuoteDecision` checks for an existing `sales_quote_decisions` row first and throws `409` on a second decision attempt — accept/reject is a one-shot action, not replayable into a duplicate commercial commitment. |
| SEC (public-endpoint scoping) | PASS | The share-link token is `sha256`-hashed at rest (verified for F036), row-locked (`FOR UPDATE`) before use, and requires both a valid, unrevoked, unexpired link **and** the correct organization/quotation pairing before any quotation content is returned. |
| AUDIT | PASS | `quotation.viewed` is recorded as a real audit event with `userId: null` (a public/anonymous actor, correctly distinguished from an internal user action) the first time a customer opens the link. |
| AUTO-001 / NOTIF-001 / REP-001 / AI-001 / INT-001/002 / UX-001-003 / VAL-001/002 (beyond above) / API-001/002 / OBS-001 / E2E-001-002 / UAT-001-002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |

## Net assessment (2026-09-06)

The most significant gap found in Sales so far outside the two already-fixed security bugs: quotation expiry is entirely unenforced. `valid_until` is stored and reportable, but no code path — scheduled or otherwise — ever transitions a quotation to `expired`, and the customer-facing acceptance gateway checks only the unrelated share-link token expiry, not the quotation's own commercial validity date. Recorded for the Sales gap-closing pass as a priority item (it's a functional/commercial-integrity defect, not just a missing label), alongside the smaller "no lightweight extension" gap.
