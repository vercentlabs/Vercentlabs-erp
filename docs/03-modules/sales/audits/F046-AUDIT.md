# F046 Stock reservation — Atomic requirement trace

**Correction (2026-09-06):** this audit originally claimed reservation was "a local illusion" with no real Stock effect. That was wrong for the *intended* path — a real orchestration layer exists (`services/api/src/orchestration/sales-stock-reservation.js`) that genuinely calls Stock's canonical `reserveStock`, which really does update `tenant.stock_balances.reserved_quantity` (confirmed by `services/api/tests/pass1-f015-f114-runtime.test.mjs`'s "Pass1 stock reservation... updates reserved balance once" test, asserting exactly one `UPDATE tenant.stock_balances SET reserved_quantity=reserved_quantity+...`). The research error was scoping the original grep to `modules/sales/` only and missing `orchestration/`.

**However, investigating this correction surfaced a real, more serious, live bug**, found while wiring an unrelated F034 fix: `reserveSalesOrderLines` (`order-governance.js`, the *local-only* mirror function the orchestration layer calls internally) was **also directly exposed** on `POST /api/sales/orders/operations` as a bare `reserve_lines` action — reachable with no Stock call at all, bypassing the orchestration layer entirely. A caller hitting that endpoint got a success response implying stock was reserved, when only the local counter moved. **Fixed as part of this pass** (see below) by removing that duplicate, unsafe route action; the correctly-integrated `pass1-operations` route (`action: "reserve-stock"`) is the only remaining reservation entry point, and the UI (`pass1-operations-workspace.tsx`) already only ever used that one — the dangerous action had no live UI caller, only latent API exposure.

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001 (request reservation without overselling) | PASS, via the correct entry point | `reserveSalesOrderLineFromStock` calls `checkSalesOrderLineAvailability` (verified F045) first, rejects if `!availability.canPromise`, then calls Stock's `reserveStock` with an idempotency key, and only mirrors the reservation into Sales' own `sales_order_line_progress.reserved_quantity` *after* the real reservation succeeds (`if (!reservation.replayed) { ...reserveSalesOrderLines... }`) — correct ordering, real effect. |
| CAP-002 (races) | PASS | `reserveStock`'s own idempotency-keyed, balance-conditioned update (verified via the runtime test) plus the availability pre-check together prevent a race from over-committing the same physical stock. |
| **CAP-001/CAP-003 (a second, unsafe entry point existed) — fixed this pass, not merely recorded.** | FIXED | `apps/web/src/app/api/sales/orders/operations/route.ts` exposed `reserveSalesOrderLines` directly via a `reserve_lines` action, with zero Stock integration — the exact "local illusion" bug the original (incorrect) audit described, except it was real for *this one specific route*, not for the feature as a whole. Removed the action and its import; added a doc-comment on `reserveSalesOrderLines` itself warning it must only be called after a real Stock reservation succeeds (i.e., from the orchestration layer), and a regression test (`apps/web/tests/sales-orders-operations-no-fake-reservation.test.mjs`) asserting the route no longer references it. |
| CAP-002 (release on cancel) | **GAP, still real, recorded.** | Neither `cancelSalesOrder` nor any other function releases a Stock reservation (`reserveStock`'s counterpart release/cancel function, if one exists, was not traced this pass) when an order is cancelled — a cancelled order's previously-reserved stock may remain reserved indefinitely. Needs tracing Stock's own reservation-release contract in a future pass. |
| FLOW-001 (REQUESTED -> RESERVED/PARTIAL/FAILED/RELEASED) | PARTIAL | RESERVED/PARTIAL (via `allocated`/`partially_allocated`) are real; RELEASED has no code path (same gap as above). |
| CAP-002 (warehouse selection, amendment deltas, backorder creation, failures/retries, reconciliation) | NOT INDEPENDENTLY VERIFIED THIS PASS | Not re-traced after the correction. |
| AUTO-001 / NOTIF-001 / REP-001 / AI-001 / UX-001-003 / API-001/002 / OBS-001 / E2E-001-002 / UAT-001-002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |

## Net assessment (2026-09-06, corrected)

The core capability is real when used correctly. The genuinely serious finding from this feature is not "reservation doesn't work" but "reservation could silently not work, depending which of two routes a caller used" — and that inconsistency has been fixed by removing the unsafe duplicate. A real, narrower gap remains: no reservation-release path on order cancellation.
