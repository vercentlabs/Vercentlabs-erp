# F057 Sales commissions — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows) by reading `createSalesCommissionRule`/`accrueSalesCommission` in full (`services/api/src/modules/sales/pass1-operations.js:129-161`), the `sales_commission_entries` schema (`database/tenant/migrations/071_pass1_f015_f114_operational_gaps.sql:163-181`), and tracing every caller of `accrueSalesCommission` (only the manual `action: "accrue-commission"` route).

This is a more nuanced finding than the F052/F054/F055/F056 cluster — the calculation core is genuinely correct, unlike those complete dead-ends; the gaps are in triggering and lifecycle progression, not the math itself.

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001 (calculate explainable accruals from governed plans) | PASS | `accrueSalesCommission` resolves the applicable rule with real precedence (an explicit `ruleId`, or the most specific active rule matching company + owner + validity window, owner-specific preferred over general via `ORDER BY owner_user_id NULLS LAST`), computes `basisAmount × ratePercent` off either `net_sales` (subtotal) or `gross_margin` (margin_amount) per the rule's own `basis`, and is idempotent by natural key (`ON CONFLICT (organization_id, sales_order_id, owner_user_id, rule_id) DO UPDATE`) — re-running accrual for the same order/owner/rule updates the same row rather than duplicating it. |
| CAP-002 (basis: revenue/margin) | PASS | Both `net_sales` and `gross_margin` bases are real, distinct, validated options (`Set(["net_sales","gross_margin"])`), not a single hardcoded basis. |
| **CAP-001 (automatic triggering) — wiring gap, recorded, not fixed this pass. Same pattern as several CRM findings this session (e.g. the Lead SLA scan).** | FAIL | `accrueSalesCommission` is reachable **only** via an explicit manual action (`action: "accrue-commission"` on the pass1-operations route) — confirmed by tracing every caller in the repo. Nothing calls it automatically when an order confirms, is invoiced, or is paid. A manager must remember to manually accrue commission for every qualifying order; nothing will do it for them. |
| **FLOW-001 (ENTRY PENDING -> APPROVED/ELIGIBLE -> PAID/REVERSED) — GAP, recorded, not fixed this pass.** | FAIL | `sales_commission_entries.status` has a real 4-value `CHECK` constraint (`accrued`/`approved`/`paid`/`reversed`), but every entry is created with (and stays at) `accrued` forever — no function anywhere transitions it to `approved`, `paid`, or `reversed`, and nothing in HR & Payroll ever reads this table (confirmed via grep) to actually pay out a commission. The schema was built for the full lifecycle; only the first step is wired. |
| CAP-002 (returns/credits, clawbacks) | Consequence of the above | Since nothing ever reverses a paid commission, a returned/credited order has no mechanism to claw back commission already accrued on it — moot until the lifecycle exists, but worth noting as the natural next gap once accrual is automated. |
| CAP-002 (splits, tiers, effective dating) | PARTIAL | Effective dating (`valid_from`/`valid_to`) is real and enforced in the rule-selection query; splits (multiple owners sharing one sale's commission) and tiered rates (different rates at different volume thresholds) have no support — one rule, one flat rate, one owner per entry. |
| CAP-003 (module boundary) | PASS | Commission entries are Sales-local records referencing `sales_orders`/`public.users` only — no direct writes into HR & Payroll's tables were found (the gap is an absent integration, not a boundary violation). |
| AUTO-001 / NOTIF-001 / REP-001 / AI-001 / UX-001-003 / VAL-002 / API-001/002 / OBS-001 / E2E-001-002 / UAT-001-002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |

## Net assessment (2026-09-06)

The calculation engine is real and correct — this distinguishes F057 from the preceding cluster of complete dead-ends. The gaps are triggering (nothing calls accrual automatically) and lifecycle completion (approval/payout/reversal states exist in the schema but are never reached) — both real, both recorded for the gap-closing pass, neither requiring the calculation logic itself to change.
