# F032 Customer addresses and contacts — Atomic requirement trace

Verified against `SUBREQUIREMENT_REGISTER.csv` (37 rows, same generic per-feature template as F031) by reading `loadDocumentContext` in full (`services/api/src/modules/sales/index.js:100-282`, the shared context-builder every quotation/order create and amend routes through), the `tenant.contacts`/`tenant.addresses` table definitions in `database/tenant/migrations/001_business_data_foundation.sql`, and the version-snapshot columns in `008_sales_module.sql`. Like F031, Sales does not own contacts/addresses — it consumes CRM's `tenant.contacts`/`tenant.addresses` (owned by the same `business_parties` aggregate) read-only, so this trace is about that consumption boundary plus purpose-specific (billing/shipping) usage on documents.

| ID | Verdict | Evidence |
|---|---|---|
| CAP-001/CAP-003/SEM-06 (consumption boundary) | PASS | Same full-file grep as F031: zero mutations against `tenant.contacts`/`tenant.addresses` anywhere in the Sales module — only reads. |
| FR-001/US-001/VAL-001 (purpose-specific, permission-safe usage) | PASS | `loadDocumentContext` (`:174-207`) resolves an optional `contactId` and separate `billingAddressId`/`shippingAddressId`, each independently validated to belong to the *same* `party_id` the document is being created for (`AND party_id=$2 AND id=$3`) — picking another customer's contact or address throws a labeled `409` ("The selected Billing address does not belong to the customer."), not a silent cross-customer leak. |
| SEM-02 (identity, reference integrity, deduplication) | PASS | `contacts_primary_party_idx` is a partial unique index enforcing at most one `is_primary=true` active contact per party (`001_business_data_foundation.sql:234-236`) — real DB-level dedup of the "primary contact" concept, not just an app-layer convention. |
| DATA-002/BR-002 (immutable historical snapshot) | PASS | `sales_quotation_versions`/`sales_order_versions` persist `contact_snapshot`, `billing_address_snapshot` and `shipping_address_snapshot` (`:2384` area) at version-creation time, alongside the `customer_snapshot` already verified for F031 — a later edit or archival of a contact/address cannot retroactively alter what an accepted quotation or confirmed order actually shipped to/billed. |
| BR-001 (single authoritative path) | PASS | Every quotation/order create and amend path routes through the same `loadDocumentContext`, so contact/address selection rules cannot be bypassed by a different entry point within Sales. |
| SEC-001 (company scope) | PASS (after F031's fix) | The company boundary for a document's customer, contact and addresses is enforced up front — `party.company_id !== companyId` throws before contact/address resolution ever runs (`:156-160`), and `companyId` is itself cross-checked against `context.activeCompanyId` (`:113-121`) unless `allowAllCompanies`. The *read-side* equivalent (the `getSalesOptions` contacts/addresses picker queries) was the one place this boundary was missing until the F031 fix — that fix's `party.company_id` join now scopes both `contacts` and `addresses` in the picker the same way this write path already did, closing the same class of gap on the read side too. |
| SEC-002 (contact PII / restricted fields) | PASS (no gap found) | Unlike CRM's Lead PII (which the CRM pass specifically redacted), Sales contact email/phone/mobile carry no confidentiality flag in the schema and are ordinary data any `sales.view` holder needs to actually contact the customer — consistent with `credit_limit` being similarly unredacted everywhere in this module (verified during F031). Cost/margin are the only fields this module treats as field-level-restricted, and those are already covered by F031's `redactMargin` fix. |
| FR-003/PERF-001 (volume/pagination) | **GAP, same class already recorded for F031.** `getSalesOptions`'s `contacts`/`addresses` queries have no `LIMIT`, same as its `parties` query — covered by the single Sales gap-closing item already logged in `docs/PRODUCTION_TRACKER.md`. |
| AUTO-001 / APP-001 / NOTIF-001 / REP-001 / AI-001 / INT-001/002 / UX-001-003 / CALC-001 / API-001/002 / OBS-001 / E2E-001-002 / UAT-001-002 | NOT INDEPENDENTLY VERIFIED | Not traced this pass. |

## Net assessment (2026-09-06)

No new gap beyond the one already recorded for F031 (unbounded picker queries — same root cause, same fix scope). The write-side company/customer-ownership boundary for contacts and addresses was already solid before this session's fixes; the read-side gap the F031 fix closed applies here too since it's the same `getSalesOptions` queries.
