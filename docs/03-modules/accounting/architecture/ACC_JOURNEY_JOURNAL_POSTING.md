# Journal posting and reversal

Draft → validate accounts/dimensions/period/currency → approval where required → atomic balanced post → ledger/audit/outbox. Retry uses stable idempotency/source identity. Correction uses linked reversal/adjustment; posted truth is immutable.
