# Recurring, accrual, prepayment and revenue schedules

Create versioned schedule → generate occurrence identity → post each occurrence exactly once → link reversal/recognition → handle period locks and source changes explicitly → reconcile schedule balance to ledger.
