# Budget to actual

Version/approve budget by period/dimension/currency → lock baseline where required → compare to ledger actuals using consistent mappings → drill variance to source → controlled forecast/revision without rewriting historical baseline.
