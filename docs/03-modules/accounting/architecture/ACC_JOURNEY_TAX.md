# Tax to report

Resolve effective-dated tax configuration from authoritative maintained sources → calculate deterministically with explicit rounding → post tax components → reconcile source transactions to tax ledgers/report data → review exceptions → produce dated report evidence.
