# Benchmark Evidence Relevance Review

Every captured benchmark row must support the feature/capability it is mapped to, not merely come from an authoritative vendor. Review feature-to-source semantic relevance, publication/update/access dates, official-source preference and decision disposition. Known mismatches such as a payment-network source attached to a barcode-scanning requirement must be corrected before architecture freeze.
