# Agent Work Package Evidence

Store one evidence packet per registered AWP using the exact work-package ID, for example:

`AWP-W03-F002-01.md`

Evidence is package-specific. It may support a canonical wave exit gate but never marks the wave COMPLETE by itself.
