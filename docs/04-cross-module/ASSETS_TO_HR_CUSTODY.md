# Contract — Assets custody to HR identity

Assets references employee/user IDs through an HR/directory public query and snapshots display attributes only as policy allows. Assignment/transfer history remains in Assets. HR changes cannot silently rewrite historical custodian evidence; deactivation/termination produces an exception requiring reassignment under policy.
