import { theme } from "../theme.ts";

/** 4px-grid spacing scale, in px. Keys are the step number (theme.json's
 * spacing.4 === 16px, i.e. step 4 on the 4px grid). */
export const spacing = theme.spacing;
