import { ListPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Supplier categories" };

export default function Page() {
  return <ListPage name="categories" />;
}
