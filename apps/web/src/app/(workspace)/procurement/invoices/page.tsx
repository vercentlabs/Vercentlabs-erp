import { RegisterPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Supplier invoices" };

export default function Page() {
  return <RegisterPage name="invoices" />;
}
