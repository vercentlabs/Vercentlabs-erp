import { RegisterPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Supplier lead times" };

export default function Page() {
  return <RegisterPage name="lead-times" />;
}
