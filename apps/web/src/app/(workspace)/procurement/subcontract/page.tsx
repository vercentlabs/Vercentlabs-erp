import { RegisterPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Subcontract orders" };

export default function Page() {
  return <RegisterPage name="subcontract" />;
}
