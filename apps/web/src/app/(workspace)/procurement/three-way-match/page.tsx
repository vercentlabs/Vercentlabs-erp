import { ListPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Three-way match" };

export default function Page() {
  return <ListPage name="exceptions" />;
}
