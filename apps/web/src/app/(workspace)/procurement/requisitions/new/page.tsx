import { FormPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "New requisition" };

export default function Page() {
  return <FormPage name="requisitions" />;
}
