import { ListPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Requisitions" };

export default function Page() {
  return <ListPage name="requisitions" />;
}
