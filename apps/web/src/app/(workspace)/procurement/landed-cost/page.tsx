import { RegisterPage } from "@/features/procurement/registry/registry";

export const metadata = { title: "Landed cost" };

export default function Page() {
  return <RegisterPage name="landed-cost" />;
}
