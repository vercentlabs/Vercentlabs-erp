import { getProcurementDashboard } from "@vercentlabs/api";import { procurementSession,tenantTransaction } from "@/modules/procurement/server";import { errorResponse,ok } from "@/core/http";export async function GET(){try{const{context}=await procurementSession();return ok({dashboard:await tenantTransaction(context.organizationId,c=>getProcurementDashboard(c,context))});}catch(error){return errorResponse(error);}}
