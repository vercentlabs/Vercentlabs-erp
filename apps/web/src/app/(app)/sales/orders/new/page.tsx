import SalesDocumentEditor from "@/modules/sales/components/document-editor";import { requirePermission } from "@/core/authorization";export default async function NewOrderPage(){await requirePermission("sales.order.create");return <><section className="page-heading"><div><p className="eyebrow">Sales · New order</p><h1>Create a direct sales order</h1><p>Use direct orders only when the customer commitment does not require a quotation.</p></div></section><SalesDocumentEditor mode="order"/></>}
