import SalesDocumentEditor from "@/modules/sales/components/document-editor";import { PageHeader } from "@/shared/design";import { requirePermission } from "@/core/authorization";export default async function NewOrderPage(){await requirePermission("sales.order.create");return <><PageHeader eyebrow="Sales · New order" title="Create a direct sales order" description="Use direct orders only when the customer commitment does not require a quotation." /><SalesDocumentEditor mode="order"/></>}
