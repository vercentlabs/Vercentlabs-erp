import { PageHeader } from "@/shared/design";import { requirePermission } from "@/core/authorization";export default async function SalesSettingsPage(){await requirePermission("sales.settings.manage");return <><PageHeader eyebrow="Sales · Settings" title="Commercial controls" description="Configure approval thresholds, margin floors, seller GST state, default price list and invoicing basis through governed master-data migrations and settings APIs." /><section className="panel"><h2>Foundation installed</h2><p>The Sales settings record is provisioned per organisation. Administrative editing remains intentionally restricted until a dedicated settings form is connected to the same validation rules as Sales documents.</p></section></>}
