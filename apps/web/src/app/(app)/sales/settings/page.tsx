import { requirePermission } from "@/core/authorization";export default async function SalesSettingsPage(){await requirePermission("sales.settings.manage");return <><section className="page-heading"><div><p className="eyebrow">Sales · Settings</p><h1>Commercial controls</h1><p>Configure approval thresholds, margin floors, seller GST state, default price list and invoicing basis through governed master-data migrations and settings APIs.</p></div></section><section className="panel"><h2>Foundation installed</h2><p>The Sales settings record is provisioned per organisation. Administrative editing remains intentionally restricted until a dedicated settings form is connected to the same validation rules as Sales documents.</p></section></>}
