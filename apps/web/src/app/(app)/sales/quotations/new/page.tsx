import SalesDocumentEditor from "@/modules/sales/components/document-editor";import { PageHeader } from "@/shared/design";import { requirePermission } from "@/core/authorization";export default async function NewQuotationPage({searchParams}:{searchParams:Promise<{opportunityId?:string}>}){await requirePermission("sales.quotation.create");const {opportunityId}=await searchParams;return <><PageHeader eyebrow="Sales · New quotation" title="Build a governed commercial proposal" description="Pricing, GST, UOM conversion, rounding and approval controls are calculated on the server." /><SalesDocumentEditor mode="quotation" opportunityId={opportunityId}/></>}
