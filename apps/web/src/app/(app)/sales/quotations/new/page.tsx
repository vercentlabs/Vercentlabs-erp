import SalesDocumentEditor from "@/modules/sales/components/document-editor";import { requirePermission } from "@/core/authorization";export default async function NewQuotationPage({searchParams}:{searchParams:Promise<{opportunityId?:string}>}){await requirePermission("sales.quotation.create");const {opportunityId}=await searchParams;return <><section className="page-heading"><div><p className="eyebrow">Sales · New quotation</p><h1>Build a governed commercial proposal</h1><p>Pricing, GST, UOM conversion, rounding and approval controls are calculated on the server.</p></div></section><SalesDocumentEditor mode="quotation" opportunityId={opportunityId}/></>}
