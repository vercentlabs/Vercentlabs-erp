import { getAccountingOptions } from "@vercentlabs/api";import SubledgerDocumentEditor from "@/modules/accounting/components/subledger-document-editor";import { requireWorkspace } from "@/core/auth";import { hasPermission,PERMISSIONS } from "@/core/authorization";import { accountingContext } from "@/modules/accounting";import { tenantTransaction } from "@/core/db";export const dynamic="force-dynamic";type Option={id:string;code?:string;name?:string;display_name?:string;party_type?:string;currency_code?:string;ledger_id?:string;account_class?:string;is_group?:boolean};
export default async function NewPayablePage(){const session=await requireWorkspace();if(!hasPermission(session,PERMISSIONS.accountingPayablesManage))return <section className="panel"><h1>Payables permission required</h1></section>;if(!session.activeCompanyId)return <section className="panel"><h1>Select a company first</h1></section>;const context=accountingContext(session);const data=await tenantTransaction(context.organizationId,(client)=>getAccountingOptions(client,context,session.activeCompanyId)) as unknown as {company:{id:string;base_currency:string};parties:Option[];ledgers:Option[];accounts:Option[]};return <><section className="page-heading"><div><p className="eyebrow">Accounts payable</p><h1>New vendor bill</h1><p>Create a draft supplier liability. Posting will create expense, input-tax and payable entries.</p></div></section><SubledgerDocumentEditor kind="payable" companyId={data.company.id} baseCurrency={data.company.base_currency} parties={data.parties} ledgers={data.ledgers} accounts={data.accounts}/></>}
