import {requireWorkspace} from "@/lib/auth";export default async function Page({params}:{params:Promise<{id:string}>}){await requireWorkspace();const{id}=await params;return <><section className="page-heading"><div><p className="eyebrow">Procurement document</p><h1>Orders detail</h1><p>Document {id} with governed lifecycle, approvals, history and cross-module handoffs.</p></div></section><section className="panel"><h2>Lifecycle and evidence</h2><p>Review line details, supplier context, approvals, versions, exceptions and audit history.</p></section></>;}