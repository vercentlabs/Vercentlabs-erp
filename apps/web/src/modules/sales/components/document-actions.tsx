"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function SalesDocumentActions({ type, id, status }: { type:"quotation"|"order"; id:string; status:string }) {
 const router=useRouter();const [pending,setPending]=useState(false);const [message,setMessage]=useState("");
 async function act(action:string,extra:Record<string,unknown>={}){setPending(true);setMessage("");try{const endpoint=type==="quotation"?`/api/sales/quotations/${id}/actions`:`/api/sales/orders/${id}/actions`;const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,...extra})});const data=await response.json();if(!data.ok)throw new Error(data.message);if(action==="send"&&data.result?.token)setMessage(`Customer link: ${location.origin}/quote/${data.result.token}`);else setMessage("Action completed.");if(action==="convert"&&data.result?.orderId)router.push(`/sales/orders/${data.result.orderId}`);router.refresh();}catch(error){setMessage(error instanceof Error?error.message:"Action failed.");}finally{setPending(false);}}
 return <section className="panel sales-command-bar"><div><p className="eyebrow">Document actions</p>{message?<p className="sales-action-message">{message}</p>:<p>Every action is permission checked and added to the document history.</p>}</div><div className="sales-actions">{type==="quotation"&&status==="draft"?<button className="primary-button" disabled={pending} onClick={()=>act("submit")}>Submit</button>:null}{type==="quotation"&&status==="approved"?<button className="primary-button" disabled={pending} onClick={()=>act("send",{expiresInDays:30})}>Send securely</button>:null}{type==="quotation"&&status==="accepted"?<button className="primary-button" disabled={pending} onClick={()=>act("convert")}>Create sales order</button>:null}{type==="order"&&status==="draft"?<button className="primary-button" disabled={pending} onClick={()=>act("confirm")}>Confirm order</button>:null}{type==="order"&&status==="confirmed"?<><button className="secondary-button" disabled={pending} onClick={()=>act("request_fulfillment",{idempotencyKey:crypto.randomUUID()})}>Request fulfilment</button><button className="secondary-button" disabled={pending} onClick={()=>act("request_invoice",{idempotencyKey:crypto.randomUUID(),quantityBasis:"ordered"})}>Request invoice</button><button className="secondary-button" disabled={pending} onClick={()=>act("hold",{holdType:"commercial",reason:"Manual commercial review"})}>Place hold</button></>:null}{type==="order"&&status==="on_hold"?<button className="secondary-button" disabled>Release from the active hold record</button>:null}{type==="order"&&(status==="confirmed"||status==="on_hold")?<button className="secondary-button" disabled={pending} onClick={()=>act("close")}>Close order</button>:null}</div></section>;
}
