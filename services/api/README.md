# Vercent ERP API

Permanent backend service boundary.

Authentication and onboarding currently use protected Next.js Route Handlers in `apps/web`.
New business-domain APIs and future extraction work belong here without duplicating the
working authentication foundation.
