# Product evidence

Each file here records the concrete, runtime proof (commands executed, test
results, migrations run) that backs a specific SP or F identifier's status in
`product/registers/shared-platform.yaml`. `PROMPT-001-FOUNDATION.md` is the
evidence record for this prompt (engineering foundation only, no SP or F
identifier moved past `NOT_STARTED`).
